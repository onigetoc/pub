import { DOMParser } from '@xmldom/xmldom';

// During an iterative conversion (see tree-to-latex) every child is converted
// before its parent and its LaTeX is stored in this memo. While a memo is
// active, the helper returns the child's precomputed string instead of building
// a converter that would recurse — keeping the call stack flat at any depth.
let conversionMemo = null;
/**
 * Activates (or clears, with `null`) the memo consulted by
 * {@link mathMLElementToLaTeXConverter}.
 *
 * @param memo - the memo to make active, or `null` to disable memoization.
 * @returns the previously active memo, so callers can restore it afterwards.
 */
const setConversionMemo = (memo) => {
    const previous = conversionMemo;
    conversionMemo = memo;
    return previous;
};
/**
 * Resolves the {@link ToLaTeXConverter} for an element. While a memo is active
 * and already contains the element, returns a converter that yields the
 * precomputed LaTeX (no recursion); otherwise builds the real converter.
 *
 * @param mathMLElement - the element to convert.
 * @returns a converter bound to the element.
 */
const mathMLElementToLaTeXConverter = (mathMLElement) => {
    const precomputed = conversionMemo === null || conversionMemo === void 0 ? void 0 : conversionMemo.get(mathMLElement);
    if (precomputed !== undefined)
        return new PrecomputedConverter(precomputed);
    return new MathMLElementToLatexConverterAdapter(mathMLElement).toLatexConverter();
};
class PrecomputedConverter {
    constructor(_latex) {
        this._latex = _latex;
    }
    convert() {
        return this._latex;
    }
}

/**
 * Collapses every run of whitespace in the string into a single space.
 *
 * @param str - the string to normalize.
 * @returns the string with runs of whitespace reduced to single spaces.
 */
const normalizeWhiteSpaces = (str) => {
    return str.replace(/\s+/g, ' ');
};

/**
 * Converts a MathML `<math>` root element into LaTeX.
 *
 * Recursively converts every child to LaTeX, joins the results with spaces
 * and normalizes the resulting whitespace.
 *
 * @example
 * // <math><mi>x</mi><mo>=</mo><mn>1</mn></math> -> x = 1
 */
let Math$1 = class Math {
    constructor(mathElement) {
        this._mathmlElement = mathElement;
    }
    /**
     * @returns the LaTeX representation of this element.
     */
    convert() {
        const unnormalizedLatex = this._mathmlElement.children
            .map((child) => mathMLElementToLaTeXConverter(child))
            .map((converter) => converter.convert())
            .join(' ');
        return normalizeWhiteSpaces(unnormalizedLatex);
    }
};

/**
 * Base helper that surrounds a string with an opening and a closing delimiter.
 */
class Wrapper {
    constructor(open, close) {
        this._open = open;
        this._close = close;
    }
    /** @returns `str` surrounded by the configured open/close delimiters. */
    wrap(str) {
        return this._open + str + this._close;
    }
}

/**
 * Wraps a string in LaTeX curly braces (`{ ... }`), e.g. for subscript and
 * superscript arguments.
 */
class BracketWrapper {
    constructor() {
        this._open = '{';
        this._close = '}';
    }
    /** @returns `str` wrapped in curly braces. */
    wrap(str) {
        return new Wrapper(this._open, this._close).wrap(str);
    }
}

/**
 * Wraps a string in auto-sizing LaTeX parentheses (`\left( ... \right)`).
 */
class ParenthesisWrapper {
    constructor() {
        this._open = '\\left(';
        this._close = '\\right)';
    }
    /** @returns `str` wrapped in `\left(`/`\right)`. */
    wrap(str) {
        return new Wrapper(this._open, this._close).wrap(str);
    }
    /** @returns `str` wrapped in parentheses only when it is longer than one char. */
    wrapIfMoreThanOneChar(str) {
        if (str.length <= 1)
            return str;
        return this.wrap(str);
    }
}

/**
 * Lookup table mapping math operators, keyed by their literal HTML character
 * reference (e.g. `&#x2211;`), to the equivalent LaTeX command. Consumed by the
 * `<mo>` converter to translate operator content into LaTeX.
 */
const allMathOperatorsByChar = {
    _: '\\underline',
    '&#x23E1;': '\\underbrace',
    '&#x23E0;': '\\overbrace',
    '&#x23DF;': '\\underbrace',
    '&#x23DE;': '\\overbrace',
    '&#x23DD;': '\\underbrace',
    '&#x23DC;': '\\overbrace',
    '&#x23B5;': '\\underbrace',
    '&#x23B4;': '\\overbrace',
    '&#x20DC;': '\\square',
    '&#x20DB;': '\\square',
    '&#x2064;': '',
    '&#x2057;': "''''",
    '&#x203E;': '\\bar',
    '&#x2037;': '```',
    '&#x2036;': '``',
    '&#x2035;': '`',
    '&#x2034;': "'''",
    '&#x2033;': "''",
    '&#x201F;': '``',
    '&#x201E;': ',,',
    '&#x201B;': '`',
    '&#x201A;': ',',
    '&#x302;': '\\hat',
    '&#x2F7;': '\\sim',
    '&#x2DD;': '\\sim',
    '&#x2DC;': '\\sim',
    '&#x2DA;': '\\circ',
    '&#x2D9;': '\\cdot',
    '&#x2D8;': '',
    '&#x2CD;': '\\_',
    '&#x2CB;': 'ˋ',
    '&#x2CA;': 'ˊ',
    '&#x2C9;': 'ˉ',
    '&#x2C7;': '',
    '&#x2C6;': '\\hat',
    '&#xBA;': 'o',
    '&#xB9;': '1',
    '&#xB8;': '¸',
    '&#xB4;': '´',
    '&#xB3;': '3',
    '&#xB2;': '2',
    '&#xB0;': '\\circ',
    '&#xAF;': '\\bar',
    '&#xAA;': 'a',
    '&#xA8;': '\\cdot\\cdot',
    '~': '\\sim',
    '`': '`',
    '^': '\\hat',
    '--': '--',
    '++': '++',
    '&amp;': '\\&',
    '&#x2061;': '',
    '&#x221C;': '\\sqrt[4]{}',
    '&#x221B;': '\\sqrt[3]{}',
    '&#x221A;': '\\sqrt{}',
    '&#x2146;': 'd',
    '&#x2145;': '\\mathbb{D}',
    '?': '?',
    '@': '@',
    '//': '//',
    '!!': '!!',
    '!': '!',
    '&#x266F;': '\\#',
    '&#x266E;': '',
    '&#x266D;': '',
    '&#x2032;': "'",
    '&lt;>': '<>',
    '**': '\\star\\star',
    '&#x2207;': '\\nabla',
    '&#x2202;': '\\partial',
    '&#x2299;': '\\bigodot',
    '&#xAC;': '\\neg',
    '&#x2222;': '\\measuredangle',
    '&#x2221;': '\\measuredangle',
    '&#x2220;': '\\angle',
    '&#xF7;': '\\div',
    '/': '/',
    '&#x2216;': '\\backslash',
    '\\': '\\backslash',
    '%': '\\%',
    '&#x2297;': '\\bigotimes',
    '&#xB7;': '\\cdot',
    '&#x2A3F;': '\\coprod',
    '&#x2A2F;': '\\times',
    '&#x22C5;': '\\cdot',
    '&#x22A1;': '\\boxdot',
    '&#x22A0;': '\\boxtimes',
    '&#x2062;': '',
    '&#x2043;': '-',
    '&#x2022;': '\\cdot',
    '&#xD7;': '\\times',
    '.': '.',
    '*': '\\star',
    '&#x222A;': '\\cup',
    '&#x2229;': '\\cap',
    '&#x2210;': '\\coprod',
    '&#x220F;': '\\prod',
    '&#x2240;': '',
    '&#x2AFF;': '',
    '&#x2AFC;': '\\mid\\mid\\mid',
    '&#x2A09;': '\\times',
    '&#x2A08;': '',
    '&#x2A07;': '',
    '&#x2A06;': '\\sqcup',
    '&#x2A05;': '\\sqcap',
    '&#x2A02;': '\\otimes',
    '&#x2A00;': '\\odot',
    '&#x22C2;': '\\cap',
    '&#x22C1;': '\\vee',
    '&#x22C0;': '\\wedge',
    '&#x2A04;': '\\uplus',
    '&#x2A03;': '\\cup',
    '&#x22C3;': '\\cup',
    '&#x2A1C;': '\\underline{\\int}',
    '&#x2A1B;': '\\overline{\\int}',
    '&#x2A1A;': '\\int',
    '&#x2A19;': '\\int',
    '&#x2A18;': '\\int',
    '&#x2A17;': '\\int',
    '&#x2A16;': '\\oint',
    '&#x2A15;': '\\oint',
    '&#x2A14;': '\\int',
    '&#x2A13;': '\\int',
    '&#x2A12;': '\\int',
    '&#x2A11;': '\\int',
    '&#x2A10;': '\\int',
    '&#x2A0F;': '\\bcancel{\\int}',
    '&#x2A0E;': '',
    '&#x2A0D;': '\\hcancel{\\int}',
    '&#x2A0C;': '\\iiiint',
    '&#x2233;': '\\oint',
    '&#x2232;': '\\oint',
    '&#x2231;': '\\int',
    '&#x2230;': '\\oiint',
    '&#x222F;': '\\oiint',
    '&#x222E;': '\\oint',
    '&#x222B;': '\\int',
    '&#x2A01;': '\\oplus',
    '&#x2298;': '\\oslash',
    '&#x2296;': '\\ominus',
    '&#x2295;': '\\oplus',
    '&#x222D;': '\\iiint',
    '&#x222C;': '\\iint',
    '&#x2A0B;': '',
    '&#x2A0A;': '',
    '&#x2211;': '\\sum',
    '&#x229F;': '\\boxminus',
    '&#x229E;': '\\boxplus',
    '&#x2214;': '\\dot{+}',
    '&#x2213;': '+-',
    '&#x2212;': '-',
    '&#xB1;': '\\pm',
    '-': '-',
    '+': '+',
    '&#x2B46;': '\\Rrightarrow',
    '&#x2B45;': '\\Lleftarrow',
    '&#x29F4;': ':\\rightarrow',
    '&#x29EF;': '',
    '&#x29DF;': '\\bullet-\\bullet',
    '&#x299F;': '\\angle',
    '&#x299E;': '\\measuredangle',
    '&#x299D;': '\\measuredangle',
    '&#x299C;': '\\perp',
    '&#x299B;': '\\measuredangle',
    '&#x299A;': '',
    '&#x2999;': '\\vdots',
    '&#x297F;': '',
    '&#x297E;': '',
    '&#x297D;': '\\prec',
    '&#x297C;': '\\succ',
    '&#x297B;': '\\underset{\\rightarrow}{\\supset}',
    '&#x297A;': '',
    '&#x2979;': '\\underset{\\rightarrow}{\\subset}',
    '&#x2978;': '\\underset{\\rightarrow}{>}',
    '&#x2977;': '',
    '&#x2976;': '\\underset{\\leftarrow}{<}',
    '&#x2975;': '\\underset{\\approx}{\\rightarrow}',
    '&#x2974;': '\\underset{\\sim}{\\rightarrow}',
    '&#x2973;': '\\underset{\\sim}{\\leftarrow}',
    '&#x2972;': '\\overset{\\sim}{\\rightarrow}',
    '&#x2971;': '\\overset{=}{\\rightarrow}',
    '&#x2970;': '',
    '&#x296F;': '',
    '&#x296E;': '',
    '&#x296D;': '\\overline{\\rightharpoondown}',
    '&#x296C;': '\\underline{\\rightharpoonup}',
    '&#x296B;': '\\overline{\\leftharpoondown}',
    '&#x296A;': '\\underline{\\leftharpoonup}',
    '&#x2969;': '\\rightleftharpoons',
    '&#x2968;': '\\rightleftharpoons',
    '&#x2967;': '\\rightleftharpoons',
    '&#x2966;': '\\rightleftharpoons',
    '&#x2965;': '\\Downarrow',
    '&#x2964;': '\\Rightarrow',
    '&#x2963;': '\\Uparrow',
    '&#x2962;': '\\Leftarrow',
    '&#x2961;': '\\downarrow',
    '&#x2960;': '\\uparrow',
    '&#x295F;': '\\rightarrow',
    '&#x295E;': '\\leftarrow',
    '&#x295D;': '\\downarrow',
    '&#x295C;': '\\uparrow',
    '&#x295B;': '\\rightarrow',
    '&#x295A;': '\\leftarrow',
    '&#x2959;': '\\downarrow',
    '&#x2958;': '\\uparrow',
    '&#x2957;': '\\rightarrow',
    '&#x2956;': '\\leftarrow',
    '&#x2955;': '\\downarrow',
    '&#x2954;': '\\uparrow',
    '&#x2953;': '\\rightarrow',
    '&#x2952;': '\\leftarrow',
    '&#x2951;': '\\updownarrow',
    '&#x2950;': '\\leftrightarrow',
    '&#x294F;': '\\updownarrow',
    '&#x294E;': '\\leftrightarrow',
    '&#x294D;': '\\updownarrow',
    '&#x294C;': '\\updownarrow',
    '&#x294B;': '\\leftrightarrow',
    '&#x294A;': '\\leftrightarrow',
    '&#x2949;': '',
    '&#x2948;': '\\leftrightarrow',
    '&#x2947;': '\\nrightarrow',
    '&#x2946;': '',
    '&#x2945;': '',
    '&#x2944;': '\\rightleftarrows',
    '&#x2943;': '\\leftrightarrows',
    '&#x2942;': '\\rightleftarrows',
    '&#x2941;': '\\circlearrowright',
    '&#x2940;': '\\circlearrowleft',
    '&#x293F;': '\\rightarrow',
    '&#x293E;': '\\leftarrow',
    '&#x293D;': '',
    '&#x293C;': '',
    '&#x293B;': '',
    '&#x293A;': '',
    '&#x2939;': '',
    '&#x2938;': '',
    '&#x2937;': '\\Rsh',
    '&#x2936;': '\\Lsh',
    '&#x2935;': '\\downarrow',
    '&#x2934;': '\\uparrow',
    '&#x2933;': '\\leadsto',
    '&#x2932;': '',
    '&#x2931;': '',
    '&#x2930;': '',
    '&#x292F;': '',
    '&#x292E;': '',
    '&#x292D;': '',
    '&#x292C;': '\\times',
    '&#x292B;': '\\times',
    '&#x292A;': '',
    '&#x2929;': '',
    '&#x2928;': '',
    '&#x2927;': '',
    '&#x2926;': '',
    '&#x2925;': '',
    '&#x2924;': '',
    '&#x2923;': '',
    '&#x2922;': '',
    '&#x2921;': '',
    '&#x2920;': '\\mapsto\\cdot',
    '&#x291F;': '\\cdot\\leftarrow',
    '&#x291E;': '\\rightarrow\\cdot',
    '&#x291D;': '\\leftarrow',
    '&#x291C;': '\\rightarrow',
    '&#x291B;': '\\leftarrow',
    '&#x291A;': '\\rightarrow',
    '&#x2919;': '\\leftarrow',
    '&#x2918;': '\\rightarrow',
    '&#x2917;': '\\rightarrow',
    '&#x2916;': '\\rightarrow',
    '&#x2915;': '\\rightarrow',
    '&#x2914;': '\\rightarrow',
    '&#x2913;': '\\downarrow',
    '&#x2912;': '\\uparrow',
    '&#x2911;': '\\rightarrow',
    '&#x2910;': '\\rightarrow',
    '&#x290F;': '\\rightarrow',
    '&#x290E;': '\\leftarrow',
    '&#x290D;': '\\rightarrow',
    '&#x290C;': '\\leftarrow',
    '&#x290B;': '\\Downarrow',
    '&#x290A;': '\\Uparrow',
    '&#x2909;': '\\uparrow',
    '&#x2908;': '\\downarrow',
    '&#x2907;': '\\Rightarrow',
    '&#x2906;': '\\Leftarrow',
    '&#x2905;': '\\mapsto',
    '&#x2904;': '\\nLeftrightarrow',
    '&#x2903;': '\\nRightarrow',
    '&#x2902;': '\\nLeftarrow',
    '&#x2901;': '\\rightsquigarrow',
    '&#x2900;': '\\rightsquigarrow',
    '&#x27FF;': '\\rightsquigarrow',
    '&#x27FE;': '\\Rightarrow',
    '&#x27FD;': '\\Leftarrow',
    '&#x27FC;': '\\mapsto',
    '&#x27FB;': '\\leftarrow',
    '&#x27FA;': '\\Longleftrightarrow',
    '&#x27F9;': '\\Longrightarrow',
    '&#x27F8;': '\\Longleftarrow',
    '&#x27F7;': '\\leftrightarrow',
    '&#x27F6;': '\\rightarrow',
    '&#x27F5;': '\\leftarrow',
    '&#x27F1;': '\\Downarrow',
    '&#x27F0;': '\\Uparrow',
    '&#x22B8;': '\\rightarrow',
    '&#x21FF;': '\\leftrightarrow',
    '&#x21FE;': '\\rightarrow',
    '&#x21FD;': '\\leftarrow',
    '&#x21FC;': '\\nleftrightarrow',
    '&#x21FB;': '\\nrightarrow',
    '&#x21FA;': '\\nleftarrow',
    '&#x21F9;': '\\nleftrightarrow',
    '&#x21F8;': '\\nrightarrow',
    '&#x21F7;': '\\nleftarrow',
    '&#x21F6;': '\\Rrightarrow',
    '&#x21F5;': '',
    '&#x21F4;': '\\rightarrow',
    '&#x21F3;': '\\Updownarrow',
    '&#x21F2;': '\\searrow',
    '&#x21F1;': '\\nwarrow',
    '&#x21F0;': '\\Leftarrow',
    '&#x21EF;': '\\Uparrow',
    '&#x21EE;': '\\Uparrow',
    '&#x21ED;': '\\Uparrow',
    '&#x21EC;': '\\Uparrow',
    '&#x21EB;': '\\Uparrow',
    '&#x21EA;': '\\Uparrow',
    '&#x21E9;': '\\Downarrow',
    '&#x21E8;': '\\Rightarrow',
    '&#x21E7;': '\\Uparrow',
    '&#x21E6;': '\\Leftarrow',
    '&#x21E5;': '\\rightarrow',
    '&#x21E4;': '\\leftarrow',
    '&#x21E3;': '\\downarrow',
    '&#x21E2;': '\\rightarrow',
    '&#x21E1;': '\\uparrow',
    '&#x21E0;': '\\leftarrow',
    '&#x21DF;': '\\downarrow',
    '&#x21DE;': '\\uparrow',
    '&#x21DD;': '\\rightsquigarrow',
    '&#x21DC;': '\\leftarrow',
    '&#x21DB;': '\\Rrightarrow',
    '&#x21DA;': '\\Lleftarrow',
    '&#x21D9;': '\\swarrow',
    '&#x21D8;': '\\searrow',
    '&#x21D7;': '\\nearrow',
    '&#x21D6;': '\\nwarrow',
    '&#x21D5;': '\\Updownarrow',
    '&#x21D4;': '\\Leftrightarrow',
    '&#x21D3;': '\\Downarrow',
    '&#x21D2;': '\\Rightarrow',
    '&#x21D1;': '\\Uparrow',
    '&#x21D0;': '\\Leftarrow',
    '&#x21CF;': '\\nRightarrow',
    '&#x21CE;': '\\nLeftrightarrow',
    '&#x21CD;': '\\nLeftarrow',
    '&#x21CC;': '\\rightleftharpoons',
    '&#x21CB;': '\\leftrightharpoons',
    '&#x21CA;': '\\downdownarrows',
    '&#x21C9;': '\\rightrightarrows',
    '&#x21C8;': '\\upuparrows',
    '&#x21C7;': '\\leftleftarrows',
    '&#x21C6;': '\\leftrightarrows',
    '&#x21C5;': '',
    '&#x21C4;': '\\rightleftarrows',
    '&#x21C3;': '\\downharpoonleft',
    '&#x21C2;': '\\downharpoonright',
    '&#x21C1;': '\\rightharpoondown',
    '&#x21C0;': '\\rightharpoonup',
    '&#x21BF;': '\\upharpoonleft',
    '&#x21BE;': '\\upharpoonright',
    '&#x21BD;': '\\leftharpoondown',
    '&#x21BC;': '\\leftharpoonup',
    '&#x21BB;': '\\circlearrowright',
    '&#x21BA;': '\\circlearrowleft',
    '&#x21B9;': '\\leftrightarrows',
    '&#x21B8;': '\\overline{\\nwarrow}',
    '&#x21B7;': '\\curvearrowright',
    '&#x21B6;': '\\curvearrowleft',
    '&#x21B5;': '\\swarrow',
    '&#x21B4;': '\\searrow',
    '&#x21B3;': '\\Rsh',
    '&#x21B2;': '\\Lsh',
    '&#x21B1;': '\\Rsh',
    '&#x21B0;': '\\Lsh',
    '&#x21AF;': '\\swarrow',
    '&#x21AE;': '',
    '&#x21AD;': '\\leftrightsquigarrow',
    '&#x21AC;': '\\looparrowright',
    '&#x21AB;': '\\looparrowleft',
    '&#x21AA;': '\\hookrightarrow',
    '&#x21A9;': '\\hookleftarrow',
    '&#x21A8;': '\\underline{\\updownarrow}',
    '&#x21A7;': '\\downarrow',
    '&#x21A6;': '\\rightarrowtail',
    '&#x21A5;': '\\uparrow',
    '&#x21A4;': '\\leftarrowtail',
    '&#x21A3;': '\\rightarrowtail',
    '&#x21A2;': '\\leftarrowtail',
    '&#x21A1;': '\\downarrow',
    '&#x21A0;': '\\twoheadrightarrow',
    '&#x219F;': '\\uparrow',
    '&#x219E;': '\\twoheadleftarrow',
    '&#x219D;': '\\nearrow',
    '&#x219C;': '\\nwarrow',
    '&#x219B;': '',
    '&#x219A;': '',
    '&#x2199;': '\\swarrow',
    '&#x2198;': '\\searrow',
    '&#x2197;': '\\nearrow',
    '&#x2196;': '\\nwarrow',
    '&#x2195;': '\\updownarrow',
    '&#x2194;': '\\leftrightarrow',
    '&#x2193;': '\\downarrow',
    '&#x2192;': '\\rightarrow',
    '&#x2191;': '\\uparrow',
    '&#x2190;': '\\leftarrow',
    '|||': '\\left|||\\right.',
    '||': '\\left\\|\\right.',
    '|': '\\left|\\right.',
    '&#x2AFE;': '',
    '&#x2AFD;': '//',
    '&#x2AFB;': '///',
    '&#x2AFA;': '',
    '&#x2AF9;': '',
    '&#x2AF8;': '',
    '&#x2AF7;': '',
    '&#x2AF6;': '\\vdots',
    '&#x2AF5;': '',
    '&#x2AF4;': '',
    '&#x2AF3;': '',
    '&#x2AF2;': '\\nparallel',
    '&#x2AF1;': '',
    '&#x2AF0;': '',
    '&#x2AEF;': '',
    '&#x2AEE;': '\\bcancel{\\mid}',
    '&#x2AED;': '',
    '&#x2AEC;': '',
    '&#x2AEB;': '',
    '&#x2AEA;': '',
    '&#x2AE9;': '',
    '&#x2AE8;': '\\underline{\\perp}',
    '&#x2AE7;': '\\overline{\\top}',
    '&#x2AE6;': '',
    '&#x2AE5;': '',
    '&#x2AE4;': '',
    '&#x2AE3;': '',
    '&#x2AE2;': '',
    '&#x2AE1;': '',
    '&#x2AE0;': '\\perp',
    '&#x2ADF;': '\\top',
    '&#x2ADE;': '\\dashv',
    '&#x2ADD;&#x338;': '',
    '&#x2ADD;': '',
    '&#x2ADB;': '\\pitchfork',
    '&#x2ADA;': '',
    '&#x2AD9;': '',
    '&#x2AD8;': '',
    '&#x2AD7;': '',
    '&#x2AD6;': '',
    '&#x2AD5;': '',
    '&#x2AD4;': '',
    '&#x2AD3;': '',
    '&#x2AD2;': '',
    '&#x2AD1;': '',
    '&#x2AD0;': '',
    '&#x2ACF;': '',
    '&#x2ACE;': '',
    '&#x2ACD;': '',
    '&#x2ACC;': '\\underset{\\neq}{\\supset}',
    '&#x2ACB;': '\\underset{\\neq}{\\subset}',
    '&#x2ACA;': '\\underset{\\approx}{\\supset}',
    '&#x2AC9;': '\\underset{\\approx}{\\subset}',
    '&#x2AC8;': '\\underset{\\sim}{\\supset}',
    '&#x2AC7;': '\\underset{\\sim}{\\subset}',
    '&#x2AC6;': '\\supseteqq',
    '&#x2AC5;': '\\subseteqq',
    '&#x2AC4;': '\\dot{\\supseteq}',
    '&#x2AC3;': '\\dot{\\subseteq}',
    '&#x2AC2;': '\\underset{\\times}{\\supset}',
    '&#x2AC1;': '\\underset{\\times}{\\subset}',
    '&#x2AC0;': '\\underset{+}{\\supset}',
    '&#x2ABF;': '\\underset{+}{\\subset}',
    '&#x2ABE;': '',
    '&#x2ABD;': '',
    '&#x2ABC;': '\\gg ',
    '&#x2ABB;': '\\ll',
    '&#x2ABA;': '\\underset{\\cancel{\\approx}}{\\succ}',
    '&#x2AB9;': '\\underset{\\cancel{\\approx}}{\\prec}',
    '&#x2AB8;': '\\underset{\\approx}{\\succ}',
    '&#x2AB7;': '\\underset{\\approx}{\\prec}',
    '&#x2AB6;': '\\underset{\\cancel{=}}{\\succ}',
    '&#x2AB5;': '\\underset{\\cancel{=}}{\\prec}',
    '&#x2AB4;': '\\underset{=}{\\succ}',
    '&#x2AB3;': '\\underset{=}{\\prec}',
    '&#x2AB2;': '',
    '&#x2AB1;': '',
    '&#x2AAE;': '',
    '&#x2AAD;': '\\underline{\\hcancel{>}}',
    '&#x2AAC;': '\\underline{\\hcancel{>}}',
    '&#x2AAB;': '\\hcancel{>}',
    '&#x2AAA;': '\\hcancel{<}',
    '&#x2AA9;': '',
    '&#x2AA8;': '',
    '&#x2AA7;': '\\vartriangleright',
    '&#x2AA6;': '\\vartriangleleft',
    '&#x2AA5;': '><',
    '&#x2AA4;': '><',
    '&#x2AA3;': '\\underline{\\ll}',
    '&#x2AA2;&#x338;': '\\cancel{\\gg}',
    '&#x2AA2;': '\\gg',
    '&#x2AA1;&#x338;': '\\cancel{\\ll}',
    '&#x2AA1;': '\\ll',
    '&#x2AA0;': '\\overset{\\sim}{\\geqq}',
    '&#x2A9F;': '\\overset{\\sim}{\\leqq}',
    '&#x2A9E;': '\\overset{\\sim}{>}',
    '&#x2A9D;': '\\overset{\\sim}{<}',
    '&#x2A9C;': '',
    '&#x2A9B;': '',
    '&#x2A9A;': '\\overset{=}{>}',
    '&#x2A99;': '\\overset{=}{<}',
    '&#x2A98;': '',
    '&#x2A97;': '',
    '&#x2A96;': '',
    '&#x2A95;': '',
    '&#x2A94;': '',
    '&#x2A93;': '',
    '&#x2A92;': '\\underset{=}{\\gtrless}',
    '&#x2A91;': '\\underset{=}{\\lessgtr}',
    '&#x2A90;': '\\underset{<}{\\gtrsim}',
    '&#x2A8F;': '\\underset{>}{\\lesssim}',
    '&#x2A8E;': '\\underset{\\simeq}{>}',
    '&#x2A8D;': '\\underset{\\simeq}{<}',
    '&#x2A8C;': '\\gtreqqless',
    '&#x2A8B;': '\\lesseqqgtr',
    '&#x2A8A;': '\\underset{\\cancel{\\approx}}{>}',
    '&#x2A89;': '\\underset{\\approx}{<}',
    '&#x2A86;': '\\underset{\\approx}{>}',
    '&#x2A85;': '\\underset{\\approx}{<}',
    '&#x2A84;': '',
    '&#x2A83;': '',
    '&#x2A82;': '',
    '&#x2A81;': '',
    '&#x2A80;': '',
    '&#x2A7F;': '',
    '&#x2A7E;&#x338;': '\\bcancel{\\geq}',
    '&#x2A7E;': '\\geq',
    '&#x2A7D;&#x338;': '\\bcancel{\\leq}',
    '&#x2A7D;': '\\leq',
    '&#x2A7C;': '',
    '&#x2A7B;': '',
    '&#x2A7A;': '',
    '&#x2A79;': '',
    '&#x2A78;': '\\overset{\\dots}{\\equiv}',
    '&#x2A77;': '',
    '&#x2A76;': '===',
    '&#x2A75;': '==',
    '&#x2A74;': '::=',
    '&#x2A73;': '',
    '&#x2A72;': '\\underset{=}{+}',
    '&#x2A71;': '\\overset{=}{+}',
    '&#x2A70;': '\\overset{\\approx}{=}',
    '&#x2A6F;': '\\overset{\\wedge}{=}',
    '&#x2A6E;': '\\overset{*}{=}',
    '&#x2A6D;': '\\dot{\\approx}',
    '&#x2A6C;': '',
    '&#x2A6B;': '',
    '&#x2A6A;': '\\dot{\\sim}',
    '&#x2A69;': '',
    '&#x2A68;': '',
    '&#x2A67;': '\\dot{\\equiv}',
    '&#x2A66;': '\\underset{\\cdot}{=}',
    '&#x2A65;': '',
    '&#x2A64;': '',
    '&#x2A63;': '\\underset{=}{\\vee}',
    '&#x2A62;': '\\overset{=}{\\vee}',
    '&#x2A61;': 'ul(vv)',
    '&#x2A60;': '\\underset{=}{\\wedge}',
    '&#x2A5F;': '\\underline{\\wedge}',
    '&#x2A5E;': '\\overset{=}{\\wedge}',
    '&#x2A5D;': '\\hcancel{\\vee}',
    '&#x2A5C;': '\\hcancel{\\wedge}',
    '&#x2A5B;': '',
    '&#x2A5A;': '',
    '&#x2A59;': '',
    '&#x2A58;': '\\vee',
    '&#x2A57;': '\\wedge',
    '&#x2A56;': '',
    '&#x2A55;': '',
    '&#x2A54;': '',
    '&#x2A53;': '',
    '&#x2A52;': '\\dot{\\vee}',
    '&#x2A51;': '\\dot{\\wedge}',
    '&#x2A50;': '',
    '&#x2A4F;': '',
    '&#x2A4E;': '',
    '&#x2A4D;': '\\overline{\\cap}',
    '&#x2A4C;': '\\overline{\\cup}',
    '&#x2A4B;': '',
    '&#x2A4A;': '',
    '&#x2A49;': '',
    '&#x2A48;': '',
    '&#x2A47;': '',
    '&#x2A46;': '',
    '&#x2A45;': '',
    '&#x2A44;': '',
    '&#x2A43;': '\\overline{\\cap}',
    '&#x2A42;': '\\overline{\\cup}',
    '&#x2A41;': '',
    '&#x2A40;': '',
    '&#x2A3E;': '',
    '&#x2A3D;': '\\llcorner',
    '&#x2A3C;': '\\lrcorner',
    '&#x2A3B;': '',
    '&#x2A3A;': '',
    '&#x2A39;': '',
    '&#x2A38;': '',
    '&#x2A37;': '',
    '&#x2A36;': '\\hat{\\otimes}',
    '&#x2A35;': '',
    '&#x2A34;': '',
    '&#x2A33;': '',
    '&#x2A32;': '\\underline{\\times}',
    '&#x2A31;': '\\underline{\\times}',
    '&#x2A30;': '\\dot{\\times}',
    '&#x2A2E;': '',
    '&#x2A2D;': '',
    '&#x2A2C;': '',
    '&#x2A2B;': '',
    '&#x2A2A;': '',
    '&#x2A29;': '',
    '&#x2A28;': '',
    '&#x2A27;': '',
    '&#x2A26;': '\\underset{\\sim}{+}',
    '&#x2A25;': '\\underset{\\circ}{+}',
    '&#x2A24;': '\\overset{\\sim}{+}',
    '&#x2A23;': '\\hat{+}',
    '&#x2A22;': '\\dot{+}',
    '&#x2A21;': '\\upharpoonright',
    '&#x2A20;': '>>',
    '&#x2A1F;': '',
    '&#x2A1E;': '\\triangleleft',
    '&#x2A1D;': '\\bowtie',
    '&#x29FF;': '',
    '&#x29FE;': '+',
    '&#x29FB;': '\\hcancel{|||}',
    '&#x29FA;': '\\hcancel{||}',
    '&#x29F9;': '\\backslash',
    '&#x29F8;': '/',
    '&#x29F7;': 'hcancel{\backslash}',
    '&#x29F6;': '',
    '&#x29F5;': '\\backslash',
    '&#x29F2;': '\\Phi',
    '&#x29F1;': '',
    '&#x29F0;': '',
    '&#x29EE;': '',
    '&#x29ED;': '',
    '&#x29EC;': '',
    '&#x29EB;': '\\lozenge',
    '&#x29EA;': '',
    '&#x29E9;': '',
    '&#x29E8;': '',
    '&#x29E7;': '\\ddagger',
    '&#x29E2;': '\\sqcup\\sqcup',
    '&#x29E1;': '',
    '&#x29E0;': '\\square',
    '&#x29DE;': '',
    '&#x29DD;': '',
    '&#x29DC;': '',
    '&#x29DB;': '\\{\\{',
    '&#x29D9;': '\\{',
    '&#x29D8;': '\\}',
    '&#x29D7;': '',
    '&#x29D6;': '',
    '&#x29D5;': '\\bowtie',
    '&#x29D4;': '\\bowtie',
    '&#x29D3;': '\\bowtie',
    '&#x29D2;': '\\bowtie',
    '&#x29D1;': '\\bowtie',
    '&#x29D0;&#x338;': '| \\not\\triangleright',
    '&#x29D0;': '| \\triangleright',
    '&#x29CF;&#x338;': '\\not\\triangleleft |',
    '&#x29CF;': '\\triangleleft |',
    '&#x29CE;': '',
    '&#x29CD;': '\\triangle',
    '&#x29CC;': '',
    '&#x29CB;': '\\underline{\\triangle}',
    '&#x29CA;': '\\dot{\\triangle}',
    '&#x29C9;': '',
    '&#x29C8;': '\\boxed{\\circ}',
    '&#x29C7;': '\\boxed{\\circ}',
    '&#x29C6;': '\\boxed{\\rightarrow}',
    '&#x29C5;': '\\bcancel{\\square}',
    '&#x29C4;': '\\cancel{\\square}',
    '&#x29C3;': '\\odot',
    '&#x29C2;': '\\odot',
    '&#x29BF;': '\\odot',
    '&#x29BE;': '\\odot',
    '&#x29BD;': '\\varnothing',
    '&#x29BC;': '\\oplus',
    '&#x29BB;': '\\otimes',
    '&#x29BA;': '',
    '&#x29B9;': '\\varnothing',
    '&#x29B8;': '\\varnothing',
    '&#x29B7;': '\\ominus',
    '&#x29B6;': '\\ominus',
    '&#x29B5;': '\\ominus',
    '&#x29B4;': '\\vec{\\varnothing}',
    '&#x29B3;': '\\vec{\\varnothing}',
    '&#x29B2;': '\\dot{\\varnothing}',
    '&#x29B1;': '\\overline{\\varnothing}',
    '&#x29B0;': '\\varnothing',
    '&#x29AF;': '',
    '&#x29AE;': '',
    '&#x29AD;': '',
    '&#x29AC;': '',
    '&#x29AB;': '',
    '&#x29AA;': '',
    '&#x29A9;': '',
    '&#x29A8;': '',
    '&#x29A7;': '',
    '&#x29A6;': '',
    '&#x29A5;': '',
    '&#x29A4;': '',
    '&#x29A3;': '',
    '&#x29A2;': '',
    '&#x29A1;': '\\not\\lor',
    '&#x29A0;': '\\bcancel{>}',
    '&#x2982;': ':',
    '&#x2981;': '\\circ',
    '&#x2758;': '|',
    '&#x25B2;': '\\bigtriangleup',
    '&#x22FF;': '\\Epsilon',
    '&#x22FE;': '\\overline{\\ni}',
    '&#x22FD;': '\\overline{\\ni}',
    '&#x22FC;': '\\in',
    '&#x22FB;': '\\in',
    '&#x22FA;': '\\in',
    '&#x22F9;': '\\underline{\\in}',
    '&#x22F8;': '\\underline{\\in}',
    '&#x22F7;': '\\overline{\\in}',
    '&#x22F6;': '\\overline{\\in}',
    '&#x22F5;': '\\dot{\\in}',
    '&#x22F4;': '\\in',
    '&#x22F3;': '\\in',
    '&#x22F2;': '\\in',
    '&#x22F0;': '\\ddots',
    '&#x22E9;': '\\underset{\\sim}{\\succ}',
    '&#x22E8;': '\\underset{\\sim}{\\prec}',
    '&#x22E7;': '\\underset{\\not\\sim}{>}',
    '&#x22E6;': '\\underset{\\not\\sim}{<}',
    '&#x22E5;': '\\not\\sqsupseteq',
    '&#x22E4;': '\\not\\sqsubseteq',
    '&#x22E3;': '\\not\\sqsupseteq',
    '&#x22E2;': '\\not\\sqsubseteq',
    '&#x22E1;': '\\nsucc',
    '&#x22E0;': '\\nprec',
    '&#x22DF;': '\\succ',
    '&#x22DE;': '\\prec',
    '&#x22DD;': '\\overline{>}',
    '&#x22DC;': '\\overline{<}',
    '&#x22DB;': '\\underset{>}{\\leq}',
    '&#x22DA;': '\\underset{<}{\\geq}',
    '&#x22D5;': '\\#',
    '&#x22D3;': '\\cup',
    '&#x22D2;': '\\cap',
    '&#x22D1;': '\\supset',
    '&#x22D0;': '\\subset',
    '&#x22CF;': '\\wedge',
    '&#x22CE;': '\\vee',
    '&#x22CD;': '\\simeq',
    '&#x22C8;': '\\bowtie',
    '&#x22C7;': '\\ast',
    '&#x22C6;': '\\star',
    '&#x22C4;': '\\diamond',
    '&#x22BF;': '\\triangle',
    '&#x22BE;': '\\measuredangle',
    '&#x22BD;': '\\overline{\\lor}',
    '&#x22BC;': '\\overline{\\land}',
    '&#x22BB;': '\\underline{\\lor}',
    '&#x22BA;': '\\top',
    '&#x22B9;': '',
    '&#x22B7;': '\\circ\\multimap',
    '&#x22B6;': '\\circ\\multimap',
    '&#x22B3;': '\\triangleright',
    '&#x22B2;': '\\triangleleft',
    '&#x22B1;': '\\succ',
    '&#x22B0;': '\\prec',
    '&#x22AB;': '|\\models',
    '&#x22AA;': '|\\models',
    '&#x22A7;': '\\models',
    '&#x22A6;': '\\vdash',
    '&#x229D;': '\\ominus',
    '&#x229C;': '\\ominus',
    '&#x229B;': '\\odot',
    '&#x229A;': '\\odot',
    '&#x2294;': '\\sqcup',
    '&#x2293;': '\\sqcap',
    '&#x2292;': '\\sqsupseteq',
    '&#x2291;': '\\sqsubseteq',
    '&#x2290;&#x338;': '\\not\\sqsupset',
    '&#x2290;': '\\sqsupset',
    '&#x228F;&#x338;': '\\not\\sqsubset',
    '&#x228F;': '\\sqsubset',
    '&#x228E;': '\\cup',
    '&#x228D;': '\\cup',
    '&#x228C;': '\\cup',
    '&#x227F;&#x338;': '\\not\\succsim',
    '&#x227F;': '\\succsim',
    '&#x227E;': '\\precsim',
    '&#x2279;': '\\not\\overset{>}{<}',
    '&#x2278;': '\\not\\overset{>}{<}',
    '&#x2277;': '\\overset{>}{<}',
    '&#x2276;': '\\overset{<}{>}',
    '&#x2275;': '\\not\\geg',
    '&#x2274;': '\\not\\leq',
    '&#x2273;': '\\geg',
    '&#x2272;': '\\leq',
    '&#x226C;': '',
    '&#x2267;': '\\geg',
    '&#x2266;&#x338;': '\\not\\leq',
    '&#x2266;': '\\leq',
    '&#x2263;': '\\overset{=}{=} ',
    '&#x225E;': '\\overset{m}{=} ',
    '&#x225D;': '\\overset{def}{=}',
    '&#x2258;': '=',
    '&#x2256;': '=',
    '&#x2255;': '=:',
    '&#x2253;': '\\doteq',
    '&#x2252;': '\\doteq',
    '&#x2251;': '\\doteq',
    '&#x2250;': '\\doteq',
    '&#x224F;&#x338;': '',
    '&#x224F;': '',
    '&#x224E;&#x338;': '',
    '&#x224E;': '',
    '&#x224C;': '\\approx',
    '&#x224B;': '\\approx',
    '&#x224A;': '\\approx',
    '&#x2242;&#x338;': '\\neq',
    '&#x2242;': '=',
    '&#x223F;': '\\sim',
    '&#x223E;': '\\infty',
    '&#x223D;&#x331;': '\\sim',
    '&#x223D;': '\\sim',
    '&#x223B;': '\\sim',
    '&#x223A;': ':-:',
    '&#x2239;': '-:',
    '&#x2238;': '\\bot',
    '&#x2237;': '::',
    '&#x2236;': ':',
    '&#x2223;': '|',
    '&#x221F;': '\\llcorner',
    '&#x2219;': '\\cdot',
    '&#x2218;': '\\circ',
    '&#x2217;': '*',
    '&#x2215;': '/',
    '&#x220E;': '\\square',
    '&#x220D;': '\\ni',
    '&#x220A;': '\\in',
    '&#x2206;': '\\Delta',
    '&#x2044;': '/',
    '&#x2AB0;&#x338;': '\\nsucceq',
    '&#x2AB0;': '\\succeq',
    '&#x2AAF;&#x338;': '\\npreceq',
    '&#x2AAF;': '\\preceq',
    '&#x2A88;': '\\ngeqslant',
    '&#x2A87;': '\\nleqslant',
    '&#x29F3;': '\\Phi',
    '&#x29E6;': '\\models',
    '&#x29E5;': '\\not\\equiv',
    '&#x29E4;': '\\approx\\neq',
    '&#x29E3;': '\\neq',
    '&#x29C1;': '\\circle',
    '&#x29C0;': '\\circle',
    '&#x25E6;': '\\circle',
    '&#x25D7;': '\\circle',
    '&#x25D6;': '\\circle',
    '&#x25CF;': '\\circle',
    '&#x25CE;': '\\circledcirc',
    '&#x25CD;': '\\circledcirc',
    '&#x25CC;': '\\circledcirc',
    '&#x25C9;': '\\circledcirc',
    '&#x25C8;': '\\diamond',
    '&#x25C7;': '\\diamond',
    '&#x25C6;': '\\diamond',
    '&#x25C5;': '\\triangleleft',
    '&#x25C4;': '\\triangleleft',
    '&#x25C3;': '\\triangleleft',
    '&#x25C2;': '\\triangleleft',
    '&#x25C1;': '\\triangleleft',
    '&#x25C0;': '\\triangleleft',
    '&#x25BF;': '\\triangledown',
    '&#x25BE;': '\\triangledown',
    '&#x25BD;': '\\triangledown',
    '&#x25BC;': '\\triangledown',
    '&#x25B9;': '\\triangleright',
    '&#x25B8;': '\\triangleright',
    '&#x25B7;': '\\triangleright',
    '&#x25B6;': '\\triangleright',
    '&#x25B5;': '\\triangle',
    '&#x25B4;': '\\triangle',
    '&#x25B3;': '\\triangle',
    '&#x25B1;': '\\square',
    '&#x25B0;': '\\square',
    '&#x25AF;': '\\square',
    '&#x25AE;': '\\square',
    '&#x25AD;': '\\square',
    '&#x25AB;': '\\square',
    '&#x25AA;': '\\square',
    '&#x25A1;': '\\square',
    '&#x25A0;': '\\square',
    '&#x22ED;': '\\not\\triangleright',
    '&#x22EC;': '\\not\\triangleleft',
    '&#x22EB;': '\\not\\triangleright',
    '&#x22EA;': '\\not\\triangleleft',
    '&#x22D9;': '\\ggg',
    '&#x22D8;': '\\lll',
    '&#x22D7;': '*>',
    '&#x22D6;': '<*',
    '&#x22D4;': '\\pitchfork',
    '&#x22CC;': '',
    '&#x22CB;': '',
    '&#x22CA;': '\\rtimes',
    '&#x22C9;': '\\ltimes',
    '&#x22B5;': '\\triangleright',
    '&#x22B4;': '',
    '&#x22A5;': '\\bot',
    '&#x2281;': '\\nsucc',
    '&#x2280;': '\\preceq',
    '&#x227D;': '\\succeq',
    '&#x227C;': '\\preceq',
    '&#x227B;': '\\succ',
    '&#x227A;': '\\prec',
    '&#x2271;': '\\geq/',
    '&#x2270;': '\\leq/',
    '&#x226D;': '\\neq',
    '&#x226B;&#x338;': '\\not\\gg',
    '&#x226B;': '\\gg',
    '&#x226A;&#x338;': '\\not\\ll',
    '&#x226A;': '\\ll',
    '&#x2269;': '\\ngeqslant',
    '&#x2268;': '\\nleqslant',
    '&#x2261;': '\\equiv',
    '&#x225F;': '\\doteq',
    '&#x225C;': '\\triangleq',
    '&#x225B;': '\\doteq',
    '&#x225A;': '\\triangleq',
    '&#x2259;': '\\triangleq',
    '&#x2257;': '\\doteq',
    '&#x2254;': ':=',
    '&#x224D;': '\\asymp',
    '&#x2247;': '\\ncong',
    '&#x2246;': '\\ncong',
    '&#x2245;': '\\cong',
    '&#x2244;': '\\not\\simeq',
    '&#x2243;': '\\simeq',
    '&#x2241;': '\\not\\sim',
    '&#x2226;': '\\not\\parallel',
    '&#x2225;': '\\parallel',
    '&#x2224;': '\\not|',
    '&#x221D;': '\\propto',
    '==': '==',
    '=': '=',
    ':=': ':=',
    '/=': '=',
    '-=': '-=',
    '+=': '+=',
    '*=': '*=',
    '!=': '!=',
    '&#x2260;': '\\neq',
    '&#x2262;': '\\equiv /',
    '&#x2249;': '\\approx /',
    '&#x223C;': 'sim',
    '&#x2248;': '\\approx',
    '&#x226E;': '</',
    '&lt;': '<',
    '&#x226F;': '>/',
    '>=': '>=',
    '>': '>',
    '&#x2265;': '\\geq',
    '&#x2264;': '\\leq',
    '&lt;=': '<=',
    '&#x228B;': '\\supsetneq',
    '&#x228A;': '\\subsetneq',
    '&#x2289;': '\\nsupseteq',
    '&#x2288;': '\\nsubseteq',
    '&#x2287;': '\\supseteq',
    '&#x2286;': '\\subseteq',
    '&#x2285;': '\\not\\supset',
    '&#x2284;': '\\not\\subset',
    '&#x2283;&#x20D2;': '\\supset |',
    '&#x2283;': '\\supset',
    '&#x2282;&#x20D2;': '\\subset |',
    '&#x2282;': '\\subset',
    '&#x220C;': '\\not\\in',
    '&#x2209;': '\\notin',
    '&#x2208;': '\\in',
    '&#x2201;': 'C',
    '&#x2204;': '\\nexists',
    '&#x2203;': '\\exists',
    '&#x2200;': '\\forall',
    '&#x2227;': '\\land',
    '&amp;&amp;': '\\&\\&',
    '&#x2228;': '\\lor',
    '&#x22AF;': '\\cancel{\\vDash}',
    '&#x22AE;': '\\cancel{\\Vdash}',
    '&#x22AD;': '\\nvDash',
    '&#x22AC;': '\\nvDash',
    '&#x22A9;': '\\Vdash',
    '&#x22A8;': '\\vDash',
    '&#x22A4;': '\\top',
    '&#x22A3;': '\\dashv',
    '&#x22A2;': '\\vdash',
    '&#x220B;': '\\ni',
    '&#x22F1;': '\\ddots',
    '&#x22EF;': '\\hdots',
    '&#x22EE;': '\\vdots',
    '&#x2026;': '\\hdots',
    '&#x3F6;': '\\ni',
    ':': ':',
    '...': '\\cdots',
    '..': '..',
    '->': '->',
    '&#x2235;': '\\because',
    '&#x2234;': '\\therefore ',
    '&#x2063;': '',
    ',': ',',
    ';': ';',
    '&#x29FD;': '\\}',
    '&#x29FC;': '\\{',
    '&#x2998;': '\\]',
    '&#x2997;': '\\[',
    '&#x2996;': '\\ll',
    '&#x2995;': '\\gg',
    '&#x2994;': '\\gg',
    '&#x2993;': '\\ll',
    '&#x2992;': '\\gg',
    '&#x2991;': '\\ll',
    '&#x2990;': '\\]',
    '&#x298F;': '\\]',
    '&#x298E;': '\\]',
    '&#x298D;': '\\[',
    '&#x298C;': '\\[',
    '&#x298B;': '\\]',
    '&#x298A;': '\\triangleright',
    '&#x2989;': '\\triangleleft',
    '&#x2988;': '|\\)',
    '&#x2987;': '\\(|',
    '&#x2986;': '|\\)',
    '&#x2985;': '\\(\\(',
    '&#x2984;': '|\\}',
    '&#x2983;': '\\{|',
    '&#x2980;': '\\||',
    '&#x27EF;': '\\left. \\right]',
    '&#x27EE;': '\\left[ \\right.',
    '&#x27ED;': '\\left. \\right]]',
    '&#x27EC;': '\\left[[ \\right.',
    '&#x27EB;': '\\gg',
    '&#x27EA;': '\\ll',
    '&#x27E9;': '\\rangle',
    '&#x27E8;': '\\langle',
    '&#x27E7;': '\\left. \\right]]',
    '&#x27E6;': '\\left[[ \\right.',
    '&#x2773;': '\\left.\\right)',
    '&#x2772;': '\\left(\\right.',
    '&#x232A;': '\\rangle',
    '&#x2329;': '\\langle',
    '&#x230B;': '\\rfloor',
    '&#x230A;': '\\lfloor',
    '&#x2309;': '\\rceil',
    '&#x2308;': '\\lceil',
    '&#x2016;': '\\parallel',
    '}': '\\left.\\right\\}',
    '{': '\\left\\{\\right.',
    ']': '\\left]\\right.',
    '[': '\\left[\\right.',
    ')': '\\left.\\right)',
    '(': '\\left(\\right.',
    '&#x201D;': '"',
    '&#x201C;': '``',
    '&#x2019;': "'",
    '&#x2018;': '`',
    '%CE%B1': '\\alpha',
    '%CE%B2': '\\beta',
    '%CE%B3': '\\gamma',
    '%CE%93': '\\Gamma',
    '%CE%B4': '\\delta',
    '%CE%94': '\\Delta',
    '%CF%B5': '\\epsilon',
    '%CE%B6': '\\zeta',
    '%CE%B7': '\\eta',
    '%CE%B8': '\\theta',
    '%CE%98': '\\Theta',
    '%CE%B9': '\\iota',
    '%CE%BA': '\\kappa',
    '%CE%BB': '\\lambda',
    '%CE%BC': '\\mu',
    '%CE%BD': '\\nu',
    '%CE%BF': '\\omicron',
    '%CF%80': '\\pi',
    '%CE%A0': '\\Pi',
    '%CF%81': '\\pho',
    '%CF%83': '\\sigma',
    '%CE%A3': '\\Sigma',
    '%CF%84': '\\tau',
    '%CF%85': '\\upsilon',
    '%CE%A5': '\\Upsilon',
    '%CF%95': '\\phi',
    '%CE%A6': '\\Phi',
    '%CF%87': '\\chi',
    '%CF%88': '\\psi',
    '%CE%A8': '\\Psi',
    '%CF%89': '\\omega',
    '%CE%A9': '\\Omega',
};

/**
 * Lookup table mapping math operators, keyed by their decoded unicode glyph
 * (e.g. `∑`), to the equivalent LaTeX command. Consumed by the `<mo>` converter
 * to translate operator content into LaTeX.
 */
const allMathOperatorsByGlyph = {
    _: '\\underline',
    '⏡': '\\underbrace',
    '⏠': '\\overbrace',
    '⏟': '\\underbrace',
    '⏞': '\\overbrace',
    '⏝': '\\underbrace',
    '⏜': '\\overbrace',
    '⎵': '\\underbrace',
    '⎴': '\\overbrace',
    '⃜': '\\square',
    '⃛': '\\square',
    '⁤': '',
    '⁗': "''''",
    '‾': '\\overline',
    '‷': '```',
    '‶': '``',
    '‵': '`',
    '‴': "'''",
    '″': "''",
    '‟': '``',
    '„': ',,',
    '‛': '`',
    '‚': ',',
    '^': '\\hat',
    '˷': '\\sim',
    '˝': '\\sim',
    '˜': '\\sim',
    '˚': '\\circ',
    '˙': '\\cdot',
    '˘': ' ',
    ˍ: '\\_',
    ˋ: 'ˋ',
    ˊ: 'ˊ',
    ˉ: 'ˉ',
    ˇ: '',
    ˆ: '\\hat',
    º: 'o',
    '¹': '1',
    '¸': ',',
    '´': '´',
    '³': '3',
    '²': '2',
    '°': '\\circ',
    '¯': '\\bar',
    ª: 'a',
    '↛': '\\nrightarrow',
    '¨': '\\cdot\\cdot',
    '~': '\\sim',
    '`': '`',
    '--': '--',
    '++': '++',
    '&': '\\&',
    '∜': '\\sqrt[4]{}',
    '∛': '\\sqrt[3]{}',
    '√': '\\sqrt{}',
    ⅆ: 'd',
    ⅅ: '\\mathbb{D}',
    '?': '?',
    '@': '@',
    '//': '//',
    '!!': '!!',
    '!': '!',
    '♯': '\\#',
    '♮': '',
    '♭': '',
    '′': "'",
    '<>': '<>',
    '**': '\\star\\star',
    '∇': '\\nabla',
    '∂': '\\partial',
    '⊙': '\\bigodot',
    '¬': '\\neg',
    '∢': '\\measuredangle',
    '∡': '\\measuredangle',
    '∠': '\\angle',
    '÷': '\\div',
    '/': '/',
    '∖': '\\backslash',
    '\\': '\\backslash',
    '%': '\\%',
    '⊗': '\\bigotimes',
    '·': '\\cdot',
    '⨿': '\\coprod',
    '⨯': '\\times',
    '⋅': '\\cdot',
    '⊡': '\\boxdot',
    '⊠': '\\boxtimes',
    '⁢': '',
    '⁡': '',
    '⁃': '-',
    '•': '\\cdot',
    '.': '.',
    '*': '\\star',
    '∪': '\\cup',
    '∩': '\\cap',
    '∐': '\\coprod',
    '∏': '\\prod',
    '≀': '',
    '⫿': '',
    '⫼': '\\mid\\mid\\mid',
    '⨉': '\\times',
    '⨈': '',
    '⨇': '',
    '⨆': '\\sqcup',
    '⨅': '\\sqcap',
    '⨂': '\\otimes',
    '⨀': '\\odot',
    '⋂': '\\cap',
    '⋁': '\\vee',
    '⋀': '\\wedge',
    '⨄': '\\uplus',
    '⨃': '\\cup',
    '⋃': '\\cup',
    '⨜': '\\underline{\\int}',
    '⨛': '\\overline{\\int}',
    '⨚': '\\int',
    '⨙': '\\int',
    '⨘': '\\int',
    '⨗': '\\int',
    '⨖': '\\oint',
    '⨕': '\\oint',
    '⨔': '\\int',
    '⨓': '\\int',
    '⨒': '\\int',
    '⨑': '\\int',
    '⨐': '\\int',
    '⨏': '\\bcancel{\\int}',
    '⨎': '',
    '⨍': '\\hcancel{\\int}',
    '⨌': '\\iiiint',
    '∳': '\\oint',
    '∲': '\\oint',
    '∱': '\\int',
    '∰': '\\oiint',
    '∯': '\\oiint',
    '∮': '\\oint',
    '∫': '\\int',
    '⨁': '\\oplus',
    '⊘': '\\oslash',
    '⊖': '\\ominus',
    '⊕': '\\oplus',
    '∭': '\\iiint',
    '∬': '\\iint',
    '⨋': '',
    '⨊': '',
    '∑': '\\sum',
    '⊟': '\\boxminus',
    '⊞': '\\boxplus',
    '∔': '\\dot{+}',
    '∓': '+-',
    '−': '-',
    '±': '\\pm',
    '-': '-',
    '+': '+',
    '⭆': '\\Rrightarrow',
    '⭅': '\\Lleftarrow',
    '⧴': ':\\rightarrow',
    '⧯': '',
    '⧟': '\\bullet-\\bullet',
    '⦟': '\\angle',
    '⦞': '\\measuredangle',
    '⦝': '\\measuredangle',
    '⦜': '\\perp',
    '⦛': '\\measuredangle',
    '⦚': '',
    '⦙': '\\vdots',
    '⥿': '',
    '⥾': '',
    '⥽': '\\prec',
    '⥼': '\\succ',
    '⥻': '\\underset{\\rightarrow}{\\supset}',
    '⥺': '',
    '⥹': '\\underset{\\rightarrow}{\\subset}',
    '⥸': '\\underset{\\rightarrow}{>}',
    '⥷': '',
    '⥶': '\\underset{\\leftarrow}{<}',
    '⥵': '\\underset{\\approx}{\\rightarrow}',
    '⥴': '\\underset{\\sim}{\\rightarrow}',
    '⥳': '\\underset{\\sim}{\\leftarrow}',
    '⥲': '\\overset{\\sim}{\\rightarrow}',
    '⥱': '\\overset{=}{\\rightarrow}',
    '⥰': '',
    '⥯': '',
    '⥮': '',
    '⥭': '\\overline{\\rightharpoondown}',
    '⥬': '\\underline{\\rightharpoonup}',
    '⥫': '\\overline{\\leftharpoondown}',
    '⥪': '\\underline{\\leftharpoonup}',
    '⥩': '\\rightleftharpoons',
    '⥨': '\\rightleftharpoons',
    '⥧': '\\rightleftharpoons',
    '⥦': '\\rightleftharpoons',
    '⥥': '\\Downarrow',
    '⥤': '\\Rightarrow',
    '⥣': '\\Uparrow',
    '⥢': '\\Leftarrow',
    '⥡': '\\downarrow',
    '⥠': '\\uparrow',
    '⥟': '\\rightarrow',
    '⥞': '\\leftarrow',
    '⥝': '\\downarrow',
    '⥜': '\\uparrow',
    '⥛': '\\rightarrow',
    '⥚': '\\leftarrow',
    '⥙': '\\downarrow',
    '⥘': '\\uparrow',
    '⥗': '\\rightarrow',
    '⥖': '\\leftarrow',
    '⥕': '\\downarrow',
    '⥔': '\\uparrow',
    '⥓': '\\rightarrow',
    '⥒': '\\leftarrow',
    '⥑': '\\updownarrow',
    '⥐': '\\leftrightarrow',
    '⥏': '\\updownarrow',
    '⥎': '\\leftrightarrow',
    '⥍': '\\updownarrow',
    '⥌': '\\updownarrow',
    '⥋': '\\leftrightarrow',
    '⥊': '\\leftrightarrow',
    '⥉': '',
    '⥈': '\\leftrightarrow',
    '⥇': '\\nrightarrow',
    '⥆': '',
    '⥅': '',
    '⥄': '\\rightleftarrows',
    '⥃': '\\leftrightarrows',
    '⥂': '\\rightleftarrows',
    '⥁': '\\circlearrowright',
    '⥀': '\\circlearrowleft',
    '⤿': '\\rightarrow',
    '⤾': '\\leftarrow',
    '⤽': '\\leftarrow',
    '⤼': '\\rightarrow',
    '⤻': '\\rightarrow',
    '⤺': '\\leftarrow',
    '⤹': '\\downarrow',
    '⤸': '\\downarrow',
    '⤷': '\\Rsh',
    '⤶': '\\Lsh',
    '⤵': '\\downarrow',
    '⤴': '\\uparrow',
    '⤳': '\\rightarrow',
    '⤲': '\\leftarrow',
    '⤱': ' ',
    '⤰': ' ',
    '⤯': ' ',
    '⤮': ' ',
    '⤭': ' ',
    '⤬': '\\times',
    '⤫': '\\times',
    '⤪': ' ',
    '⤩': ' ',
    '⤨': ' ',
    '⤧': ' ',
    '⤦': ' ',
    '⤥': ' ',
    '⤤': ' ',
    '⤣': ' ',
    '⤢': ' ',
    '⤡': ' ',
    '⤠': '\\mapsto\\cdot',
    '⤟': '\\cdot\\leftarrow',
    '⤞': '\\rightarrow\\cdot',
    '⤝': '\\leftarrow',
    '⤜': '\\rightarrow',
    '⤛': '\\leftarrow',
    '⤚': '\\rightarrow',
    '⤙': '\\leftarrow',
    '⤘': '\\rightarrow',
    '⤗': '\\rightarrow',
    '⤖': '\\rightarrow',
    '⤕': '\\rightarrow',
    '⤔': '\\rightarrow',
    '⤓': '\\downarrow',
    '⤒': '\\uparrow',
    '⤑': '\\rightarrow',
    '⤐': '\\rightarrow',
    '⤏': '\\rightarrow',
    '⤎': '\\leftarrow',
    '⤍': '\\rightarrow',
    '⤌': '\\leftarrow',
    '⤋': '\\Downarrow',
    '⤊': '\\Uparrow',
    '⤉': '\\uparrow',
    '⤈': '\\downarrow',
    '⤇': '\\Rightarrow',
    '⤆': '\\Leftarrow',
    '⤅': '\\mapsto',
    '⤄': '\\nLeftrightarrow',
    '⤃': '\\nRightarrow',
    '⤂': '\\nLeftarrow',
    '⤁': '\\rightsquigarrow',
    '⤀': '\\rightsquigarrow',
    '⟿': '\\rightsquigarrow',
    '⟾': '\\Rightarrow',
    '⟽': '\\Leftarrow',
    '⟼': '\\mapsto',
    '⟻': '\\leftarrow',
    '⟺': '\\Longleftrightarrow',
    '⟹': '\\Longrightarrow',
    '⟸': '\\Longleftarrow',
    '⟷': '\\leftrightarrow',
    '⟶': '\\rightarrow',
    '⟵': '\\leftarrow',
    '⟱': '\\Downarrow',
    '⟰': '\\Uparrow',
    '⊸': '\\rightarrow',
    '⇿': '\\leftrightarrow',
    '⇾': '\\rightarrow',
    '⇽': '\\leftarrow',
    '⇼': '\\nleftrightarrow',
    '⇻': '\\nrightarrow',
    '⇺': '\\nleftarrow',
    '⇹': '\\nleftrightarrow',
    '⇸': '\\nrightarrow',
    '⇷': '\\nleftarrow',
    '⇶': '\\Rrightarrow',
    '⇵': '',
    '⇴': '\\rightarrow',
    '⇳': '\\Updownarrow',
    '⇲': '\\searrow',
    '⇱': '\\nwarrow',
    '⇰': '\\Leftarrow',
    '⇯': '\\Uparrow',
    '⇮': '\\Uparrow',
    '⇭': '\\Uparrow',
    '⇬': '\\Uparrow',
    '⇫': '\\Uparrow',
    '⇪': '\\Uparrow',
    '⇩': '\\Downarrow',
    '⇨': '\\Rightarrow',
    '⇧': '\\Uparrow',
    '⇦': '\\Leftarrow',
    '⇥': '\\rightarrow',
    '⇤': '\\leftarrow',
    '⇣': '\\downarrow',
    '⇢': '\\rightarrow',
    '⇡': '\\uparrow',
    '⇠': '\\leftarrow',
    '⇟': '\\downarrow',
    '⇞': '\\uparrow',
    '⇝': '\\rightsquigarrow',
    '⇜': '\\leftarrow',
    '⇛': '\\Rrightarrow',
    '⇚': '\\Lleftarrow',
    '⇙': '\\swarrow',
    '⇘': '\\searrow',
    '⇗': '\\nearrow',
    '⇖': '\\nwarrow',
    '⇕': '\\Updownarrow',
    '⇔': '\\Leftrightarrow',
    '⇓': '\\Downarrow',
    '⇒': '\\Rightarrow',
    '⇑': '\\Uparrow',
    '⇐': '\\Leftarrow',
    '⇏': '\\nRightarrow',
    '⇎': '\\nLeftrightarrow',
    '⇍': '\\nLeftarrow',
    '⇌': '\\rightleftharpoons',
    '⇋': '\\leftrightharpoons',
    '⇊': '\\downdownarrows',
    '⇉': '\\rightrightarrows',
    '⇈': '\\upuparrows',
    '⇇': '\\leftleftarrows',
    '⇆': '\\leftrightarrows',
    '⇅': '',
    '⇄': '\\rightleftarrows',
    '⇃': '\\downharpoonleft',
    '⇂': '\\downharpoonright',
    '⇁': '\\rightharpoondown',
    '⇀': '\\rightharpoonup',
    '↿': '\\upharpoonleft',
    '↾': '\\upharpoonright',
    '↽': '\\leftharpoondown',
    '↼': '\\leftharpoonup',
    '↻': '\\circlearrowright',
    '↺': '\\circlearrowleft',
    '↹': '\\leftrightarrows',
    '↸': '\\overline{\\nwarrow}',
    '↷': '\\curvearrowright',
    '↶': '\\curvearrowleft',
    '↵': '\\swarrow',
    '↴': '\\searrow',
    '↳': '\\Rsh',
    '↲': '\\Lsh',
    '↱': '\\Rsh',
    '↰': '\\Lsh',
    '↯': '\\swarrow',
    '↮': '',
    '↭': '\\leftrightsquigarrow',
    '↬': '\\looparrowright',
    '↫': '\\looparrowleft',
    '↪': '\\hookrightarrow',
    '↩': '\\hookleftarrow',
    '↨': '\\underline{\\updownarrow}',
    '↧': '\\downarrow',
    '↦': '\\mapsto',
    '↥': '\\uparrow',
    '↤': '\\leftarrowtail',
    '↣': '\\rightarrowtail',
    '↢': '\\leftarrowtail',
    '↡': '\\downarrow',
    '↠': '\\twoheadrightarrow',
    '↟': '\\uparrow',
    '↞': '\\twoheadleftarrow',
    '↝': '\\nearrow',
    '↜': '\\nwarrow',
    '↚': '',
    '↙': '\\swarrow',
    '↘': '\\searrow',
    '↗': '\\nearrow',
    '↖': '\\nwarrow',
    '↕': '\\updownarrow',
    '↔': '\\leftrightarrow',
    '↓': '\\downarrow',
    '→': '\\rightarrow',
    '↑': '\\uparrow',
    '←': '\\leftarrow',
    '|||': '\\left|||\\right.',
    '||': '\\left\\|\\right.',
    '|': '\\mid',
    '⫾': '',
    '⫽': '//',
    '⫻': '///',
    '⫺': '',
    '⫹': '',
    '⫸': '',
    '⫷': '',
    '⫶': '\\vdots',
    '⫵': '',
    '⫴': '',
    '⫳': '',
    '⫲': '\\nparallel',
    '⫱': '',
    '⫰': '',
    '⫯': '',
    '⫮': '\\bcancel{\\mid}',
    '⫭': '',
    '⫬': '',
    '⫫': '',
    '⫪': '',
    '⫩': '',
    '⫨': '\\underline{\\perp}',
    '⫧': '\\overline{\\top}',
    '⫦': '',
    '⫥': '',
    '⫤': '',
    '⫣': '',
    '⫢': '',
    '⫡': '',
    '⫠': '\\perp',
    '⫟': '\\top',
    '⫞': '\\dashv',
    '⫝̸': '',
    '⫝': '',
    '⫛': '\\pitchfork',
    '⫚': '',
    '⫙': '',
    '⫘': '',
    '⫗': '',
    '⫖': '',
    '⫕': '',
    '⫔': '',
    '⫓': '',
    '⫒': '',
    '⫑': '',
    '⫐': '',
    '⫏': '',
    '⫎': '',
    '⫍': '',
    '⫌': '\\underset{\\neq}{\\supset}',
    '⫋': '\\underset{\\neq}{\\subset}',
    '⫊': '\\underset{\\approx}{\\supset}',
    '⫉': '\\underset{\\approx}{\\subset}',
    '⫈': '\\underset{\\sim}{\\supset}',
    '⫇': '\\underset{\\sim}{\\subset}',
    '⫆': '\\supseteqq',
    '⫅': '\\subseteqq',
    '⫄': '\\dot{\\supseteq}',
    '⫃': '\\dot{\\subseteq}',
    '⫂': '\\underset{\\times}{\\supset}',
    '⫁': '\\underset{\\times}{\\subset}',
    '⫀': '\\underset{+}{\\supset}',
    '⪿': '\\underset{+}{\\subset}',
    '⪾': '',
    '⪽': '',
    '⪼': '\\gg ',
    '⪻': '\\ll',
    '⪺': '\\underset{\\cancel{\\approx}}{\\succ}',
    '⪹': '\\underset{\\cancel{\\approx}}{\\prec}',
    '⪸': '\\underset{\\approx}{\\succ}',
    '⪷': '\\underset{\\approx}{\\prec}',
    '⪶': '\\underset{\\cancel{=}}{\\succ}',
    '⪵': '\\underset{\\cancel{=}}{\\prec}',
    '⪴': '\\underset{=}{\\succ}',
    '⪳': '\\underset{=}{\\prec}',
    '⪲': '',
    '⪱': '',
    '⪮': '',
    '⪭': '\\underline{\\hcancel{>}}',
    '⪬': '\\underline{\\hcancel{>}}',
    '⪫': '\\hcancel{>}',
    '⪪': '\\hcancel{<}',
    '⪩': '',
    '⪨': '',
    '⪧': '\\vartriangleright',
    '⪦': '\\vartriangleleft',
    '⪥': '><',
    '⪤': '><',
    '⪣': '\\underline{\\ll}',
    '⪢̸': '\\cancel{\\gg}',
    '⪢': '\\gg',
    '⪡̸': '\\cancel{\\ll}',
    '⪡': '\\ll',
    '⪠': '\\overset{\\sim}{\\geqq}',
    '⪟': '\\overset{\\sim}{\\leqq}',
    '⪞': '\\overset{\\sim}{>}',
    '⪝': '\\overset{\\sim}{<}',
    '⪜': '',
    '⪛': '',
    '⪚': '\\overset{=}{>}',
    '⪙': '\\overset{=}{<}',
    '⪘': '',
    '⪗': '',
    '⪖': '',
    '⪕': '',
    '⪔': '',
    '⪓': '',
    '⪒': '\\underset{=}{\\gtrless}',
    '⪑': '\\underset{=}{\\lessgtr}',
    '⪐': '\\underset{<}{\\gtrsim}',
    '⪏': '\\underset{>}{\\lesssim}',
    '⪎': '\\underset{\\simeq}{>}',
    '⪍': '\\underset{\\simeq}{<}',
    '⪌': '\\gtreqqless',
    '⪋': '\\lesseqqgtr',
    '⪊': '\\underset{\\cancel{\\approx}}{>}',
    '⪉': '\\underset{\\approx}{<}',
    '⪆': '\\underset{\\approx}{>}',
    '⪅': '\\underset{\\approx}{<}',
    '⪄': '',
    '⪃': '',
    '⪂': '',
    '⪁': '',
    '⪀': '',
    '⩿': '',
    '⩾̸': '\\bcancel{\\geq}',
    '⩾': '\\geq',
    '⩽̸': '\\bcancel{\\leq}',
    '⩽': '\\leq',
    '⩼': '',
    '⩻': '',
    '⩺': '',
    '⩹': '',
    '⩸': '\\overset{\\dots}{\\equiv}',
    '⩷': '',
    '⩶': '===',
    '⩵': '==',
    '⩴': '::=',
    '⩳': '',
    '⩲': '\\underset{=}{+}',
    '⩱': '\\overset{=}{+}',
    '⩰': '\\overset{\\approx}{=}',
    '⩯': '\\overset{\\wedge}{=}',
    '⩮': '\\overset{*}{=}',
    '⩭': '\\dot{\\approx}',
    '⩬': '',
    '⩫': '',
    '⩪': '\\dot{\\sim}',
    '⩩': '',
    '⩨': '',
    '⩧': '\\dot{\\equiv}',
    '⩦': '\\underset{\\cdot}{=}',
    '⩥': '',
    '⩤': '',
    '⩣': '\\underset{=}{\\vee}',
    '⩢': '\\overset{=}{\\vee}',
    '⩡': 'ul(vv)',
    '⩠': '\\underset{=}{\\wedge}',
    '⩟': '\\underline{\\wedge}',
    '⩞': '\\overset{=}{\\wedge}',
    '⩝': '\\hcancel{\\vee}',
    '⩜': '\\hcancel{\\wedge}',
    '⩛': '',
    '⩚': '',
    '⩙': '',
    '⩘': '\\vee',
    '⩗': '\\wedge',
    '⩖': '',
    '⩕': '',
    '⩔': '',
    '⩓': '',
    '⩒': '\\dot{\\vee}',
    '⩑': '\\dot{\\wedge}',
    '⩐': '',
    '⩏': '',
    '⩎': '',
    '⩍': '\\overline{\\cap}',
    '⩌': '\\overline{\\cup}',
    '⩋': '',
    '⩊': '',
    '⩉': '',
    '⩈': '',
    '⩇': '',
    '⩆': '',
    '⩅': '',
    '⩄': '',
    '⩃': '\\overline{\\cap}',
    '⩂': '\\overline{\\cup}',
    '⩁': '',
    '⩀': '',
    '⨾': '',
    '⨽': '\\llcorner',
    '⨼': '\\lrcorner',
    '⨻': '',
    '⨺': '',
    '⨹': '',
    '⨸': '',
    '⨷': '',
    '⨶': '\\hat{\\otimes}',
    '⨵': '',
    '⨴': '',
    '⨳': '',
    '⨲': '\\underline{\\times}',
    '⨱': '\\underline{\\times}',
    '⨰': '\\dot{\\times}',
    '⨮': '\\bigodot',
    '⨭': '\\bigodot',
    '⨬': '',
    '⨫': '',
    '⨪': '',
    '⨩': '',
    '⨨': '',
    '⨧': '',
    '◻': '\\Box',
    '⨦': '\\underset{\\sim}{+}',
    '⨥': '\\underset{\\circ}{+}',
    '⨤': '\\overset{\\sim}{+}',
    '⨣': '\\hat{+}',
    '⨢': '\\dot{+}',
    '⨡': '\\upharpoonright',
    '⨠': '>>',
    '⨟': '',
    '⨞': '\\triangleleft',
    '⨝': '\\bowtie',
    '⧿': '',
    '⧾': '+',
    '⧻': '\\hcancel{|||}',
    '⧺': '\\hcancel{||}',
    '⧹': '\\backslash',
    '⧸': '/',
    '⧷': 'hcancel{\backslash}',
    '⧶': '',
    '⧵': '\\backslash',
    '⧲': '\\Phi',
    '⧱': '',
    '⧰': '',
    '⧮': '',
    '⧭': '',
    '⧬': '',
    '⧫': '\\lozenge',
    '⧪': '',
    '⧩': '',
    '⧨': '',
    '⧧': '\\ddagger',
    '⧢': '\\sqcup\\sqcup',
    '⧡': '',
    '⧠': '\\square',
    '⧞': '',
    '⧝': '',
    '⧜': '',
    '⧛': '\\{\\{',
    '⧙': '\\{',
    '⧘': '\\}',
    '⧗': '',
    '⧖': '',
    '⧕': '\\bowtie',
    '⧔': '\\bowtie',
    '⧓': '\\bowtie',
    '⧒': '\\bowtie',
    '⧑': '\\bowtie',
    '⧐̸': '| \\not\\triangleright',
    '⧐': '| \\triangleright',
    '⧏̸': '\\not\\triangleleft |',
    '⧏': '\\triangleleft |',
    '⧎': '',
    '⧍': '\\triangle',
    '⧌': '',
    '⧋': '\\underline{\\triangle}',
    '⧊': '\\dot{\\triangle}',
    '⧉': '',
    '⧈': '\\boxed{\\circ}',
    '⧇': '\\boxed{\\circ}',
    '⧆': '\\boxed{\\rightarrow}',
    '⧅': '\\bcancel{\\square}',
    '⧄': '\\cancel{\\square}',
    '⧃': '\\odot',
    '⧂': '\\odot',
    '⦿': '\\odot',
    '⦾': '\\odot',
    '⦽': '\\varnothing',
    '⦼': '\\oplus',
    '⦻': '\\otimes',
    '⦺': '',
    '⦹': '\\varnothing',
    '⦸': '\\varnothing',
    '⦷': '\\ominus',
    '⦶': '\\ominus',
    '⦵': '\\ominus',
    '⦴': '\\vec{\\varnothing}',
    '⦳': '\\vec{\\varnothing}',
    '⦲': '\\dot{\\varnothing}',
    '⦱': '\\overline{\\varnothing}',
    '⦰': '\\varnothing',
    '⦯': '\\measuredangle',
    '⦮': '\\measuredangle',
    '⦭': '\\measuredangle',
    '⦬': '\\measuredangle',
    '⦫': '\\measuredangle',
    '⦪': '\\measuredangle',
    '⦩': '\\measuredangle',
    '⦨': '\\measuredangle',
    '⦧': '',
    '⦦': '',
    '⦥': '',
    '⦤': '',
    '⦣': '\\ulcorner',
    '⦢': '\\measuredangle',
    '⦡': '\\not\\lor',
    '⦠': '\\bcancel{>}',
    '⦂': ':',
    '⦁': '\\cdot',
    '❘': '\\mid',
    '▲': '\\bigtriangleup',
    '⋿': '\\Epsilon',
    '⋾': '\\overline{\\ni}',
    '⋽': '\\overline{\\ni}',
    '⋼': '\\in',
    '⋻': '\\in',
    '⋺': '\\in',
    '⋹': '\\underline{\\in}',
    '⋸': '\\underline{\\in}',
    '⋷': '\\overline{\\in}',
    '⋶': '\\overline{\\in}',
    '⋵': '\\dot{\\in}',
    '⋴': '\\in',
    '⋳': '\\in',
    '⋲': '\\in',
    '⋰': '\\ddots',
    '։': ':',
    '⋩': '\\underset{\\sim}{\\succ}',
    '⋨': '\\underset{\\sim}{\\prec}',
    '⋧': '\\underset{\\not\\sim}{>}',
    '⋦': '\\underset{\\not\\sim}{<}',
    '⋥': '\\not\\sqsupseteq',
    '⋤': '\\not\\sqsubseteq',
    '⋣': '\\not\\sqsupseteq',
    '⋢': '\\not\\sqsubseteq',
    '⋡': '\\nsucc',
    '⋠': '\\nprec',
    '⋟': '\\succ',
    '⋞': '\\prec',
    '⋝': '\\overline{>}',
    '⋜': '\\overline{<}',
    '⋛': '\\underset{>}{\\leq}',
    '⋚': '\\underset{<}{\\geq}',
    '⋕': '\\#',
    '⋓': '\\cup',
    '⋒': '\\cap',
    '⋑': '\\supset',
    '⋐': '\\subset',
    '⋏': '\\wedge',
    '⋎': '\\vee',
    '⋍': '\\simeq',
    '⋈': '\\Join',
    '⋇': '\\ast',
    '⋆': '\\star',
    '⋄': '\\diamond',
    '⊿': '\\triangle',
    '⊾': '\\measuredangle',
    '⊽': '\\overline{\\lor}',
    '⊼': '\\overline{\\land}',
    '⊻': '\\underline{\\lor}',
    '⊺': '\\top',
    土: '\\pm',
    十: '+',
    '⊹': '',
    '⊷': '\\circ\\multimap',
    '⊶': '\\circ\\multimap',
    '⊳': '\\triangleright',
    '⊲': '\\triangleleft',
    '⊱': '\\succ',
    '⊰': '\\prec',
    '⊫': '|\\models',
    '⊪': '|\\models',
    '⊧': '\\models',
    '⊦': '\\vdash',
    '⊝': '\\ominus',
    '⊜': '\\ominus',
    '⊛': '\\odot',
    '⊚': '\\odot',
    '⊔': '\\sqcup',
    '⊓': '\\sqcap',
    '⊒': '\\sqsupseteq',
    '⊑': '\\sqsubseteq',
    '⊐̸': '\\not\\sqsupset',
    '⊐': '\\sqsupset',
    '⊏̸': '\\not\\sqsubset',
    '⊏': '\\sqsubset',
    '⊎': '\\cup',
    '⊍': '\\cup',
    '⊌': '\\cup',
    '≿̸': '\\not\\succsim',
    '≿': '\\succsim',
    '≾': '\\precsim',
    '≹': '\\not\\overset{>}{<}',
    '≸': '\\not\\overset{>}{<}',
    '≷': '\\overset{>}{<}',
    '≶': '\\overset{<}{>}',
    '≵': '\\not\\geg',
    '≴': '\\not\\leq',
    '≳': '\\geg',
    '≲': '\\leq',
    '≬': '',
    '≧': '\\geqq',
    '≦̸': '\\not\\leq',
    '≦': '\\leq',
    '≣': '\\overset{=}{=} ',
    '≞': '\\overset{m}{=} ',
    '≝': '\\overset{def}{=}',
    '≘': '=',
    '≖': '=',
    '≕': '=:',
    '≓': '\\doteq',
    '≒': '\\doteq',
    '≑': '\\doteq',
    '≐': '\\doteq',
    '≏̸': '',
    '≏': '',
    '≎̸': '',
    '≎': '',
    '≌': '\\approx',
    '≋': '\\approx',
    '≊': '\\approx',
    '≂̸': '\\neq',
    '≂': '=',
    '∿': '\\sim',
    '∾': '\\infty',
    '∽̱': '\\sim',
    '∽': '\\sim',
    '∻': '\\sim',
    '∺': ':-:',
    '∹': '-:',
    '∸': '\\bot',
    '∷': '::',
    '∶': ':',
    '∣': '\\mid',
    '∟': '\\llcorner',
    '∘': '\\circ',
    '∗': '*',
    '∕': '/',
    '∎': '\\square',
    '∍': '\\ni',
    '∊': '\\in',
    '∆': '\\Delta',
    '⁄': '/',
    '⪰̸': '\\nsucceq',
    '⪰': '\\succeq',
    '⪯̸': '\\npreceq',
    '⪯': '\\preceq',
    '⪈': '\\ngeqslant',
    '⪇': '\\nleqslant',
    '⧳': '\\Phi',
    '⧦': '\\models',
    '⧥': '\\not\\equiv',
    '⧤': '\\approx\\neq',
    '⧣': '\\neq',
    '⧁': '\\circle',
    '⧀': '\\circle',
    '◦': '\\circle',
    '◗': '\\circle',
    '◖': '\\circle',
    '●': '\\circle',
    '◎': '\\circledcirc',
    '◍': '\\circledcirc',
    '◌': '\\circledcirc',
    '◉': '\\circledcirc',
    '◈': '\\diamond',
    '◇': '\\diamond',
    '◆': '\\diamond',
    '◅': '\\triangleleft',
    '◄': '\\triangleleft',
    '◃': '\\triangleleft',
    '◂': '\\triangleleft',
    '◁': '\\triangleleft',
    '◀': '\\triangleleft',
    '▿': '\\triangledown',
    '▾': '\\triangledown',
    '▽': '\\triangledown',
    '▼': '\\triangledown',
    '▹': '\\triangleright',
    '▸': '\\triangleright',
    '▷': '\\triangleright',
    '▶': '\\triangleright',
    '▵': '\\triangle',
    '▴': '\\triangle',
    '△': '\\triangle',
    '▱': '\\square',
    '▰': '\\blacksquare',
    '▯': '\\square',
    '▮': '\\blacksquare',
    '▭': '\\square',
    '▫': '\\square',
    '▪': '\\square',
    '□': '\\square',
    '■': '\\blacksquare',
    '⋭': '\\not\\triangleright',
    '⋬': '\\not\\triangleleft',
    '⋫': '\\not\\triangleright',
    '⋪': '\\not\\triangleleft',
    '⋙': '\\ggg',
    '⋘': '\\lll',
    '⋗': '*>',
    '⋖': '<*',
    '⋔': '\\pitchfork',
    '⋌': '',
    '⋋': '\\bowtie',
    '⋊': '\\ltimes',
    '⋉': '\\rtimes',
    '⊵': '\\triangleright',
    '\\triangleleft': '',
    '⊥': '\\bot',
    '⊁': '\\nsucc',
    '⊀': '\\preceq',
    '≽': '\\succeq',
    '≼': '\\preceq',
    '≻': '\\succ',
    '≺': '\\prec',
    '≱': '\\geq/',
    '≰': '\\leq/',
    '≭': '\\neq',
    '≫̸': '\\not\\gg',
    '≫': '\\gg',
    '≪̸': '\\not\\ll',
    '≪': '\\ll',
    '≩': '\\ngeqslant',
    '≨': '\\nleqslant',
    '≡': '\\equiv',
    '≟': '\\doteq',
    '≜': '\\triangleq',
    '≛': '\\doteq',
    '≚': '\\triangleq',
    '≙': '\\triangleq',
    '≗': '\\doteq',
    '≔': ':=',
    '≍': '\\asymp',
    '≇': '\\ncong',
    '≆': '\\ncong',
    '≅': '\\cong',
    '≄': '\\not\\simeq',
    '≃': '\\simeq',
    '≁': '\\not\\sim',
    '∦': '\\not\\parallel',
    '∥': '\\parallel',
    '∤': '\\not|',
    '∝': '\\propto',
    '==': '==',
    '=': '=',
    ':=': ':=',
    '/=': '=',
    '-=': '-=',
    '+=': '+=',
    '*=': '*=',
    '!=': '!=',
    '≠': '\\neq',
    '≢': '\\equiv /',
    '≉': '\\approx /',
    '∼': '\\sim',
    '≈': '\\approx',
    '≮': '</',
    '<': '<',
    '≯': '>/',
    '>=': '>=',
    '>': '>',
    '≥': '\\geq',
    '≤': '\\leq',
    '<=': '<=',
    '⊋': '\\supsetneq',
    '⊊': '\\subsetneq',
    '⊉': '\\nsupseteq',
    '⊈': '\\nsubseteq',
    '⊇': '\\supseteq',
    '⊆': '\\subseteq',
    '⊅': '\\not\\supset',
    '⊄': '\\not\\subset',
    '⊃⃒': '\\supset |',
    '⊃': '\\supset',
    '⊂⃒': '\\subset |',
    '⊂': '\\subset',
    '∌': '\\not\\in',
    '∉': '\\notin',
    '∈': '\\in',
    '∁': 'C',
    '∄': '\\nexists',
    '∃': '\\exists',
    '∀': '\\forall',
    '∧': '\\land',
    '&&': '\\&\\&',
    '∨': '\\lor',
    '⊯': '\\cancel{\\vDash}',
    '⊮': '\\cancel{\\Vdash}',
    '⊭': '\\nvDash',
    '⊬': '\\nvDash',
    '⊩': '\\Vdash',
    '⊨': '\\vDash',
    '⊤': '\\top',
    '⊣': '\\dashv',
    '⊢': '\\vdash',
    '∋': '\\ni',
    '⋱': '\\ddots',
    '⋯': '\\hdots',
    '⋮': '\\vdots',
    '϶': '\\ni',
    ':': ':',
    '...': '\\cdots',
    '..': '..',
    '->': '->',
    '∵': '\\because',
    '∴': '\\therefore ',
    '⁣': '',
    ',': ',',
    ';': ';',
    '⧽': '\\}',
    '⧼': '\\{',
    '⦘': '\\]',
    '⦗': '\\[',
    '⦖': '\\ll',
    '⦕': '\\gg',
    '⦔': '\\gg',
    '⦓': '\\ll',
    '⦒': '\\gg',
    '⦑': '\\ll',
    '⦐': '\\]',
    '⦏': '\\]',
    '⦎': '\\]',
    '⦍': '\\[',
    '⦌': '\\[',
    '⦋': '\\]',
    '⦊': '\\triangleright',
    '⦉': '\\triangleleft',
    '⦈': '|\\)',
    '⦇': '\\(|',
    '⦆': '|\\)',
    '⦅': '\\(\\(',
    '⦄': '|\\}',
    '⦃': '\\{|',
    '⦀': '\\||',
    '⟯': '\\left. \\right]',
    '⟮': '\\left[ \\right.',
    '⟭': '\\left. \\right]]',
    '⟬': '\\left[[ \\right.',
    '⟫': '\\gg',
    '⟪': '\\ll',
    '⟧': '\\)|',
    '⟦': '\\(|',
    '❳': '\\left.\\right)',
    '❲': '\\left(\\right.',
    '〉': '\\rangle',
    '〈': '\\langle',
    '⌋': '\\rfloor',
    '⌊': '\\lfloor',
    '⌉': '\\rceil',
    '⌈': '\\lceil',
    '‖': '\\parallel',
    '}': '\\left.\\right\\}',
    '{': '\\left\\{\\right.',
    ']': '\\left]\\right.',
    '[': '\\left[\\right.',
    ')': '\\left.\\right)',
    '(': '\\left(\\right.',
    '”': '\\"',
    '“': '\\text{``}',
    '’': "'",
    '‘': '`',
    α: '\\alpha',
    β: '\\beta',
    γ: '\\gamma',
    Γ: '\\Gamma',
    δ: '\\delta',
    Δ: '\\Delta',
    ϵ: '\\epsilon',
    ζ: '\\zeta',
    η: '\\eta',
    θ: '\\theta',
    Θ: '\\Theta',
    ι: '\\iota',
    κ: '\\kappa',
    λ: '\\lambda',
    ν: '\\nu',
    ο: '\\omicron',
    π: '\\pi',
    Π: '\\Pi',
    ρ: '\\rho',
    σ: '\\sigma',
    Σ: '\\Sigma',
    τ: '\\tau',
    υ: '\\upsilon',
    Υ: '\\Upsilon',
    ϕ: '\\phi',
    Φ: '\\Phi',
    χ: '\\chi',
    ψ: '\\psi',
    Ψ: '\\Psi',
    ω: '\\omega',
    Ω: '\\Omega',
    Ω: '\\Omega',
    '∅': '\\emptyset',
    '⟲': '\\circlearrowleft',
    '⟳': '\\circlearrowright',
    '×': '\\times',
    '½': '\\dfrac{1}{2}',
    μ: '\\mu',
    Ө: '\\theta',
    '✓': '\\checkmark',
    '⟩': '\\rangle',
    '⟨': '\\langle',
    '¼': '\\dfrac{1}{4}',
    '…': '\\ldots',
    ℏ: '\\hbar',
    ℜ: '\\mathfrak{R}',
    Ѳ: '\\theta',
    Ø: '\\emptyset',
    ϱ: '\\varrho',
    ф: '\\phi',
    ℇ: '\\varepsilon',
    T: 'T',
    '∙': '\\cdot',
    Ρ: 'P',
    '∞': '\\infty',
    ᐁ: '\\nabla',
    ƞ: '\\eta',
    '⁺': '^{+}',
    '⁻': '^{-}',
    '⁼': '^{=}',
    '⁽': '^{(}',
    '⁾': '^{)}',
    '〗': '\\)|',
    '〖': '\\langle',
    ';': ';',
    '൦': '\\circ',
    '┴': '\\perp',
    '✕': '\\times',
    '⎻': '-',
    '»': '\\gg',
    '⬆': '\\uparrow',
    '⬇': '\\downarrow',
    '⬅': '\\leftarrow',
    '➡': '\\rightarrow',
    '⎼': '-',
    '⎜': '\\mid',
    '⎥': '\\mid',
    ħ: '\\hbar',
    '⮕': '\\rightarrow',
    '・': '\\cdot',
    '¦': '\\mid',
    '£': '\\pounds',
    '¥': '\\yen',
    '✗': '\\times',
    '✔': '\\checkmark',
    ⁿ: '^{n}',
    '«': '\\ll',
    เ: '\\prime',
    '†': '\\dagger',
    '│': '\\mid',
    $: '\\$',
    '#': '\\#',
    '℃': '\\text{\\textdegree C}',
    '℉': '\\text{\\textdegree F}',
    '█': '\\blacksquare',
    '℧': '\\mho',
    ⅇ: '\\text{e}',
    ɼ: 'r',
    '‡': '\\ddagger',
    ἱ: 'i',
    ϒ: '\\Upsilon',
    '𝛿': '\\delta',
    '˳': '\\cdot',
    ѳ: '\\theta',
    '𝜙': '\\phi',
    П: '\\prod',
    о: 'o',
    ђ: '\\hbar',
    Ʌ: '\\Lambda',
    '।': '\\mid',
    '€': '\\euro',
    ῡ: '\\bar{u}',
    φ: '\\varphi',
    ȼ: 'c',
    '𝞮': '\\epsilon',
    Χ: '\\mathsf{X}',
    ₙ: '_{n}',
};

/**
 * Lookup table mapping math symbols, keyed by their literal HTML character
 * reference (e.g. `&#x2200;`), to the equivalent LaTeX command. Consumed by the
 * `<mi>`/`<mo>` converters to translate symbol content into LaTeX.
 */
const allMathSymbolsByChar = {
    '&#xA0;': '\\textrm{ }',
    '&#x2203;': '\\exists',
    '&#x2200;': '\\forall',
    '&#x21D4;': '\\iff',
    '&#x21D2;': '=>',
    '&#xAC;': '\\neg',
    '&#x2124;': '\\mathbb{Z}',
    '&#x211D;': '\\mathbb{R}',
    '&#x211A;': '\\mathbb{Q}',
    '&#x2115;': '\\mathbb{N}',
    '&#x2102;': 'CC',
    '&#x25A1;': '\\square',
    '&#x22C4;': '\\diamond',
    '&#x25B3;': '\\triangle',
    '&#x2322;': '\\frown',
    '&#x2220;': '\\angle',
    '&#x22F1;': '\\ddots',
    '&#x22EE;': '\\vdots',
    '&#x2235;': '\\because',
    '&#x2234;': '\\therefore',
    '&#x2135;': '\\aleph',
    '&#x2205;': '\\oslash',
    '&#xB1;': '\\pm',
    '&#x2207;': '\\nabla',
    '&#x2202;': '\\partial',
    '&#x222E;': '\\oint',
    '&#x222B;': '\\int',
    '&#x22C3;': '\\cup',
    '&#x222A;': '\\cup',
    '&#x22C2;': '\\cap',
    '&#x2229;': '\\cap',
    '&#x22C1;': '\\vee',
    '&#x2228;': '\\vee',
    '&#x22C0;': '\\wedge',
    '&#x2227;': '\\wedge',
    '&#x220F;': '\\prod',
    '&#x2211;': '\\sum',
    '&#x2299;': '\\bigodot',
    '&#x2297;': '\\bigoplus',
    '&#x2295;': 'o+',
    '&#x2218;': '@',
    '&#x22C8;': '\\bowtie',
    '&#x22CA;': '\\rtimes',
    '&#x22C9;': '\\ltimes',
    '&#xF7;': '\\div',
    '&#xD7;': '\\times',
    '\\': '\\backslash',
    '&#x22C6;': '\\star',
    '&#x2217;': '\\star',
    '&#x22C5;': '\\cdot',
    '&#x3A9;': '\\Omega',
    '&#x3C9;': '\\omega',
    '&#x3A8;': '\\Psi',
    '&#x3C8;': '\\psi',
    '&#x3C7;': '\\chi',
    '&#x3C6;': '\\varphi',
    '&#x3A6;': '\\Phi',
    '&#x3D5;': '\\phi',
    '&#x3C5;': '\\upsilon',
    '&#x3C4;': '\\tau',
    '&#x3A3;': '\\Sigma',
    '&#x3C3;': '\\sigma',
    '&#x3C1;': '\\rho',
    '&#x3A0;': '\\Pi',
    '&#x3C0;': '\\pi',
    '&#x39E;': '\\Xi',
    '&#x3BE;': '\\xi',
    '&#x3BD;': '\\nu',
    '&#x3BC;': '\\mu',
    '&#x39B;': '\\Lambda',
    '&#x3BB;': '\\lambda',
    '&#x3BA;': '\\kappa',
    '&#x3B9;': '\\iota',
    '&#x3D1;': '\\vartheta',
    '&#x398;': '\\Theta',
    '&#x3B8;': '\\theta',
    '&#x3B7;': '\\eta',
    '&#x3B6;': '\\zeta',
    '&#x25B;': '\\varepsilon',
    '&#x3B5;': '\\epsilon',
    '&#x394;': '\\Delta',
    '&#x3B4;': '\\delta',
    '&#x393;': '\\Gamma',
    '&#x3B3;': '\\gamma',
    '&#x3B2;': '\\beta',
    '&#x3B1;': '\\alpha',
    '&#x221E;': '\\infty',
    '\u202c': '\\text{\\textdir TRT}',
    '\u200e': '\\text{\\textdir LTR}',
};

/**
 * Lookup table mapping math symbols, keyed by their decoded unicode glyph
 * (e.g. `∀`), to the equivalent LaTeX command. Consumed by the `<mi>`/`<mo>`
 * converters to translate symbol content into LaTeX.
 */
const allMathSymbolsByGlyph = {
    ' ': '\\textrm{ }',
    // Invisible operators (U+2061-U+2064): no visual rendering in LaTeX.
    '⁡': '',
    '⁢': '',
    '⁣': '',
    '⁤': '',
    '∃': '\\exists',
    '∀': '\\forall',
    '⇔': '\\iff',
    '⇒': '\\Rightarrow',
    '¬': '\\neg',
    '□': '\\square',
    '⋄': '\\diamond',
    '△': '\\triangle',
    '⌢': '\\frown',
    '∠': '\\angle',
    '⋱': '\\ddots',
    '⋮': '\\vdots',
    '∵': '\\because',
    '∴': '\\therefore',
    ℵ: '\\aleph',
    '∅': '\\emptyset',
    '±': '\\pm',
    '∇': '\\nabla',
    '∂': '\\partial',
    '∮': '\\oint',
    '∫': '\\int',
    '⋃': '\\cup',
    '∪': '\\cup',
    '⋂': '\\cap',
    '∩': '\\cap',
    '⋁': '\\vee',
    '∨': '\\vee',
    '⋀': '\\wedge',
    '∧': '\\wedge',
    '∏': '\\prod',
    '∑': '\\sum',
    '⊙': '\\bigodot',
    '⊗': '\\bigoplus',
    '⊕': 'o+',
    '∘': '@',
    '⋈': '\\bowtie',
    '⋊': '\\rtimes',
    '⋉': '\\ltimes',
    '÷': '\\div',
    '×': '\\times',
    '\\': '\\backslash',
    '⋆': '\\star',
    '∗': '\\star',
    '⋅': '\\cdot',
    Ω: '\\Omega',
    ω: '\\omega',
    Ψ: '\\Psi',
    ψ: '\\psi',
    χ: '\\chi',
    φ: '\\varphi',
    Φ: '\\Phi',
    ϕ: '\\phi',
    υ: '\\upsilon',
    τ: '\\tau',
    Σ: '\\Sigma',
    σ: '\\sigma',
    ρ: '\\rho',
    Π: '\\Pi',
    π: '\\pi',
    Ξ: '\\Xi',
    ξ: '\\xi',
    ν: '\\nu',
    μ: '\\mu',
    Λ: '\\Lambda',
    λ: '\\lambda',
    κ: '\\kappa',
    ι: '\\iota',
    ϑ: '\\vartheta',
    Θ: '\\Theta',
    θ: '\\theta',
    η: '\\eta',
    ζ: '\\zeta',
    ɛ: '\\varepsilon',
    ε: '\\epsilon',
    Δ: '\\Delta',
    δ: '\\delta',
    Γ: '\\Gamma',
    γ: '\\gamma',
    β: '\\beta',
    α: '\\alpha',
    '∞': '\\infty',
    ϵ: '\\epsilon',
    µ: '\\mu',
    '²': '^{2}',
    ı: '\\imath',
    '∎': '\\blacksquare',
    ม: '\\mathbf{m}',
    Ω: '\\Omega',
    '⟲': '\\circlearrowleft',
    '⟳': '\\circlearrowright',
    त: ' ',
    '¥': '\\yen',
    '⁽': '^{(}',
    '⁾': '^{)}',
    ß: '\\ss',
    Ћ: '\\hbar',
    '⦵': '\\ominus',
    '⊿': '\\bigtriangleup',
    "↛'": '\\nrightarrow',
    '†': '\\dagger',
    เ: '\\prime',
    白: ' ',
    '⿱': ' ',
    ℸ: '\\wp',
    퓰: ' ',
    ⁿ: '^{n}',
    '✔': '\\checkmark',
    '✗': '\\times',
    '½': '\\dfrac{1}{2}',
    Ө: '\\theta',
    '✓': '\\checkmark',
    '⟩': '\\rangle',
    '⟨': '\\langle',
    '〈': '\\langle',
    '¼': '\\dfrac{1}{4}',
    '…': '\\ldots',
    ℏ: '\\hbar',
    ℜ: '\\mathfrak{R}',
    Ѳ: '\\theta',
    Ø: '\\emptyset',
    ϱ: '\\varrho',
    ф: '\\phi',
    T: 'T',
    '∙': '\\cdot',
    Ρ: 'P',
    ᐁ: '\\nabla',
    ƞ: '\\eta',
    ɣ: '\\gamma',
    ћ: '\\hbar',
    Ɛ: '\\varepsilon',
    ⅅ: '\\_{D}',
    𝜆: '\\lambda',
    '〗': '\\rangle',
    '〖': '\\langle',
    ';': ';',
    𝑥: 'x',
    𝑦: 'y',
    𝑧: 'z',
    𝑖: 'i',
    𝑗: 'j',
    𝑘: 'k',
    𝑚: 'm',
    𝑒: 'e',
    𝑟: 'r',
    ɳ: '\\eta',
    '𝛽': '\\beta',
    '⍵': '\\omega',
    '℘': '\\wp',
    '𝜋': '\\pi',
    Є: '\\epsilon',
    є: '\\epsilon',
    𝜀: '\\epsilon',
    п: '\\pi',
    Ν: '\\nu',
    ɵ: '\\theta',
    '𝜓': '\\psi',
    ϴ: '\\theta',
    ɸ: '\\phi',
    Ӷ: '\\Gamma',
    ɭ: '\\ell',
    ʋ: '\\upsilon',
    𝛟: '\\varphi',
    '⍬': '\\theta',
    Ф: '\\Phi',
    '𝜑': '\\varphi',
    ⅈ: 'i',
    ο: 'o',
    ơ: 'o',
    ƒ: 'f',
    '⍴': '\\rho',
    '🇽': 'x',
    '𝑝': 'p',
    '𝑞': 'q',
    '𝑠': 's',
    '𝑡': 't',
    '𝑢': 'u',
    '𝑣': 'v',
    '𝑤': 'w',
    '𝑎': 'a',
    '𝑏': 'b',
    '𝑐': 'c',
    '𝑑': 'd',
    '𝑓': 'f',
    '𝑔': 'g',
    '𝑙': 'l',
    '𝑛': 'n',
    '𝑜': 'o',
    '𝔀': 'w',
    '𝚟': 'v',
    ṁ: 'm',
    '൦': '\\circ',
    '┴': '\\perp',
    '✕': '\\times',
    '∣': '\\mid',
    Փ: '\\Phi',
    '⎜': '\\mid',
    ħ: '\\hbar',
    ፈ: ' ',
    '⦨': '\\llbracket',
    ế: '\\hat{e}',
    '¢': '\\cent',
    '⤹': '\\downarrow',
    '⤸': '\\downarrow',
    '⤷': '\\Rsh',
    '⤶': '\\Lsh',
    '⤵': '\\downarrow',
    '⤴': '\\uparrow',
    '⤳': '\\rightarrow',
    '|': '\\mid',
    '⎥': '\\mid',
    '♥': '\\heartsuit',
    О: '0',
    Υ: 'Y',
    х: 'x',
    𝓏: 'z',
    𝓎: 'y',
    𝓍: 'x',
    р: 'p',
    а: 'a',
    '£': '\\pounds',
    m: 'm',
    '𝚵': '\\Xi',
    '⓪': '\\textcircled{0}',
    '①': '\\textcircled{1}',
    '②': '\\textcircled{2}',
    '③': '\\textcircled{3}',
    '④': '\\textcircled{4}',
    '⑤': '\\textcircled{5}',
    '⑥': '\\textcircled{6}',
    '⑦': '\\textcircled{7}',
    '⑧': '\\textcircled{8}',
    '⑨': '\\textcircled{9}',
    '⑩': '\\textcircled{10}',
    '⑪': '\\textcircled{11}',
    '⑫': '\\textcircled{12}',
    '⑬': '\\textcircled{13}',
    '⑭': '\\textcircled{14}',
    '⑮': '\\textcircled{15}',
    '⑯': '\\textcircled{16}',
    '⑰': '\\textcircled{17}',
    '⑱': '\\textcircled{18}',
    '⑲': '\\textcircled{19}',
    '⑳': '\\textcircled{20}',
    '㉑': '\\textcircled{21}',
    '㉒': '\\textcircled{22}',
    '㉓': '\\textcircled{23}',
    '㉔': '\\textcircled{24}',
    '㉕': '\\textcircled{25}',
    '㉖': '\\textcircled{26}',
    '㉗': '\\textcircled{27}',
    '㉘': '\\textcircled{28}',
    '㉙': '\\textcircled{29}',
    '㉚': '\\textcircled{30}',
    '㉛': '\\textcircled{31}',
    '㉜': '\\textcircled{32}',
    '㉝': '\\textcircled{33}',
    '㉞': '\\textcircled{34}',
    '㉟': '\\textcircled{35}',
    '㊱': '\\textcircled{36}',
    '㊲': '\\textcircled{37}',
    '㊳': '\\textcircled{38}',
    '㊴': '\\textcircled{39}',
    '㊵': '\\textcircled{40}',
    '㊶': '\\textcircled{41}',
    '㊷': '\\textcircled{42}',
    '㊸': '\\textcircled{43}',
    '㊹': '\\textcircled{44}',
    '㊺': '\\textcircled{45}',
    '㊻': '\\textcircled{46}',
    '㊼': '\\textcircled{47}',
    '㊽': '\\textcircled{48}',
    '㊾': '\\textcircled{49}',
    '㊿': '\\textcircled{50}',
    '&': '\\&',
    '‖': '\\parallel',
    '%': '\\%',
    '“': '\\text{``}',
    $: '\\$',
    '#': '\\#',
    '℃': '\\text{\\textdegree C}',
    '℉': '\\text{\\textdegree F}',
    '█': '\\blacksquare',
    '℧': '\\mho',
    '⌋': '\\rfloor',
    '⌊': '\\lfloor',
    '⌉': '\\rceil',
    '⌈': '\\lceil',
    ℇ: '\\varepsilon',
    ⅇ: '\\text{e}',
    ɼ: 'r',
    '↛': '\\nrightarrow',
    ˆ: '\\hat{}',
    '‾': '\\overline',
    '→': '\\rightarrow',
    '‡': '\\ddagger',
    '・': '\\cdot',
    '▱': '\\square',
    '∆': '\\Delta',
    ἱ: 'i',
    '∡': '\\angle',
    ϒ: '\\Upsilon',
    '↓': '\\downarrow',
    '↑': '\\uparrow',
    '»': '\\gg',
    '⊤': '\\top',
    '⧸': '/',
    '𝛿': '\\delta',
    '˳': '\\cdot',
    '։': ':',
    '⦪': '\\measuredangle',
    '⦩': '\\measuredangle',
    '⦫': '\\measuredangle',
    '⦁': '\\cdot',
    ѳ: '\\theta',
    '⦢': '\\measuredangle',
    '¸': ',',
    '⎻': '\\overline',
    '⟦': '\\llbracket',
    '𝜙': '\\phi',
    П: '\\prod',
    о: 'o',
    '≈': '\\approx',
    '≤': '\\leq',
    ђ: '\\hbar',
    Ʌ: '\\Lambda',
    土: '\\pm',
    '⎼': '-',
    十: '+',
    '≠': '\\neq',
    '←': '\\leftarrow',
    '।': '\\mid',
    '€': '\\euro',
    '˘': ' ',
    ῡ: '\\bar{u}',
    '∥': '\\parallel',
    '↔': '\\leftrightarrow',
    '√': '\\sqrt{}',
    ȼ: 'c',
    '𝞮': '\\epsilon',
    '·': '\\cdot',
    '⦬': '\\measuredangle',
    '⦮': '\\measuredangle',
    '⦭': '\\measuredangle',
    '«': '\\ll',
    Χ: '\\mathsf{X}',
    '│': '\\mid',
    '〉': '\\rangle',
    ₙ: '_{n}',
    '▫': '\\square',
    '●': '\\circle',
    '”': '\\"',
};

/**
 * The glyphs MathML uses for double-bar (norm) fences: the ascii digraph and
 * the unicode characters `‖` (U+2016) and `∥` (U+2225), as in issue #43.
 *
 * Centralized so every consumer stays in sync: the fence normalizer (sibling
 * pairing), the generic wrapper (translation to the `\|` delimiter) and the
 * mfenced separators (Vmatrix classification).
 */
const doubleBarFenceGlyphs = new Set(['||', '‖', '∥']);

/**
 * List of LaTeX accent commands that wrap their argument directly (e.g.
 * `\hat{x}`). Consumed by the under/over converters to decide whether an accent
 * should be applied as a command rather than via `\overset`/`\underset`.
 */
const latexAccents = ['\\hat', '\\bar', '\\underbrace', '\\overbrace'];

/**
 * Lookup table mapping unicode sub/superscript digit glyphs (e.g. `₀`, `⁰`),
 * keyed by glyph, to their LaTeX subscript/superscript form. Consumed by the
 * `<mi>`/`<mn>` converters to translate numeric script glyphs into LaTeX.
 */
const mathNumberByGlyph = {
    '₀': '_{0}',
    '₁': '_{1}',
    '₂': '_{2}',
    '₃': '_{3}',
    '₄': '_{4}',
    '₅': '_{5}',
    '₆': '_{6}',
    '₇': '_{7}',
    '₈': '_{8}',
    '₉': '_{9}',
    '⁰': '^{0}',
    '¹': '^{1}',
    '²': '^{2}',
    '³': '^{3}',
    '⁴': '^{4}',
    '⁵': '^{5}',
    '⁶': '^{6}',
    '⁷': '^{7}',
    '⁸': '^{8}',
    '⁹': '^{9}',
    ⁿ: '^{n}',
    ₙ: '_{n}',
    '⓪': '\\textcircled{0}',
    '①': '\\textcircled{1}',
    '②': '\\textcircled{2}',
    '③': '\\textcircled{3}',
    '④': '\\textcircled{4}',
    '⑤': '\\textcircled{5}',
    '⑥': '\\textcircled{6}',
    '⑦': '\\textcircled{7}',
    '⑧': '\\textcircled{8}',
    '⑨': '\\textcircled{9}',
    '⑩': '\\textcircled{10}',
    '⑪': '\\textcircled{11}',
    '⑫': '\\textcircled{12}',
    '⑬': '\\textcircled{13}',
    '⑭': '\\textcircled{14}',
    '⑮': '\\textcircled{15}',
    '⑯': '\\textcircled{16}',
    '⑰': '\\textcircled{17}',
    '⑱': '\\textcircled{18}',
    '⑲': '\\textcircled{19}',
    '⑳': '\\textcircled{20}',
    '㉑': '\\textcircled{21}',
    '㉒': '\\textcircled{22}',
    '㉓': '\\textcircled{23}',
    '㉔': '\\textcircled{24}',
    '㉕': '\\textcircled{25}',
    '㉖': '\\textcircled{26}',
    '㉗': '\\textcircled{27}',
    '㉘': '\\textcircled{28}',
    '㉙': '\\textcircled{29}',
    '㉚': '\\textcircled{30}',
    '㉛': '\\textcircled{31}',
    '㉜': '\\textcircled{32}',
    '㉝': '\\textcircled{33}',
    '㉞': '\\textcircled{34}',
    '㉟': '\\textcircled{35}',
    '㊱': '\\textcircled{36}',
    '㊲': '\\textcircled{37}',
    '㊳': '\\textcircled{38}',
    '㊴': '\\textcircled{39}',
    '㊵': '\\textcircled{40}',
    '㊶': '\\textcircled{41}',
    '㊷': '\\textcircled{42}',
    '㊸': '\\textcircled{43}',
    '㊹': '\\textcircled{44}',
    '㊺': '\\textcircled{45}',
    '㊻': '\\textcircled{46}',
    '㊼': '\\textcircled{47}',
    '㊽': '\\textcircled{48}',
    '㊾': '\\textcircled{49}',
    '㊿': '\\textcircled{50}',
    '½': '\\dfrac{1}{2}',
    '⅓': '\\dfrac{1}{3}',
    '⅔': '\\dfrac{2}{3}',
    '¼': '\\dfrac{1}{4}',
    '¾': '\\dfrac{3}{4}',
    '⅕': '\\dfrac{1}{5}',
    '⅖': '\\dfrac{2}{5}',
    '⅗': '\\dfrac{3}{5}',
    '⅘': '\\dfrac{4}{5}',
    '⅙': '\\dfrac{1}{6}',
    '⅚': '\\dfrac{5}{6}',
    '⅐': '\\dfrac{1}{7}',
    '⅛': '\\dfrac{1}{8}',
    '⅜': '\\dfrac{3}{8}',
    '⅝': '\\dfrac{5}{8}',
    '⅞': '\\dfrac{7}{8}',
    '⅑': '\\dfrac{1}{9}',
    '⅒': '\\dfrac{1}{10}',
};

/**
 * This class is used to convert some accents and special characters from utf-8 to latex math commands
 */
class HashUTF8ToLtXConverter {
    convert(char) {
        const isAlphanumeric = char.match(/[a-z0-9]/i);
        if (isAlphanumeric) {
            return char;
        }
        const accentChar = vowelsWithAccents[char];
        if (accentChar) {
            const lxtCmd = this.convertAccentCharToLtX(accentChar);
            if (lxtCmd) {
                return lxtCmd;
            }
            return char;
        }
        const specialChar = this.convertSpecialCharToLtX(char);
        if (specialChar) {
            return specialChar;
        }
        return char;
    }
    convertAccentCharToLtX(char) {
        const { char: vowel, accent } = char;
        const cmd = accentToLTXCmd[accent];
        if (!cmd) {
            return null;
        }
        return `\\${cmd}{${vowel}}`;
    }
    convertSpecialCharToLtX(char) {
        const specialFontChar = specialFontChars[char];
        if (!specialFontChar) {
            return null;
        }
        const { letter, fontCmd } = specialFontChar;
        return `\\${fontCmd}{${letter}}`;
    }
}
const vowelsWithAccents = {
    á: { char: 'a', accent: '´' },
    à: { char: 'a', accent: '`' },
    â: { char: 'a', accent: '^' },
    ã: { char: 'a', accent: '~' },
    ä: { char: 'a', accent: '¨' },
    å: { char: 'a', accent: '˚' },
    ą: { char: 'a', accent: '˙' },
    ă: { char: 'a', accent: '˘' },
    ǎ: { char: 'a', accent: 'ˇ' },
    ǟ: { char: 'a', accent: 'ˆ' },
    ǻ: { char: 'a', accent: '˙' },
    ǡ: { char: 'a', accent: '-' },
    ā: { char: 'a', accent: '-' },
    é: { char: 'e', accent: '´' },
    è: { char: 'e', accent: '`' },
    ê: { char: 'e', accent: '^' },
    ë: { char: 'e', accent: '¨' },
    ę: { char: 'e', accent: '˙' },
    ě: { char: 'e', accent: 'ˇ' },
    ȇ: { char: 'i', accent: '^' },
    ё: { char: 'e', accent: '¨' },
    ē: { char: 'e', accent: '-' },
    í: { char: 'i', accent: '´' },
    ì: { char: 'i', accent: '`' },
    î: { char: 'i', accent: '^' },
    ï: { char: 'i', accent: '¨' },
    į: { char: 'i', accent: '˙' },
    ǐ: { char: 'i', accent: 'ˇ' },
    ȉ: { char: 'i', accent: '`' },
    ȋ: { char: 'i', accent: '¨' },
    ī: { char: 'i', accent: '-' },
    ó: { char: 'o', accent: '´' },
    ò: { char: 'o', accent: '`' },
    ô: { char: 'o', accent: '^' },
    õ: { char: 'o', accent: '~' },
    ö: { char: 'o', accent: '¨' },
    ő: { char: 'o', accent: '˝' },
    ǒ: { char: 'o', accent: 'ˇ' },
    ȍ: { char: 'o', accent: '`' },
    ȏ: { char: 'o', accent: '¨' },
    ȫ: { char: 'o', accent: '˘' },
    ȭ: { char: 'o', accent: '˝' },
    ȯ: { char: 'o', accent: '˙' },
    ō: { char: 'o', accent: '-' },
    ú: { char: 'u', accent: '´' },
    ù: { char: 'u', accent: '`' },
    û: { char: 'u', accent: '^' },
    ü: { char: 'u', accent: '¨' },
    ű: { char: 'u', accent: '˝' },
    ǔ: { char: 'u', accent: 'ˇ' },
    ǖ: { char: 'u', accent: '¨' },
    ǘ: { char: 'u', accent: '¨' },
    ǚ: { char: 'u', accent: '¨' },
    ǜ: { char: 'u', accent: '¨' },
    ȕ: { char: 'u', accent: '`' },
    ȗ: { char: 'u', accent: '¨' },
    ū: { char: 'u', accent: '-' },
    ý: { char: 'y', accent: '´' },
    ỳ: { char: 'y', accent: '`' },
    ŷ: { char: 'y', accent: '^' },
    ÿ: { char: 'y', accent: '¨' },
    ȳ: { char: 'y', accent: '-' },
    Á: { char: 'A', accent: '´' },
    À: { char: 'A', accent: '`' },
    Â: { char: 'A', accent: '^' },
    Ã: { char: 'A', accent: '~' },
    Ä: { char: 'A', accent: '¨' },
    Å: { char: 'A', accent: '˚' },
    Å: { char: 'A', accent: '˚' },
    Ȧ: { char: 'A', accent: '˙' },
    Ă: { char: 'A', accent: '˘' },
    Ǎ: { char: 'A', accent: 'ˇ' },
    Ǟ: { char: 'A', accent: '˝' },
    Ǻ: { char: 'A', accent: '˚' },
    Ǡ: { char: 'A', accent: '-' },
    Ā: { char: 'A', accent: '-' },
    É: { char: 'E', accent: '´' },
    È: { char: 'E', accent: '`' },
    Ė: { char: 'E', accent: '˙' },
    Ê: { char: 'E', accent: '^' },
    Ë: { char: 'E', accent: '¨' },
    Ě: { char: 'E', accent: 'ˇ' },
    Ȅ: { char: 'E', accent: '`' },
    Ȇ: { char: 'E', accent: '¨' },
    Ē: { char: 'E', accent: '-' },
    Í: { char: 'I', accent: '´' },
    Ì: { char: 'I', accent: '`' },
    Î: { char: 'I', accent: '^' },
    Ï: { char: 'I', accent: '¨' },
    Ĭ: { char: 'I', accent: '˘' },
    Ǐ: { char: 'I', accent: 'ˇ' },
    Ȉ: { char: 'I', accent: '`' },
    Ȋ: { char: 'I', accent: '¨' },
    Ī: { char: 'I', accent: '-' },
    Ó: { char: 'O', accent: '´' },
    Ò: { char: 'O', accent: '`' },
    Ô: { char: 'O', accent: '^' },
    Õ: { char: 'O', accent: '~' },
    Ö: { char: 'O', accent: '¨' },
    Ő: { char: 'O', accent: '˝' },
    Ǒ: { char: 'O', accent: 'ˇ' },
    Ȍ: { char: 'O', accent: '`' },
    Ȏ: { char: 'O', accent: '¨' },
    Ȫ: { char: 'O', accent: '˘' },
    Ȭ: { char: 'O', accent: '˝' },
    Ȯ: { char: 'O', accent: '˙' },
    Ō: { char: 'O', accent: '-' },
    Ú: { char: 'U', accent: '´' },
    Ù: { char: 'U', accent: '`' },
    Û: { char: 'U', accent: '^' },
    Ü: { char: 'U', accent: '¨' },
    Ű: { char: 'U', accent: '˝' },
    Ǔ: { char: 'U', accent: 'ˇ' },
    Ǖ: { char: 'U', accent: '¨' },
    Ȕ: { char: 'U', accent: '`' },
    Ȗ: { char: 'U', accent: '¨' },
    Ū: { char: 'U', accent: '-' },
    Ý: { char: 'Y', accent: '´' },
    Ỳ: { char: 'Y', accent: '`' },
    Ŷ: { char: 'Y', accent: '^' },
    Ÿ: { char: 'Y', accent: '¨' },
    Ȳ: { char: 'Y', accent: '-' },
    ñ: { char: 'n', accent: '~' },
    Ñ: { char: 'N', accent: '~' },
    ç: { char: 'c', accent: '˙' },
    Ç: { char: 'C', accent: '˙' },
    ṽ: { char: 'v', accent: '~' },
    Ṽ: { char: 'V', accent: '~' },
    ĵ: { char: 'j', accent: '^' },
    Ĵ: { char: 'J', accent: '^' },
    ź: { char: 'z', accent: '´' },
    Ź: { char: 'Z', accent: '´' },
    Ż: { char: 'Z', accent: '^' },
    ż: { char: 'z', accent: '^' },
    Ž: { char: 'Z', accent: 'ˇ' },
    ž: { char: 'z', accent: 'ˇ' },
    ẑ: { char: 'z', accent: 'ˆ' },
};
const accentToLTXCmd = {
    '´': 'acute',
    '`': 'grave',
    '^': 'hat',
    '~': 'tilde',
    '¨': 'ddot',
    '˚': 'mathring',
    '˘': 'breve',
    ˇ: 'check',
    '˝': 'H',
    '˙': 'dot',
    '-': 'bar',
    ˆ: 'hat',
    '˜': 'tilde',
};
const specialFontChars = {
    // letter A mathbf
    𝐀: { letter: 'A', fontCmd: 'mathbf' },
    𝐁: { letter: 'B', fontCmd: 'mathbf' },
    𝐂: { letter: 'C', fontCmd: 'mathbf' },
    𝐃: { letter: 'D', fontCmd: 'mathbf' },
    𝐄: { letter: 'E', fontCmd: 'mathbf' },
    Ε: { letter: 'E', fontCmd: 'mathbf' },
    𝐅: { letter: 'F', fontCmd: 'mathbf' },
    𝐆: { letter: 'G', fontCmd: 'mathbf' },
    𝐇: { letter: 'H', fontCmd: 'mathbf' },
    𝐈: { letter: 'I', fontCmd: 'mathbf' },
    𝐉: { letter: 'J', fontCmd: 'mathbf' },
    𝐊: { letter: 'K', fontCmd: 'mathbf' },
    𝐋: { letter: 'L', fontCmd: 'mathbf' },
    𝐌: { letter: 'M', fontCmd: 'mathbf' },
    𝐍: { letter: 'N', fontCmd: 'mathbf' },
    𝐎: { letter: 'O', fontCmd: 'mathbf' },
    𝐏: { letter: 'P', fontCmd: 'mathbf' },
    𝐐: { letter: 'Q', fontCmd: 'mathbf' },
    𝐑: { letter: 'R', fontCmd: 'mathbf' },
    𝐒: { letter: 'S', fontCmd: 'mathbf' },
    𝐓: { letter: 'T', fontCmd: 'mathbf' },
    𝐔: { letter: 'U', fontCmd: 'mathbf' },
    𝐕: { letter: 'V', fontCmd: 'mathbf' },
    𝐖: { letter: 'W', fontCmd: 'mathbf' },
    𝐗: { letter: 'X', fontCmd: 'mathbf' },
    𝞆: { letter: 'X', fontCmd: 'mathbf' },
    𝐘: { letter: 'Y', fontCmd: 'mathbf' },
    𝐙: { letter: 'Z', fontCmd: 'mathbf' },
    '𝟎': { letter: '0', fontCmd: 'mathbf' },
    '𝟏': { letter: '1', fontCmd: 'mathbf' },
    '𝟐': { letter: '2', fontCmd: 'mathbf' },
    '𝟑': { letter: '3', fontCmd: 'mathbf' },
    '𝟒': { letter: '4', fontCmd: 'mathbf' },
    '𝟓': { letter: '5', fontCmd: 'mathbf' },
    '𝟔': { letter: '6', fontCmd: 'mathbf' },
    '𝟕': { letter: '7', fontCmd: 'mathbf' },
    '𝟖': { letter: '8', fontCmd: 'mathbf' },
    '𝟗': { letter: '9', fontCmd: 'mathbf' },
    // letter A mathit
    𝐴: { letter: 'A', fontCmd: 'mathit' },
    𝐵: { letter: 'B', fontCmd: 'mathit' },
    𝐶: { letter: 'C', fontCmd: 'mathit' },
    𝐷: { letter: 'D', fontCmd: 'mathit' },
    𝐸: { letter: 'E', fontCmd: 'mathit' },
    𝐹: { letter: 'F', fontCmd: 'mathit' },
    𝐺: { letter: 'G', fontCmd: 'mathit' },
    𝐻: { letter: 'H', fontCmd: 'mathit' },
    𝐼: { letter: 'I', fontCmd: 'mathit' },
    Ι: { letter: 'I', fontCmd: 'mathit' },
    𝐽: { letter: 'J', fontCmd: 'mathit' },
    𝐾: { letter: 'K', fontCmd: 'mathit' },
    𝐿: { letter: 'L', fontCmd: 'mathit' },
    𝑀: { letter: 'M', fontCmd: 'mathit' },
    𝑁: { letter: 'N', fontCmd: 'mathit' },
    𝑂: { letter: 'O', fontCmd: 'mathit' },
    𝑃: { letter: 'P', fontCmd: 'mathit' },
    𝑄: { letter: 'Q', fontCmd: 'mathit' },
    𝑅: { letter: 'R', fontCmd: 'mathit' },
    𝑆: { letter: 'S', fontCmd: 'mathit' },
    𝑇: { letter: 'T', fontCmd: 'mathit' },
    𝑈: { letter: 'U', fontCmd: 'mathit' },
    𝑉: { letter: 'V', fontCmd: 'mathit' },
    𝑊: { letter: 'W', fontCmd: 'mathit' },
    𝑋: { letter: 'X', fontCmd: 'mathit' },
    𝑌: { letter: 'Y', fontCmd: 'mathit' },
    𝑍: { letter: 'Z', fontCmd: 'mathit' },
    // letter A mathbb
    𝔸: { letter: 'A', fontCmd: 'mathbb' },
    𝔹: { letter: 'B', fontCmd: 'mathbb' },
    ℂ: { letter: 'C', fontCmd: 'mathbb' },
    𝔻: { letter: 'D', fontCmd: 'mathbb' },
    𝔼: { letter: 'E', fontCmd: 'mathbb' },
    𝔽: { letter: 'F', fontCmd: 'mathbb' },
    𝔾: { letter: 'G', fontCmd: 'mathbb' },
    ℍ: { letter: 'H', fontCmd: 'mathbb' },
    𝕀: { letter: 'I', fontCmd: 'mathbb' },
    𝕁: { letter: 'J', fontCmd: 'mathbb' },
    𝕂: { letter: 'K', fontCmd: 'mathbb' },
    𝕃: { letter: 'L', fontCmd: 'mathbb' },
    𝕄: { letter: 'M', fontCmd: 'mathbb' },
    ℕ: { letter: 'N', fontCmd: 'mathbb' },
    𝕆: { letter: 'O', fontCmd: 'mathbb' },
    ℙ: { letter: 'P', fontCmd: 'mathbb' },
    ℚ: { letter: 'Q', fontCmd: 'mathbb' },
    ℝ: { letter: 'R', fontCmd: 'mathbb' },
    𝕊: { letter: 'S', fontCmd: 'mathbb' },
    𝕋: { letter: 'T', fontCmd: 'mathbb' },
    𝕌: { letter: 'U', fontCmd: 'mathbb' },
    𝕍: { letter: 'V', fontCmd: 'mathbb' },
    𝕎: { letter: 'W', fontCmd: 'mathbb' },
    𝕏: { letter: 'X', fontCmd: 'mathbb' },
    𝕐: { letter: 'Y', fontCmd: 'mathbb' },
    ℤ: { letter: 'Z', fontCmd: 'mathbb' },
    '𝟘': { letter: '0', fontCmd: 'mathbb' },
    '𝟙': { letter: '1', fontCmd: 'mathbb' },
    '𝟚': { letter: '2', fontCmd: 'mathbb' },
    '𝟛': { letter: '3', fontCmd: 'mathbb' },
    '𝟜': { letter: '4', fontCmd: 'mathbb' },
    '𝟝': { letter: '5', fontCmd: 'mathbb' },
    '𝟞': { letter: '6', fontCmd: 'mathbb' },
    '𝟟': { letter: '7', fontCmd: 'mathbb' },
    '𝟠': { letter: '8', fontCmd: 'mathbb' },
    '𝟡': { letter: '9', fontCmd: 'mathbb' },
    // letter A mathcal
    𝒜: { letter: 'A', fontCmd: 'mathcal' },
    𝓐: { letter: 'A', fontCmd: 'mathcal' },
    ℬ: { letter: 'B', fontCmd: 'mathcal' },
    𝒞: { letter: 'C', fontCmd: 'mathcal' },
    𝒟: { letter: 'D', fontCmd: 'mathcal' },
    𝓓: { letter: 'D', fontCmd: 'mathcal' },
    ℰ: { letter: 'E', fontCmd: 'mathcal' },
    ℱ: { letter: 'F', fontCmd: 'mathcal' },
    𝓕: { letter: 'F', fontCmd: 'mathcal' },
    𝒢: { letter: 'G', fontCmd: 'mathcal' },
    ℋ: { letter: 'H', fontCmd: 'mathcal' },
    ℐ: { letter: 'I', fontCmd: 'mathcal' },
    𝒥: { letter: 'J', fontCmd: 'mathcal' },
    𝒦: { letter: 'K', fontCmd: 'mathcal' },
    ℒ: { letter: 'L', fontCmd: 'mathcal' },
    𝓛: { letter: 'L', fontCmd: 'mathcal' },
    ℳ: { letter: 'M', fontCmd: 'mathcal' },
    𝒩: { letter: 'N', fontCmd: 'mathcal' },
    𝒪: { letter: 'O', fontCmd: 'mathcal' },
    𝓞: { letter: 'O', fontCmd: 'mathcal' },
    𝒫: { letter: 'P', fontCmd: 'mathcal' },
    𝒬: { letter: 'Q', fontCmd: 'mathcal' },
    ℛ: { letter: 'R', fontCmd: 'mathcal' },
    𝕽: { letter: 'R', fontCmd: 'mathcal' },
    '℟': { letter: 'R', fontCmd: 'mathcal' },
    𝒮: { letter: 'S', fontCmd: 'mathcal' },
    𝒯: { letter: 'T', fontCmd: 'mathcal' },
    𝒰: { letter: 'U', fontCmd: 'mathcal' },
    𝒱: { letter: 'V', fontCmd: 'mathcal' },
    𝒲: { letter: 'W', fontCmd: 'mathcal' },
    𝒳: { letter: 'X', fontCmd: 'mathcal' },
    𝒴: { letter: 'Y', fontCmd: 'mathcal' },
    𝒵: { letter: 'Z', fontCmd: 'mathcal' },
    // letter A mathfrak
    𝔄: { letter: 'A', fontCmd: 'mathfrak' },
    𝔅: { letter: 'B', fontCmd: 'mathfrak' },
    ℭ: { letter: 'C', fontCmd: 'mathfrak' },
    𝔇: { letter: 'D', fontCmd: 'mathfrak' },
    𝔈: { letter: 'E', fontCmd: 'mathfrak' },
    𝔉: { letter: 'F', fontCmd: 'mathfrak' },
    𝔊: { letter: 'G', fontCmd: 'mathfrak' },
    ℌ: { letter: 'H', fontCmd: 'mathfrak' },
    ℑ: { letter: 'I', fontCmd: 'mathfrak' },
    𝔍: { letter: 'J', fontCmd: 'mathfrak' },
    𝔎: { letter: 'K', fontCmd: 'mathfrak' },
    𝔏: { letter: 'L', fontCmd: 'mathfrak' },
    𝔐: { letter: 'M', fontCmd: 'mathfrak' },
    𝔑: { letter: 'N', fontCmd: 'mathfrak' },
    𝔒: { letter: 'O', fontCmd: 'mathfrak' },
    𝔓: { letter: 'P', fontCmd: 'mathfrak' },
    𝔔: { letter: 'Q', fontCmd: 'mathfrak' },
    ℜ: { letter: 'R', fontCmd: 'mathfrak' },
    𝔖: { letter: 'S', fontCmd: 'mathfrak' },
    𝔗: { letter: 'T', fontCmd: 'mathfrak' },
    𝔘: { letter: 'U', fontCmd: 'mathfrak' },
    𝔙: { letter: 'V', fontCmd: 'mathfrak' },
    𝔚: { letter: 'W', fontCmd: 'mathfrak' },
    𝔛: { letter: 'X', fontCmd: 'mathfrak' },
    𝔜: { letter: 'Y', fontCmd: 'mathfrak' },
    ℨ: { letter: 'Z', fontCmd: 'mathfrak' },
    // letter A mathsf
    𝖠: { letter: 'A', fontCmd: 'mathsf' },
    Α: { letter: 'A', fontCmd: 'mathsf' },
    𝖡: { letter: 'B', fontCmd: 'mathsf' },
    Β: { letter: 'B', fontCmd: 'mathsf' },
    𝖢: { letter: 'C', fontCmd: 'mathsf' },
    𝖣: { letter: 'D', fontCmd: 'mathsf' },
    𝖤: { letter: 'E', fontCmd: 'mathsf' },
    𝖥: { letter: 'F', fontCmd: 'mathsf' },
    𝖦: { letter: 'G', fontCmd: 'mathsf' },
    𝖧: { letter: 'H', fontCmd: 'mathsf' },
    𝖨: { letter: 'I', fontCmd: 'mathsf' },
    𝖩: { letter: 'J', fontCmd: 'mathsf' },
    ȷ: { letter: 'J', fontCmd: 'mathsf' },
    𝖪: { letter: 'K', fontCmd: 'mathsf' },
    Κ: { letter: 'K', fontCmd: 'mathsf' },
    𝖫: { letter: 'L', fontCmd: 'mathsf' },
    𝖬: { letter: 'M', fontCmd: 'mathsf' },
    𝖭: { letter: 'N', fontCmd: 'mathsf' },
    𝖮: { letter: 'O', fontCmd: 'mathsf' },
    𝖯: { letter: 'P', fontCmd: 'mathsf' },
    𝖰: { letter: 'Q', fontCmd: 'mathsf' },
    𝖱: { letter: 'R', fontCmd: 'mathsf' },
    𝖲: { letter: 'S', fontCmd: 'mathsf' },
    𝖳: { letter: 'T', fontCmd: 'mathsf' },
    𝖴: { letter: 'U', fontCmd: 'mathsf' },
    𝖵: { letter: 'V', fontCmd: 'mathsf' },
    𝖶: { letter: 'W', fontCmd: 'mathsf' },
    𝖷: { letter: 'X', fontCmd: 'mathsf' },
    Χ: { letter: 'X', fontCmd: 'mathsf' },
    𝖸: { letter: 'Y', fontCmd: 'mathsf' },
    𝖹: { letter: 'Z', fontCmd: 'mathsf' },
    // letter A mathtt
    𝚨: { letter: 'A', fontCmd: 'mathtt' },
    𝚩: { letter: 'B', fontCmd: 'mathtt' },
    𝚪: { letter: '\\Gamma', fontCmd: 'mathtt' },
    𝚫: { letter: '\\Delta', fontCmd: 'mathtt' },
    𝚬: { letter: 'E', fontCmd: 'mathtt' },
    𝚭: { letter: 'F', fontCmd: 'mathtt' },
    𝚮: { letter: 'G', fontCmd: 'mathtt' },
    𝚯: { letter: '\\Theta', fontCmd: 'mathtt' },
    𝚰: { letter: 'I', fontCmd: 'mathtt' },
    𝚱: { letter: 'J', fontCmd: 'mathtt' },
    𝚲: { letter: '\\Lambda', fontCmd: 'mathtt' },
    𝚳: { letter: 'L', fontCmd: 'mathtt' },
    𝚴: { letter: 'M', fontCmd: 'mathtt' },
    𝚵: { letter: '\\Pi', fontCmd: 'mathtt' },
    𝚶: { letter: 'O', fontCmd: 'mathtt' },
    𝚷: { letter: '\\Pi', fontCmd: 'mathtt' },
    𝚸: { letter: 'Q', fontCmd: 'mathtt' },
    𝚹: { letter: 'R', fontCmd: 'mathtt' },
    𝚺: { letter: 'S', fontCmd: 'mathtt' },
    𝚻: { letter: 'T', fontCmd: 'mathtt' },
    𝚼: { letter: 'U', fontCmd: 'mathtt' },
    𝚽: { letter: '\\Phi', fontCmd: 'mathtt' },
    𝚾: { letter: 'W', fontCmd: 'mathtt' },
    𝚿: { letter: '\\Psi', fontCmd: 'mathtt' },
    𝛀: { letter: '\\Omega', fontCmd: 'mathtt' },
};

/**
 * Wraps a string with auto-sizing LaTeX delimiters, prefixing the given
 * open/close characters with `\left` and `\right` (e.g. `\left( ... \right)`).
 */
class GenericWrapper {
    constructor(open, close) {
        this._open = '\\left' + this._toLatexDelimiter(open);
        this._close = '\\right' + this._toLatexDelimiter(close);
    }
    /** @returns `str` wrapped in `\left`/`\right` delimiters. */
    wrap(str) {
        return new Wrapper(this._open, this._close).wrap(str);
    }
    /** Maps a MathML fence character to its valid `\left`/`\right` delimiter form. */
    _toLatexDelimiter(delimiter) {
        var _a;
        return (_a = DELIMITER_TRANSLATIONS.get(delimiter)) !== null && _a !== void 0 ? _a : delimiter;
    }
}
/**
 * MathML fence characters that are not valid `\left`/`\right` delimiters as-is:
 * braces are grouping characters (need `\{`/`\}`) and the double-bar family
 * is the norm delimiter `\|` (a bare `\left||` is a stretchy bar followed by a
 * literal one, and the unicode glyphs are not delimiters LaTeX knows at all,
 * as in issue #43).
 */
const DELIMITER_TRANSLATIONS = new Map([
    ['{', '\\{'],
    ['}', '\\}'],
    ...[...doubleBarFenceGlyphs].map((glyph) => [glyph, '\\|']),
]);

/**
 * Joins a list of strings using a list of separators, one per gap. When there
 * are more gaps than separators the last separator is reused; when no
 * separators are given, `defaultSeparator` is used. Used by `<mfenced>` to honor
 * its `separators` attribute.
 */
class JoinWithManySeparators {
    constructor(separators) {
        this._separators = separators;
    }
    /**
     * @param arr - the strings to join.
     * @param separators - the separators to place between items, in order.
     * @param defaultSeparator - separator used when `separators` is empty.
     * @returns the joined string.
     */
    static join(arr, separators, defaultSeparator = '') {
        const effectiveSeparators = separators.length > 0 ? separators : defaultSeparator !== undefined ? [defaultSeparator] : [];
        return new JoinWithManySeparators(effectiveSeparators)._join(arr);
    }
    _join(arr) {
        return arr.reduce((joinedStr, currentStr, currentIndex, strArr) => {
            const separator = currentIndex === strArr.length - 1 ? '' : this._get(currentIndex);
            return joinedStr + currentStr + separator;
        }, '');
    }
    _get(index) {
        if (!this._separators[index])
            return this._separators[this._separators.length - 1];
        return this._separators[index];
    }
}

/**
 * Looks a key up in a lookup table, returning `undefined` for keys that are not
 * own properties (e.g. `constructor`, `toString`, `__proto__`) instead of
 * resolving them through the object's prototype chain. This keeps element text
 * that happens to collide with `Object.prototype` members from leaking
 * unexpected values into the output.
 *
 * @param table - the lookup table.
 * @param key - the key to look up.
 * @returns the mapped value, or `undefined` when the key is not an own property.
 */
const ownLookup = (table, key) => Object.prototype.hasOwnProperty.call(table, key) ? table[key] : undefined;

/** Resolves a single identifier value to its LaTeX command via symbol lookup tables, falling back to UTF-8 conversion. */
class Character {
    constructor(value) {
        this._value = value;
    }
    static apply(value) {
        return new Character(value)._apply();
    }
    _apply() {
        var _a, _b, _c;
        // `??` instead of `||`: entries mapped to an empty string (e.g. the
        // invisible operators U+2061-U+2064) are valid results, not lookup misses.
        return ((_c = (_b = (_a = this._findByCharacter()) !== null && _a !== void 0 ? _a : this._findByGlyph()) !== null && _b !== void 0 ? _b : this._findByNumber()) !== null && _c !== void 0 ? _c : new HashUTF8ToLtXConverter().convert(this._value));
    }
    _findByCharacter() {
        return ownLookup(allMathSymbolsByChar, this._value);
    }
    _findByGlyph() {
        return ownLookup(allMathSymbolsByGlyph, this._value);
    }
    _findByNumber() {
        return ownLookup(mathNumberByGlyph, this._value);
    }
}

/**
 * Converts a MathML `<mi>` (identifier) element into LaTeX.
 *
 * Maps known math symbols/glyphs to their LaTeX commands (falling back to UTF-8
 * conversion), then wraps the result according to the `mathvariant` attribute.
 *
 * @example
 * // <mi mathvariant="bold">x</mi> -> \mathbf{x}
 */
class MI {
    constructor(mathElement) {
        this.utf8Converter = new HashUTF8ToLtXConverter();
        this._mathmlElement = mathElement;
    }
    /**
     * @returns the LaTeX representation of this element.
     */
    convert() {
        const normalizedValue = normalizeWhiteSpaces(this._mathmlElement.value);
        if (normalizedValue === ' ')
            return Character.apply(normalizedValue);
        const trimmedValue = normalizedValue.trim();
        const convertedChar = Character.apply(trimmedValue);
        const parsedChar = this.utf8Converter.convert(convertedChar);
        if (parsedChar !== convertedChar)
            return parsedChar;
        return this.wrapInMathVariant(convertedChar, this.getMathVariant(this._mathmlElement.attributes));
    }
    /** Extracts the `mathvariant` attribute value, or undefined when absent. */
    getMathVariant(attributes) {
        if (!attributes || !attributes.mathvariant)
            return undefined;
        return attributes.mathvariant;
    }
    /** Wraps the value in the LaTeX font command matching the given `mathvariant`. */
    wrapInMathVariant(value, mathVariant) {
        switch (mathVariant) {
            case 'bold':
                return `\\mathbf{${value}}`;
            case 'italic':
                return `\\mathit{${value}}`;
            case 'bold-italic':
                return `\\mathbf{\\mathit{${value}}}`;
            case 'double-struck':
                return `\\mathbb{${value}}`;
            case 'bold-fraktur':
                return `\\mathbf{\\mathfrak{${value}}}`;
            case 'script':
                return `\\mathcal{${value}}`;
            case 'bold-script':
                return `\\mathbf{\\mathcal{${value}}}`;
            case 'fraktur':
                return `\\mathfrak{${value}}`;
            case 'sans-serif':
                return `\\mathsf{${value}}`;
            case 'bold-sans-serif':
                return `\\mathbf{\\mathsf{${value}}}`;
            case 'sans-serif-italic':
                return `\\mathsf{\\mathit{${value}}}`;
            case 'sans-serif-bold-italic':
                return `\\mathbf{\\mathsf{\\mathit{${value}}}}`;
            case 'monospace':
                return `\\mathtt{${value}}`;
            default:
                return value;
        }
    }
}

/** Resolves a single operator value to its LaTeX command via operator lookup tables, falling back to UTF-8 conversion. */
class Operator {
    constructor(value) {
        this._value = value;
    }
    static operate(value) {
        return new Operator(value)._operate();
    }
    _operate() {
        var _a, _b, _c;
        // `??` instead of `||`: entries mapped to an empty string (e.g. the
        // invisible operators U+2061-U+2064) are valid results, not lookup misses.
        return ((_c = (_b = (_a = this._findByCharacter()) !== null && _a !== void 0 ? _a : this._findByGlyph()) !== null && _b !== void 0 ? _b : this._findByNumber()) !== null && _c !== void 0 ? _c : new HashUTF8ToLtXConverter().convert(this._value));
    }
    _findByCharacter() {
        return ownLookup(allMathOperatorsByChar, this._value);
    }
    _findByGlyph() {
        return ownLookup(allMathOperatorsByGlyph, this._value);
    }
    _findByNumber() {
        return ownLookup(mathNumberByGlyph, this._value);
    }
}

/**
 * Converts a MathML `<mo>` (operator) element into LaTeX.
 *
 * Normalizes/trims the value and resolves it to a LaTeX command through the
 * operator lookup tables, falling back to UTF-8 conversion.
 *
 * @example
 * // <mo>&#x2211;</mo> -> \sum
 */
class MO {
    constructor(mathElement) {
        this._mathmlElement = mathElement;
    }
    /**
     * @returns the LaTeX representation of this element.
     */
    convert() {
        const normalizedValue = normalizeWhiteSpaces(this._mathmlElement.value);
        const trimmedValue = normalizedValue.trim();
        return Operator.operate(trimmedValue);
    }
}

/**
 * Converts a MathML `<mn>` (number) element into LaTeX.
 *
 * Normalizes/trims the value and maps it through the number-by-glyph table,
 * returning the raw normalized value when no mapping exists.
 *
 * @example
 * // <mn>42</mn> -> 42
 */
class MN {
    constructor(mathElement) {
        this._mathmlElement = mathElement;
    }
    /**
     * @returns the LaTeX representation of this element.
     */
    convert() {
        const normalizedValue = normalizeWhiteSpaces(this._mathmlElement.value).trim();
        const convertedValue = ownLookup(mathNumberByGlyph, normalizedValue);
        return convertedValue || normalizedValue;
    }
}

/**
 * Converts a MathML `<msqrt>` element into a LaTeX square root.
 *
 * Recursively converts every child, joins the results with spaces and wraps
 * them in `\sqrt{...}`.
 *
 * @example
 * // <msqrt><mn>2</mn></msqrt> -> \sqrt{2}
 */
class MSqrt {
    constructor(mathElement) {
        this._mathmlElement = mathElement;
    }
    /**
     * @returns the LaTeX representation of this element.
     */
    convert() {
        const latexJoinedChildren = this._mathmlElement.children
            .map((child) => mathMLElementToLaTeXConverter(child))
            .map((converter) => converter.convert())
            .join(' ');
        return `\\sqrt{${latexJoinedChildren}}`;
    }
}

/** Joins the converted children with the requested separators and wraps them in the open/close fences. */
class Vector {
    constructor(open, close, separators, defaultSeparator) {
        this.separators = separators;
        this.defaultSeparator = defaultSeparator;
        this.open = open || '(';
        this.close = close || ')';
    }
    apply(latexContents) {
        const contentWithoutWrapper = JoinWithManySeparators.join(latexContents, this.separators, this.defaultSeparator);
        return new GenericWrapper(this.open, this.close).wrap(contentWithoutWrapper);
    }
}

/** Classifies a pair of open/close fences and wraps content in them. */
class Separators {
    constructor(open, close) {
        this.open = open;
        this.close = close;
    }
    wrap(str) {
        return new GenericWrapper(this.open, this.close).wrap(str);
    }
    areParentheses() {
        return this._compare('(', ')');
    }
    areSquareBrackets() {
        return this._compare('[', ']');
    }
    areBrackets() {
        return this._compare('{', '}');
    }
    areDivides() {
        return this._compare('|', '|');
    }
    areParallels() {
        return this.open === this.close && doubleBarFenceGlyphs.has(this.open);
    }
    areNotEqual() {
        return this.open !== this.close;
    }
    _compare(openToCompare, closeToCompare) {
        return this.open === openToCompare && this.close === closeToCompare;
    }
}

/** Renders the converted children as a LaTeX matrix environment chosen from the open/close fences. */
class Matrix {
    constructor(open, close) {
        this._genericCommand = 'matrix';
        this.separators = new Separators(open, close);
    }
    apply(latexContents) {
        const command = this._command;
        const matrix = `\\begin{${command}}\n${latexContents.join('')}\n\\end{${command}}`;
        return command === this._genericCommand ? this.separators.wrap(matrix) : matrix;
    }
    get _command() {
        if (this.separators.areParentheses())
            return 'pmatrix';
        if (this.separators.areSquareBrackets())
            return 'bmatrix';
        if (this.separators.areBrackets())
            return 'Bmatrix';
        if (this.separators.areDivides())
            return 'vmatrix';
        if (this.separators.areParallels())
            return 'Vmatrix';
        if (this.separators.areNotEqual())
            return this._genericCommand;
        return 'bmatrix';
    }
}

/**
 * Converts a MathML `<mfenced>` element into LaTeX delimited content.
 *
 * Children are converted and either wrapped as a matrix (when a descendant
 * `<mtable>` is present, picking the environment from the open/close fences) or
 * joined with the `separators`/`open`/`close` attributes as a delimited vector.
 *
 * @example
 * // <mfenced open="(" close=")"><mi>a</mi><mi>b</mi></mfenced> -> \left(a,b\right)
 */
class MFenced {
    constructor(mathmlElement) {
        this._mathmlElement = mathmlElement;
        this.open = this._mathmlElement.attributes.open || '';
        this.close = this._mathmlElement.attributes.close || '';
    }
    /**
     * @returns the LaTeX representation of this element.
     */
    convert() {
        const latexChildren = this._mathmlElement.children
            .map((child) => mathMLElementToLaTeXConverter(child))
            .map((converter) => converter.convert());
        if (this._isThereRelativeOfName(this._mathmlElement.children, 'mtable'))
            return new Matrix(this.open, this.close).apply(latexChildren);
        const separatorsAttr = this._mathmlElement.attributes.separators;
        const hasSeparatorsAttribute = separatorsAttr !== undefined;
        const separatorsArray = separatorsAttr ? Array.from(separatorsAttr) : [];
        // Use comma default only if no separators **attribute** was provided
        const defaultSeparator = !hasSeparatorsAttribute ? ',' : '';
        return new Vector(this.open, this.close, separatorsArray, defaultSeparator).apply(latexChildren);
    }
    /** Depth-first searches the subtree for any descendant element with the given name. */
    _isThereRelativeOfName(mathmlElements, elementName) {
        var _a;
        // Iterative depth-first search so a deeply nested subtree cannot overflow
        // the call stack.
        const stack = [...mathmlElements];
        while (stack.length > 0) {
            const element = stack.pop();
            if (element.name === elementName)
                return true;
            stack.push(...((_a = element.children) !== null && _a !== void 0 ? _a : []));
        }
        return false;
    }
}

/**
 * Thrown when a MathML element does not have the number of children its
 * converter requires (e.g. `<mfrac>` needs exactly two).
 */
class InvalidNumberOfChildrenError extends Error {
    /**
     * @param tagName - the offending element's tag name.
     * @param expectedNumberOfChild - the number of children the converter expects.
     * @param currentNumberOfChild - the number of children actually present.
     * @param comparison - how to read the expectation (e.g. `exactly`, `at least`).
     */
    constructor(tagName, expectedNumberOfChild, currentNumberOfChild, comparison = 'exactly') {
        super(`${tagName} tag must have ${comparison} ${expectedNumberOfChild} children. It's actually ${currentNumberOfChild}`);
        this.name = 'InvalidNumberOfChildrenError';
    }
}

/**
 * Value object that interprets a MathML `<mfrac>` `linethickness` attribute and
 * resolves it to a LaTeX bar thickness usable in `\genfrac`.
 *
 * Mappings that LaTeX cannot reproduce exactly are approximated in points:
 * unitless multipliers of the (renderer-defined) default rule thickness, the
 * `thin`/`thick` keywords, percentages and pixels.
 *
 * @example
 * // new LineThickness(undefined).toLaTeX() -> null   (default \frac bar)
 * @example
 * // new LineThickness('0').toLaTeX()       -> '0pt'  (bar-less)
 * @example
 * // new LineThickness('2pt').toLaTeX()     -> '2pt'  (absolute, passed through)
 * @example
 * // new LineThickness('2').toLaTeX()       -> '0.8pt' (2x the default thickness)
 */
class LineThickness {
    constructor(raw) {
        this._raw = raw;
    }
    /**
     * @returns the LaTeX dimension to use as the fraction bar thickness, or `null`
     * to keep the default-thickness `\frac` bar.
     */
    toLaTeX() {
        if (!this._raw)
            return null;
        const value = this._raw.trim().toLowerCase();
        // Keywords. `medium` equals the default bar; `thin`/`thick` are renderer-defined.
        if (value === 'medium')
            return null;
        if (value === 'thin')
            return formatPoints(DEFAULT_RULE_THICKNESS_PT * 0.5);
        if (value === 'thick')
            return formatPoints(DEFAULT_RULE_THICKNESS_PT * 2);
        const match = value.match(/^(-?\d*\.?\d+)\s*([a-z%]*)$/);
        if (!match)
            return null;
        const amount = parseFloat(match[1]);
        const unit = match[2];
        if (amount === 0)
            return '0pt';
        // Unitless number: a multiplier of the (renderer-defined) default rule thickness.
        if (unit === '')
            return amount === 1 ? null : formatPoints(amount * DEFAULT_RULE_THICKNESS_PT);
        // Pixels: not a TeX unit, approximated with points (1px = 0.75pt at 96dpi).
        if (unit === 'px')
            return formatPoints(amount * PT_PER_PX);
        // Percentage: relative to the default rule thickness.
        if (unit === '%')
            return formatPoints((amount / 100) * DEFAULT_RULE_THICKNESS_PT);
        // Absolute lengths LaTeX understands: pass through unchanged.
        if (TEX_LENGTH_UNITS.includes(unit))
            return `${amount}${unit}`;
        // Unknown unit: fall back to the default bar.
        return null;
    }
}
/** Approximation of the default fraction rule thickness, which MathML leaves unspecified. */
const DEFAULT_RULE_THICKNESS_PT = 0.4;
/** Points per CSS pixel at 96dpi (1px = 1/96in, 1pt = 1/72in). */
const PT_PER_PX = 0.75;
/** Length units LaTeX can consume directly inside a `\genfrac` thickness slot. */
const TEX_LENGTH_UNITS = ['pt', 'pc', 'in', 'cm', 'mm', 'em', 'ex', 'bp', 'dd', 'cc'];
/** Formats a point value, trimming floating-point noise (e.g. `0.8pt`). */
function formatPoints(valuePt) {
    return `${Math.round(valuePt * 10000) / 10000}pt`;
}

/**
 * Converts a MathML `<mfrac>` element into a LaTeX fraction.
 *
 * Produces `\frac{numerator}{denominator}` for a default-thickness bar, or the
 * bevelled form `numerator/denominator` (each side wrapped in parentheses when
 * longer than one character) when the `bevelled` attribute is set.
 *
 * When `linethickness` requests a non-default bar, the `\genfrac` form is used so
 * the thickness is preserved: `\genfrac{}{}{<thickness>}{}{numerator}{denominator}`.
 * A zero thickness renders without the bar (used, for instance, inside binomial
 * coefficients). The surrounding parentheses of a binomial coefficient are not
 * added here: in canonical MathML they come from sibling `<mo>`/`<mfenced>`
 * elements and are converted independently. Thickness parsing is delegated to
 * {@link LineThickness}.
 *
 * @example
 * // <mfrac><mn>1</mn><mn>2</mn></mfrac> -> \frac{1}{2}
 * @example
 * // <mfrac bevelled="true"><mn>1</mn><mn>2</mn></mfrac> -> 1/2
 * @example
 * // <mfrac linethickness="0"><mi>n</mi><mi>k</mi></mfrac> -> \genfrac{}{}{0pt}{}{n}{k}
 * @example
 * // <mfrac linethickness="2pt"><mn>1</mn><mn>2</mn></mfrac> -> \genfrac{}{}{2pt}{}{1}{2}
 */
class MFrac {
    constructor(mathElement) {
        this._mathmlElement = mathElement;
    }
    /**
     * @returns the LaTeX representation of this element.
     * @throws {InvalidNumberOfChildrenError} if the element does not have exactly 2 children.
     */
    convert() {
        const { children, name } = this._mathmlElement;
        const childrenLength = children.length;
        if (childrenLength !== 2)
            throw new InvalidNumberOfChildrenError(name, 2, childrenLength);
        const num = mathMLElementToLaTeXConverter(children[0]).convert();
        const den = mathMLElementToLaTeXConverter(children[1]).convert();
        if (this._isBevelled())
            return `${this._wrapIfMoreThanOneChar(num)}/${this._wrapIfMoreThanOneChar(den)}`;
        const thickness = new LineThickness(this._mathmlElement.attributes.linethickness).toLaTeX();
        if (thickness !== null)
            return `\\genfrac{}{}{${thickness}}{}{${num}}{${den}}`;
        return `\\frac{${num}}{${den}}`;
    }
    /** Wraps the bevelled-fraction side in parentheses when it is longer than one character. */
    _wrapIfMoreThanOneChar(str) {
        return new ParenthesisWrapper().wrapIfMoreThanOneChar(str);
    }
    /** Whether the `bevelled` attribute requests the inline `a/b` form. */
    _isBevelled() {
        return !!this._mathmlElement.attributes.bevelled;
    }
}

/**
 * Converts a MathML `<mroot>` element into a LaTeX n-th root.
 *
 * Converts the first child as the radicand and the second child as the index,
 * producing `\sqrt[index]{content}`.
 *
 * @example
 * // <mroot><mn>8</mn><mn>3</mn></mroot> -> \sqrt[3]{8}
 */
class MRoot {
    constructor(mathElement) {
        this._mathmlElement = mathElement;
    }
    /**
     * @returns the LaTeX representation of this element.
     * @throws {InvalidNumberOfChildrenError} if the element does not have exactly 2 children.
     */
    convert() {
        const { name, children } = this._mathmlElement;
        const childrenLength = children.length;
        if (childrenLength !== 2)
            throw new InvalidNumberOfChildrenError(name, 2, childrenLength);
        const content = mathMLElementToLaTeXConverter(children[0]).convert();
        const rootIndex = mathMLElementToLaTeXConverter(children[1]).convert();
        return `\\sqrt[${rootIndex}]{${content}}`;
    }
}

/**
 * Converts a MathML `<maction>` element into LaTeX.
 *
 * For toggle actions (or when `actiontype` is absent) it joins all child
 * conversions with `\Longrightarrow`; otherwise it converts only the first child.
 *
 * @example
 * // <maction actiontype="toggle"><mi>a</mi><mi>b</mi></maction> -> a \Longrightarrow b
 */
class MAction {
    constructor(mathElement) {
        this._mathmlElement = mathElement;
    }
    /**
     * @returns the LaTeX representation of this element.
     */
    convert() {
        const { children } = this._mathmlElement;
        if (this._isToggle())
            return children
                .map((child) => mathMLElementToLaTeXConverter(child))
                .map((converter) => converter.convert())
                .join(' \\Longrightarrow ');
        return mathMLElementToLaTeXConverter(children[0]).convert();
    }
    /** True when the action is a toggle, i.e. `actiontype` is 'toggle' or unset. */
    _isToggle() {
        const { actiontype } = this._mathmlElement.attributes;
        return actiontype === 'toggle' || !actiontype;
    }
}

/**
 * Converts a MathML `<menclose>` element into LaTeX.
 *
 * Recursively converts its children and wraps them in the LaTeX construct that
 * matches the `notation` attribute (e.g. radical, box, strike), defaulting to a
 * long-division style overline when `notation` is absent or unrecognized.
 *
 * @example
 * // <menclose notation="radical"><mi>x</mi></menclose> -> \sqrt{x}
 */
class MEnclose {
    constructor(mathElement) {
        this._mathmlElement = mathElement;
    }
    /**
     * @returns the LaTeX representation of this element.
     */
    convert() {
        const latexJoinedChildren = this._mathmlElement.children
            .map((child) => mathMLElementToLaTeXConverter(child))
            .map((converter) => converter.convert())
            .join(' ');
        if (this._notation === 'actuarial')
            return `\\overline{\\left.${latexJoinedChildren}\\right|}`;
        if (this._notation === 'radical')
            return `\\sqrt{${latexJoinedChildren}}`;
        if (['box', 'roundedbox', 'circle'].includes(this._notation))
            return `\\boxed{${latexJoinedChildren}}`;
        if (this._notation === 'left')
            return `\\left|${latexJoinedChildren}`;
        if (this._notation === 'right')
            return `${latexJoinedChildren}\\right|`;
        if (this._notation === 'top')
            return `\\overline{${latexJoinedChildren}}`;
        if (this._notation === 'bottom')
            return `\\underline{${latexJoinedChildren}}`;
        if (this._notation === 'updiagonalstrike')
            return `\\cancel{${latexJoinedChildren}}`;
        if (this._notation === 'downdiagonalstrike')
            return `\\bcancel{${latexJoinedChildren}}`;
        if (this._notation === 'updiagonalarrow')
            return `\\cancelto{}{${latexJoinedChildren}}`;
        if (['verticalstrike', 'horizontalstrike'].includes(this._notation))
            return `\\hcancel{${latexJoinedChildren}}`;
        if (this._notation === 'madruwb')
            return `\\underline{${latexJoinedChildren}\\right|}`;
        if (this._notation === 'phasorangle')
            return `{\\angle \\underline{${latexJoinedChildren}}}`;
        return `\\overline{\\left.\\right)${latexJoinedChildren}}`;
    }
    /** Returns the `notation` attribute, defaulting to 'longdiv'. */
    get _notation() {
        return this._mathmlElement.attributes.notation || 'longdiv';
    }
}

/**
 * Converts a MathML `<merror>` element into LaTeX.
 *
 * Recursively converts its children, joins them with spaces, and wraps the
 * result in a red `\color` group.
 *
 * @example
 * // <merror><mi>x</mi></merror> -> \color{red}{x}
 */
class MError {
    constructor(mathElement) {
        this._mathmlElement = mathElement;
    }
    /**
     * @returns the LaTeX representation of this element.
     */
    convert() {
        const latexJoinedChildren = this._mathmlElement.children
            .map((child) => mathMLElementToLaTeXConverter(child))
            .map((converter) => converter.convert())
            .join(' ');
        return `\\color{red}{${latexJoinedChildren}}`;
    }
}

/**
 * Converts a MathML `<mphantom>` element into LaTeX.
 *
 * The element reserves layout space without rendering content, so it produces
 * an empty string.
 *
 * @example
 * // <mphantom>x</mphantom> -> ''
 */
class MPhantom {
    constructor(mathElement) {
        this._mathmlElement = mathElement;
    }
    /**
     * @returns the LaTeX representation of this element.
     */
    convert() {
        return '';
    }
}

/**
 * Converts a MathML `<msup>` element into a LaTeX superscript.
 *
 * Produces `base^{exponent}`. The base is parenthesized when it has more than
 * one child (and is longer than one character); the exponent is always wrapped
 * in braces.
 *
 * @example
 * // <msup><mi>x</mi><mn>2</mn></msup> -> x^{2}
 */
class MSup {
    constructor(mathElement) {
        this._mathmlElement = mathElement;
    }
    /**
     * @returns the LaTeX representation of this element.
     * @throws {InvalidNumberOfChildrenError} if the element does not have exactly 2 children.
     */
    convert() {
        const { name, children } = this._mathmlElement;
        const childrenLength = children.length;
        if (childrenLength !== 2)
            throw new InvalidNumberOfChildrenError(name, 2, childrenLength);
        const baseChild = children[0];
        const exponentChild = children[1];
        return `${this._handleBaseChild(baseChild)}^${this._handleExponentChild(exponentChild)}`;
    }
    /** Converts the base, parenthesizing it when it groups more than one child. */
    _handleBaseChild(base) {
        const baseChildren = base.children;
        const baseStr = mathMLElementToLaTeXConverter(base).convert();
        if (baseChildren.length <= 1) {
            return baseStr;
        }
        return new ParenthesisWrapper().wrapIfMoreThanOneChar(baseStr);
    }
    /** Converts the exponent and wraps it in braces. */
    _handleExponentChild(exponent) {
        const exponentStr = mathMLElementToLaTeXConverter(exponent).convert();
        return new BracketWrapper().wrap(exponentStr);
    }
}

/**
 * Converts a MathML `<msub>` element into a LaTeX subscript.
 *
 * Produces `base_{subscript}`. The base is parenthesized when it has more than
 * one child (and is longer than one character); the subscript is always wrapped
 * in braces.
 *
 * @example
 * // <msub><mi>x</mi><mn>1</mn></msub> -> x_{1}
 */
class MSub {
    constructor(mathElement) {
        this._mathmlElement = mathElement;
    }
    /**
     * @returns the LaTeX representation of this element.
     * @throws {InvalidNumberOfChildrenError} if the element does not have exactly 2 children.
     */
    convert() {
        const { name, children } = this._mathmlElement;
        const childrenLength = children.length;
        if (childrenLength !== 2)
            throw new InvalidNumberOfChildrenError(name, 2, childrenLength);
        const baseChild = children[0];
        const subscriptChild = children[1];
        return `${this._handleBaseChild(baseChild)}_${this._handleSubscriptChild(subscriptChild)}`;
    }
    /** Converts the base, parenthesizing it when it groups more than one child. */
    _handleBaseChild(base) {
        const baseChildren = base.children;
        const baseStr = mathMLElementToLaTeXConverter(base).convert();
        if (baseChildren.length <= 1) {
            return baseStr;
        }
        return new ParenthesisWrapper().wrapIfMoreThanOneChar(baseStr);
    }
    /** Converts the subscript and wraps it in braces. */
    _handleSubscriptChild(subscript) {
        const subscriptStr = mathMLElementToLaTeXConverter(subscript).convert();
        return new BracketWrapper().wrap(subscriptStr);
    }
}

/**
 * Converts a MathML `<msubsup>` element into a combined LaTeX subscript and superscript.
 *
 * Produces `base_{subscript}^{superscript}`. The base is parenthesized when it
 * has more than one child (and is longer than one character); both the subscript
 * and superscript are always wrapped in braces.
 *
 * @example
 * // <msubsup><mi>x</mi><mn>1</mn><mn>2</mn></msubsup> -> x_{1}^{2}
 */
class MSubsup {
    constructor(mathElement) {
        this._mathmlElement = mathElement;
    }
    /**
     * @returns the LaTeX representation of this element.
     * @throws {InvalidNumberOfChildrenError} if the element does not have exactly 3 children.
     */
    convert() {
        const { name, children } = this._mathmlElement;
        const childrenLength = children.length;
        if (childrenLength !== 3)
            throw new InvalidNumberOfChildrenError(name, 3, childrenLength);
        const baseChild = children[0];
        const subscriptChild = children[1];
        const superscriptChild = children[2];
        return `${this._handleBaseChild(baseChild)}_${this._handleSubscriptChild(subscriptChild)}^${this._handleSuperscriptChild(superscriptChild)}`;
    }
    /** Converts the base, parenthesizing it when it groups more than one child. */
    _handleBaseChild(base) {
        const baseChildren = base.children;
        const baseStr = mathMLElementToLaTeXConverter(base).convert();
        if (baseChildren.length <= 1) {
            return baseStr;
        }
        return new ParenthesisWrapper().wrapIfMoreThanOneChar(baseStr);
    }
    /** Converts the subscript and wraps it in braces. */
    _handleSubscriptChild(subscript) {
        const subscriptStr = mathMLElementToLaTeXConverter(subscript).convert();
        return new BracketWrapper().wrap(subscriptStr);
    }
    /** Converts the superscript and wraps it in braces. */
    _handleSuperscriptChild(superscript) {
        const superscriptStr = mathMLElementToLaTeXConverter(superscript).convert();
        return new BracketWrapper().wrap(superscriptStr);
    }
}

/**
 * Converts a MathML `<mmultiscripts>` element into LaTeX.
 *
 * Parses the structure the spec defines, `base (sub sup)* [mprescripts
 * (presub presup)*]`, accepting the `<mprescripts/>` marker at any position:
 * every prescript pair is emitted as `{}_{sub}^{sup}` before the base and
 * every postscript pair after it, with `{}` atoms separating consecutive
 * pairs so multiple pairs never produce a LaTeX double-subscript error.
 * `<none/>` placeholders and missing trailing scripts are omitted instead of
 * emitting empty `_{}`/`^{}`.
 *
 * @example
 * // <mmultiscripts><mi>X</mi><mprescripts/><mn>1</mn><mn>2</mn></mmultiscripts> -> {}_{1}^{2}X
 * @example
 * // <mmultiscripts><mi>F</mi><mn>1</mn><mn>2</mn><mn>3</mn><mn>4</mn></mmultiscripts> -> F_{1}^{2}{}_{3}^{4}
 */
class MMultiscripts {
    constructor(mathElement) {
        this._mathmlElement = mathElement;
    }
    /**
     * @returns the LaTeX representation of this element.
     * @throws {InvalidNumberOfChildrenError} when the base child is missing.
     */
    convert() {
        const { name, children } = this._mathmlElement;
        if (children.length === 0)
            throw new InvalidNumberOfChildrenError(name, 1, 0, 'at least');
        const base = this._baseLatex(children[0]);
        const { prescripts, postscripts } = this._splitScripts(children.slice(1));
        return this._pairsLatex(prescripts, true) + base + this._pairsLatex(postscripts, false);
    }
    /**
     * Converts the base, grouping a multi-token result with braces so the
     * scripts attach to all of it (and never collide with the base's own
     * scripts, e.g. a `msub` base). An empty base becomes an empty atom so the
     * surrounding scripts still have something valid to attach to.
     */
    _baseLatex(base) {
        const latex = mathMLElementToLaTeXConverter(base).convert();
        if (latex === '')
            return '{}';
        return latex.length === 1 ? latex : `{${latex}}`;
    }
    /** Splits the script children at the `<mprescripts/>` marker, wherever it is. */
    _splitScripts(scripts) {
        const markerIndex = scripts.findIndex((child) => child.name === 'mprescripts');
        const postscripts = markerIndex === -1 ? scripts : scripts.slice(0, markerIndex);
        const prescripts = markerIndex === -1 ? [] : scripts.slice(markerIndex + 1);
        return { prescripts: this._toPairs(prescripts), postscripts: this._toPairs(postscripts) };
    }
    _toPairs(elements) {
        const pairs = [];
        for (let i = 0; i < elements.length; i += 2) {
            pairs.push({ sub: this._scriptLatex(elements[i]), sup: this._scriptLatex(elements[i + 1]) });
        }
        return pairs;
    }
    /** `<none/>` placeholders and missing trailing scripts become empty scripts. */
    _scriptLatex(element) {
        if (element === undefined || element.name === 'none')
            return '';
        return mathMLElementToLaTeXConverter(element).convert();
    }
    /**
     * Renders the pairs. Prescript pairs always hang off their own empty `{}`
     * atom; the first postscript pair attaches straight to the base and every
     * following pair gets a `{}` atom, which is what prevents double-subscript
     * errors. Fully empty pairs have no LaTeX representation and are skipped.
     */
    _pairsLatex(pairs, isPrescript) {
        let latex = '';
        let attachesToBase = !isPrescript;
        for (const pair of pairs) {
            const scripts = (pair.sub ? `_{${pair.sub}}` : '') + (pair.sup ? `^{${pair.sup}}` : '');
            if (scripts === '')
                continue;
            latex += (attachesToBase ? '' : '{}') + scripts;
            attachesToBase = false;
        }
        return latex;
    }
}

/** Wraps a text value in the LaTeX text/font command(s) matching the given `mathvariant`. */
class TextCommand {
    constructor(mathvariant) {
        this._mathvariant = mathvariant || 'normal';
    }
    apply(value) {
        return this._commands.reduce((acc, command, index) => {
            if (index === 0)
                return `${command}{${value}}`;
            return `${command}{${acc}}`;
        }, '');
    }
    get _commands() {
        switch (this._mathvariant) {
            case 'bold':
                return ['\\textbf'];
            case 'italic':
                return ['\\textit'];
            case 'bold-italic':
                return ['\\textit', '\\textbf'];
            case 'double-struck':
                return ['\\mathbb'];
            case 'monospace':
                return ['\\mathtt'];
            case 'bold-fraktur':
            case 'fraktur':
                return ['\\mathfrak'];
            default:
                return ['\\text'];
        }
    }
}

/**
 * Converts a MathML `<mtext>` element into LaTeX.
 *
 * Splits the text into runs of alphanumeric/space characters and standalone
 * symbols: alphanumeric runs are wrapped by the `mathvariant` text command while
 * each symbol is delegated to the `<mi>` converter.
 *
 * @example
 * // <mtext mathvariant="bold">hi</mtext> -> \textbf{hi}
 */
class MText {
    constructor(mathElement) {
        this._mathmlElement = mathElement;
    }
    /**
     * @returns the LaTeX representation of this element.
     */
    convert() {
        const { attributes, value } = this._mathmlElement;
        return [...value]
            .map((char) => {
            // if is a letter, number or space, return it
            if (/^[a-zA-Z0-9]$/.test(char) || char === ' ')
                return {
                    value: char,
                    isAlphanumeric: true,
                };
            // if is a symbol, set it to mi parser
            return {
                value: char,
                isAlphanumeric: false,
            };
        })
            .reduce((acc, char) => {
            // merge consecutive alphanumeric characters
            if (char.isAlphanumeric) {
                const lastChar = acc[acc.length - 1];
                if (lastChar && lastChar.isAlphanumeric) {
                    lastChar.value += char.value;
                    return acc;
                }
            }
            return [...acc, char];
        }, [])
            .map((char) => {
            if (!char.isAlphanumeric) {
                return new MI({
                    name: 'mi',
                    attributes: {},
                    children: [],
                    value: char.value,
                }).convert();
            }
            return new TextCommand(attributes.mathvariant).apply(char.value);
        })
            .join('');
    }
}

/**
 * Converts a MathML `<munderover>` element into LaTeX under/over scripts.
 *
 * Converts the three children as base, under-content and over-content,
 * producing `base_{under}^{over}`.
 *
 * @example
 * // <munderover><mo>∑</mo><mi>i</mi><mi>n</mi></munderover> -> \sum_{i}^{n}
 */
class MUnderover {
    constructor(mathElement) {
        this._mathmlElement = mathElement;
    }
    /**
     * @returns the LaTeX representation of this element.
     * @throws {InvalidNumberOfChildrenError} if the element does not have exactly 3 children.
     */
    convert() {
        const { name, children } = this._mathmlElement;
        const childrenLength = children.length;
        if (childrenLength !== 3)
            throw new InvalidNumberOfChildrenError(name, 3, childrenLength);
        const base = mathMLElementToLaTeXConverter(children[0]).convert();
        const underContent = mathMLElementToLaTeXConverter(children[1]).convert();
        const overContent = mathMLElementToLaTeXConverter(children[2]).convert();
        return `${base}_{${underContent}}^{${overContent}}`;
    }
}

/**
 * Detects whether a `<mtable>` declares multiline equation alignment, the
 * shape LaTeX-to-MathML emitters (MathJax, LaTeXML) produce for `align`: a
 * `columnalign` declaration, on the table or spread over its rows/cells,
 * mixing `right` and `left` columns.
 *
 * The decision deliberately reads formatting metadata only, never cell
 * content: per the MathML spec a table without `columnalign` defaults to
 * centered columns, so inferring alignment from what the cells contain would
 * contradict the declared presentation.
 *
 * Used by the `<mtable>` converter to pick `aligned` over `matrix` when it has
 * to wrap a bare table in an environment.
 */
class EquationAlignment {
    constructor(table) {
        this._table = table;
    }
    /** @returns true when the table declares mixed right/left column alignment. */
    static matches(table) {
        return new EquationAlignment(table)._matches();
    }
    _matches() {
        const tokens = new Set(this._columnAlignTokens());
        return tokens.has('right') && tokens.has('left');
    }
    _columnAlignTokens() {
        const rows = this._rows();
        const cells = rows.reduce((acc, row) => acc.concat(this._cells(row)), []);
        return [this._table, ...rows, ...cells]
            .map((element) => element.attributes.columnalign)
            .filter((value) => !!value)
            .reduce((acc, value) => acc.concat(value.trim().split(/\s+/)), []);
    }
    _rows() {
        return this._table.children.filter((child) => child.name === 'mtr');
    }
    _cells(row) {
        return row.children.filter((child) => child.name === 'mtd');
    }
}

/**
 * Converts a MathML `<mtable>` element into LaTeX matrix rows.
 *
 * Each row child is converted and the results are joined with `\\` row
 * separators. Flags set by the tree pre-pass control the wrapper: `innerTable`
 * (table nested in another table) wraps the rows in a `matrix` environment, and
 * `bareTable` (table that no other converter wraps) wraps them in `matrix`, or
 * in `aligned` when {@link EquationAlignment} recognizes the input tree as
 * multiline equation alignment, so `&`/`\\` never end up outside an environment.
 *
 * @example
 * // <mtable><mtr><mtd><mn>1</mn></mtd></mtr></mtable> -> \begin{matrix}1\end{matrix}
 */
class MTable {
    constructor(mathElement) {
        this._mathmlElement = mathElement;
    }
    /**
     * @returns the LaTeX representation of this element.
     */
    convert() {
        const tableContent = this._mathmlElement.children
            .map((child) => mathMLElementToLaTeXConverter(child))
            .map((converter) => converter.convert())
            .join(' \\\\\n');
        // Both flags are set by the iterative pre-pass in tree-to-latex.
        if (this._hasFlag('innerTable'))
            return this._wrap(tableContent, 'matrix');
        if (this._hasFlag('bareTable')) {
            return this._wrap(tableContent, EquationAlignment.matches(this._mathmlElement) ? 'aligned' : 'matrix');
        }
        return tableContent;
    }
    /** Wraps the rows in the given LaTeX environment. */
    _wrap(latex, environment) {
        return `\\begin{${environment}}${latex}\\end{${environment}}`;
    }
    /** Checks whether the given boolean flag attribute is present and truthy. */
    _hasFlag(flag) {
        return !!this._mathmlElement.attributes[flag];
    }
}

/**
 * Converts a MathML `<mtr>` (table row) element into LaTeX.
 *
 * Each cell child is converted and the results are joined with the `&` column
 * separator used inside LaTeX matrix environments. When the row carries the
 * `bareRow` flag (set by the tree pre-pass for rows with no `<mtable>`
 * ancestor) it wraps itself in a `matrix` environment, so the `&` separators
 * never end up outside an environment.
 *
 * @example
 * // <mtr><mtd><mn>1</mn></mtd><mtd><mn>2</mn></mtd></mtr> -> 1 & 2
 */
class MTr {
    constructor(mathElement) {
        this._mathmlElement = mathElement;
    }
    /**
     * @returns the LaTeX representation of this element.
     */
    convert() {
        const row = this._mathmlElement.children
            .map((child) => mathMLElementToLaTeXConverter(child))
            .map((converter) => converter.convert())
            .join(' & ');
        // The `bareRow` flag is set by the iterative pre-pass in tree-to-latex.
        return this._mathmlElement.attributes['bareRow'] ? `\\begin{matrix}${row}\\end{matrix}` : row;
    }
}

/**
 * Fallback converter that recursively converts every child and joins the
 * results with single spaces.
 *
 * Used for container-like MathML elements that have no special LaTeX rendering
 * of their own and simply pass their children through.
 *
 * @example
 * // <mtd><mi>a</mi><mo>+</mo><mi>b</mi></mtd> -> a + b
 */
class GenericSpacingWrapper {
    constructor(mathElement) {
        this._mathmlElement = mathElement;
    }
    /**
     * @returns the LaTeX representation of this element.
     */
    convert() {
        return this._mathmlElement.children
            .map((child) => mathMLElementToLaTeXConverter(child))
            .map((converter) => converter.convert())
            .join(' ');
    }
}

/** Applies the accent to the content as a LaTeX accent command, or wraps it with `\underset`/`\overset`. */
class UnderOverSetter {
    constructor(type) {
        this._type = type;
    }
    apply(content, accent) {
        return latexAccents.includes(accent) ? `${accent}{${content}}` : `${this._defaultCommand}{${accent}}{${content}}`;
    }
    get _defaultCommand() {
        if (this._type === TagTypes.Under)
            return '\\underset';
        return '\\overset';
    }
}
var TagTypes;
(function (TagTypes) {
    TagTypes[TagTypes["Under"] = 0] = "Under";
    TagTypes[TagTypes["Over"] = 1] = "Over";
})(TagTypes || (TagTypes = {}));

/**
 * Converts a MathML `<mover>` or `<munder>` element into LaTeX.
 *
 * Converts the base and the accent, then either applies the accent as a LaTeX
 * accent command (when it is a known accent) or wraps it with `\overset`/
 * `\underset` depending on whether the element name contains `under`.
 *
 * @example
 * // <munder><mi>x</mi><mo>+</mo></munder> -> \underset{+}{x}
 */
class GenericUnderOver {
    constructor(mathElement) {
        this._mathmlElement = mathElement;
    }
    /**
     * @returns the LaTeX representation of this element.
     * @throws {InvalidNumberOfChildrenError} when the element does not have exactly 2 children.
     */
    convert() {
        const { name, children } = this._mathmlElement;
        const childrenLength = children.length;
        if (childrenLength !== 2)
            throw new InvalidNumberOfChildrenError(name, 2, childrenLength);
        const content = mathMLElementToLaTeXConverter(children[0]).convert();
        const accent = mathMLElementToLaTeXConverter(children[1]).convert();
        return this._applyCommand(content, accent);
    }
    /** Chooses the under/over command based on the element name and applies it to the content and accent. */
    _applyCommand(content, accent) {
        const type = this._mathmlElement.name.match(/under/) ? TagTypes.Under : TagTypes.Over;
        return new UnderOverSetter(type).apply(content, accent);
    }
}

/**
 * Detects the linear-system/piecewise shape emitted by editors and MathJax:
 * an `<mrow>` holding an opening `{` operator and an `<mtable>`, optionally
 * followed by an empty closing `<mo>`. The `<mrow>` converter renders this
 * shape as a LaTeX `cases` environment, so the inner table must not receive
 * another environment wrapper of its own.
 */
class LinearSystemPattern {
    constructor(element) {
        this._element = element;
    }
    /** @returns true when the element matches the pattern. */
    static matches(element) {
        return new LinearSystemPattern(element)._matches();
    }
    _matches() {
        var _a;
        if (this._element.name !== 'mrow')
            return false;
        const children = (_a = this._element.children) !== null && _a !== void 0 ? _a : [];
        if (children.length !== 2 && children.length !== 3)
            return false;
        return this._isOpeningBrace(children[0]) && this._isTable(children[1]) && this._isEmptyClosing(children[2]);
    }
    _isOpeningBrace(child) {
        return child.name === 'mo' && child.value.trim() === '{';
    }
    _isTable(child) {
        return child.name === 'mtable';
    }
    _isEmptyClosing(child) {
        return child === undefined || (child.name === 'mo' && child.value.trim() === '');
    }
}

/**
 * Converts a MathML `<mrow>` grouping element into LaTeX.
 *
 * By default it recursively converts every child and joins the results with
 * spaces. As a special case, when the row matches the linear-system pattern
 * (an opening `{` operator and an `<mtable>`, optionally followed by an empty
 * closing operator) it is rendered as a LaTeX `cases` environment.
 *
 * @example
 * // <mrow><mi>a</mi><mo>+</mo><mi>b</mi></mrow> -> a + b
 * @example
 * // <mrow><mo>{</mo><mtable>...</mtable><mo></mo></mrow> -> \begin{cases} ... \end{cases}
 */
class MRow {
    constructor(mathElement) {
        this._mathmlElement = mathElement;
    }
    /**
     * @returns the LaTeX representation of this element.
     */
    convert() {
        // Linear system pattern: { + mtable, optionally closed by an empty operator
        if (LinearSystemPattern.matches(this._mathmlElement)) {
            return this._convertAsLinearSystem();
        }
        return this._mathmlElement.children
            .map((child) => mathMLElementToLaTeXConverter(child))
            .map((converter) => converter.convert())
            .join(' ');
    }
    /** Renders the inner `<mtable>` rows as a LaTeX `cases` environment. */
    _convertAsLinearSystem() {
        const mtableChild = this._mathmlElement.children[1];
        const tableContent = mtableChild.children
            .map((child) => mathMLElementToLaTeXConverter(child))
            .map((converter) => converter.convert())
            .join(' \\\\ ');
        return `\\begin{cases} ${tableContent} \\end{cases}`;
    }
}

/**
 * Converts a MathML `<mspace>` element into LaTeX.
 *
 * Emits a LaTeX line break when `linebreak="newline"`, otherwise a single space.
 *
 * @example
 * // <mspace linebreak="newline" /> ->  \\
 */
class MSpace {
    constructor(mathElement) {
        this._mathmlElement = mathElement;
    }
    /**
     * @returns the LaTeX representation of this element.
     */
    convert() {
        const { linebreak } = this._mathmlElement.attributes;
        if (linebreak === 'newline') {
            return ' \\\\ ';
        }
        return ' ';
    }
}

/**
 * No-op converter for MathML elements that have no LaTeX representation.
 *
 * Always produces an empty string, used as a fallback/placeholder converter.
 *
 * @example
 * // any element -> ''
 */
class Void {
    constructor(_mathmlElement) {
        this._mathmlElement = _mathmlElement;
    }
    /**
     * @returns the LaTeX representation of this element.
     */
    convert() {
        return '';
    }
}

/**
 * Null-object {@link MathMLElement} used as a safe fallback when no element is
 * available, so converters never have to guard against `null`/`undefined`.
 */
class VoidMathMLElement {
    constructor() {
        this.name = 'void';
        this.value = '';
        this.children = [];
        this.attributes = {};
    }
}

/**
 * Maps a {@link MathMLElement} to the concrete {@link ToLaTeXConverter} that
 * knows how to render it, based on the element's tag name. Unknown tags fall
 * back to the generic spacing wrapper that simply joins the children.
 */
class MathMLElementToLatexConverterAdapter {
    constructor(mathMLElement) {
        this._mathMLElement = mathMLElement !== null && mathMLElement !== void 0 ? mathMLElement : new VoidMathMLElement();
    }
    /** @returns a converter instance bound to the wrapped element. */
    toLatexConverter() {
        const { name } = this._mathMLElement;
        const Converter = fromMathMLElementToLatexConverter[name] || GenericSpacingWrapper;
        return new Converter(this._mathMLElement);
    }
}
/** Registry mapping each supported MathML tag name to its converter class. */
const fromMathMLElementToLatexConverter = {
    math: Math$1,
    mi: MI,
    mo: MO,
    mn: MN,
    msqrt: MSqrt,
    mfenced: MFenced,
    mfrac: MFrac,
    mroot: MRoot,
    maction: MAction,
    menclose: MEnclose,
    merror: MError,
    mphantom: MPhantom,
    msup: MSup,
    msub: MSub,
    msubsup: MSubsup,
    mmultiscripts: MMultiscripts,
    mtext: MText,
    munderover: MUnderover,
    mtable: MTable,
    mtr: MTr,
    mover: GenericUnderOver,
    munder: GenericUnderOver,
    mrow: MRow,
    mspace: MSpace,
    mpadded: GenericSpacingWrapper,
    void: Void,
};

/**
 * Normalizes bare fence operators into `<mfenced>` elements, in place.
 *
 * MathML often delimits content with standalone `<mo>` operators (`<mo>(</mo>
 * ... <mo>)</mo>`). Converted in isolation, each becomes a self-balanced
 * `\left(\right.` / `\left.\right)` (a lone `\left(` is invalid LaTeX), which
 * leaves dangling `\right.`/`\left.` fillers in the output. This pass pairs the
 * fence operators that are real siblings and rewrites each pair into an
 * `<mfenced>`, so the existing converter produces a single stretchy
 * `\left( ... \right)` pair, and a parenthesized `<mtable>` naturally becomes a
 * `pmatrix`.
 *
 * Pairing is positional (a closer matches the nearest open fence) and may mix
 * types (`(` with `]`), which `<mfenced>` renders as valid asymmetric
 * delimiters. Parentheses, square brackets and braces are directional. Vertical
 * bars (`|`) and double bars (`||`) share one glyph for both sides, so they
 * toggle: a bar opens a pair unless a same-glyph bar is already open at the
 * current level, in which case it closes it (so `|x|` becomes `\left|x\right|`
 * and `||x||` becomes the norm `\left\|x\right\|`). Any fence that does not find
 * a match is left untouched as a valid self-balanced delimiter.
 */
class FenceNormalizer {
    constructor(root) {
        this._root = root;
    }
    /** Rewrites the subtree in place, pairing sibling fence operators into `<mfenced>`. */
    normalize() {
        const stack = [this._root];
        while (stack.length > 0) {
            const node = stack.pop();
            const children = node.children;
            if (!children || children.length === 0)
                continue;
            const paired = new FencePairing(children).pair();
            if (paired !== children)
                children.splice(0, children.length, ...paired);
            for (const child of children)
                stack.push(child);
        }
    }
}
/** Pairs the fence operators within a single list of sibling nodes. */
class FencePairing {
    constructor(children) {
        this._children = children;
    }
    /**
     * @returns a rewritten list with each matched pair nested into an `<mfenced>`,
     * or the original list (same reference) when nothing pairs.
     *
     * Single left-to-right pass: an opener starts a frame that collects its
     * content, a closer turns the innermost open frame into an `<mfenced>`, and any
     * frame still open at the end (an unmatched opener) is spilled back so it stays
     * a plain self-balanced delimiter. No recursion and no per-pop copying, so it
     * stays `O(n)` even for deeply nested or unbalanced input.
     */
    pair() {
        const root = [];
        const openFrames = [];
        let current = root;
        let paired = false;
        for (const child of this._children) {
            const top = openFrames[openFrames.length - 1];
            if (this._opensFrame(child, top)) {
                const frame = { opener: child, content: [] };
                openFrames.push(frame);
                current = frame.content;
                continue;
            }
            if (this._closesFrame(child, top)) {
                const frame = openFrames.pop();
                current = openFrames.length > 0 ? openFrames[openFrames.length - 1].content : root;
                current.push(this._makeFence(frame.opener, child, frame.content));
                paired = true;
                continue;
            }
            const scriptCloser = this._scriptBaseCloser(child, top);
            if (scriptCloser !== undefined) {
                const frame = openFrames.pop();
                current = openFrames.length > 0 ? openFrames[openFrames.length - 1].content : root;
                const fence = this._makeFence(frame.opener, scriptCloser, frame.content);
                current.push(Object.assign(Object.assign({}, child), { children: [fence, ...child.children.slice(1)] }));
                paired = true;
                continue;
            }
            current.push(child);
        }
        if (!paired)
            return this._children;
        for (const frame of openFrames)
            this._spill(frame, root);
        return root;
    }
    /** Spills an unmatched opener and its collected content back into the result, keeping source order. */
    _spill(frame, target) {
        target.push(frame.opener);
        for (const node of frame.content)
            target.push(node);
    }
    /** Builds an `<mfenced>` around the paired content, passing a lone child directly to preserve matrix mode. */
    _makeFence(opener, closer, content) {
        return {
            name: 'mfenced',
            value: '',
            children: [content.length === 1 ? content[0] : this._wrapInRow(content)],
            attributes: { open: opener.value.trim(), close: closer.value.trim() },
        };
    }
    /** Wraps several nodes in a single `<mrow>` so `<mfenced>` does not insert separators between them. */
    _wrapInRow(children) {
        return { name: 'mrow', value: '', children, attributes: {} };
    }
    /**
     * Detects the closing bar of a scripted norm, the shape editors emit for
     * `‖P‖_F^2`: a script element whose base holds only the bar that matches the
     * open bar frame (`<mo>∥</mo><mi>P</mi><msubsup><mrow><mo>∥</mo></mrow>F 2`,
     * issue #43). Without this, the nested closer is invisible to the sibling
     * pass and the opener would pair with the *next* norm's opener, fencing the
     * wrong span. The frame content becomes the fence and the script attaches to
     * it: `msubsup(mfenced(P), F, 2)`.
     *
     * @returns the matching bar, or undefined when the child is not this shape.
     */
    _scriptBaseCloser(child, top) {
        if (top === undefined || !this._isBar(top.opener))
            return undefined;
        if (!SCRIPT_ELEMENTS.has(child.name) || child.children.length === 0)
            return undefined;
        const base = child.children[0];
        const bar = base.name === 'mrow' && base.children.length === 1 ? base.children[0] : base;
        if (bar.name !== 'mo' || bar.value.trim() !== top.opener.value.trim())
            return undefined;
        return bar;
    }
    /** A directional opener, or a vertical bar that does not close the current frame. */
    _opensFrame(child, top) {
        if (this._isOpener(child))
            return true;
        return this._isBar(child) && !this._closesBar(child, top);
    }
    /** A directional closer matching a directional frame, or a bar closing a same-glyph bar frame. */
    _closesFrame(child, top) {
        if (top === undefined)
            return false;
        if (this._isCloser(child) && this._isOpener(top.opener))
            return true;
        return this._closesBar(child, top);
    }
    /** Whether `child` is a bar that closes `top`, i.e. `top` is a bar frame opened by the same glyph. */
    _closesBar(child, top) {
        if (top === undefined || !this._isBar(child) || !this._isBar(top.opener))
            return false;
        return top.opener.value.trim() === child.value.trim();
    }
    _isOpener(element) {
        return element.name === 'mo' && OPENERS.has(element.value.trim());
    }
    _isCloser(element) {
        return element.name === 'mo' && CLOSERS.has(element.value.trim());
    }
    _isBar(element) {
        return element.name === 'mo' && BARS.has(element.value.trim());
    }
}
const OPENERS = new Set(['(', '[', '{']);
const CLOSERS = new Set([')', ']', '}']);
// Single bar plus the double-bar family: bars toggle, so a sibling pair is
// read as an absolute value or norm (issue #43), while an odd leftover spills
// back and keeps its standalone meaning (`\mid`, `\parallel`).
const BARS = new Set(['|', ...doubleBarFenceGlyphs]);
/** Script elements whose base can hide the closing bar of a norm. */
const SCRIPT_ELEMENTS = new Set(['msub', 'msup', 'msubsup']);

/**
 * Pre-pass that flags tables and rows for their converters, in a single
 * iterative top-down walk (so it cannot overflow the call stack):
 *
 * - `innerTable`: every mtable nested inside another mtable;
 * - `bareTable`: every mtable that no other converter wraps in an environment
 *   (not inside an mfenced, not the table of a linear-system row);
 * - `bareRow`: every mtr with no mtable ancestor.
 *
 * The flagged converters then emit a valid environment themselves, keeping the
 * invariant that `&`/`\\` alignment markers never appear outside one.
 */
class TableAnnotator {
    constructor(root) {
        this._root = root;
    }
    /** Walks the tree and stamps the wrapper flags in place. */
    normalize() {
        const stack = [{ node: this._root, insideMtable: false, wrapped: false }];
        while (stack.length > 0) {
            const frame = stack.pop();
            this._annotate(frame);
            this._pushChildren(frame, stack);
        }
    }
    _annotate({ node, insideMtable, wrapped }) {
        if (node.name === 'mtable') {
            if (insideMtable)
                node.attributes['innerTable'] = 'innerTable';
            else if (!wrapped)
                node.attributes['bareTable'] = 'bareTable';
        }
        if (node.name === 'mtr' && !insideMtable)
            node.attributes['bareRow'] = 'bareRow';
    }
    _pushChildren({ node, insideMtable, wrapped }, stack) {
        var _a;
        const childInsideMtable = insideMtable || node.name === 'mtable';
        const childWrapped = wrapped || node.name === 'mfenced';
        const linearSystemTable = LinearSystemPattern.matches(node) ? node.children[1] : undefined;
        const children = (_a = node.children) !== null && _a !== void 0 ? _a : [];
        for (let i = 0; i < children.length; i++) {
            stack.push({
                node: children[i],
                insideMtable: childInsideMtable,
                wrapped: childWrapped || children[i] === linearSystemTable,
            });
        }
    }
}

/**
 * Converts a MathML element tree to LaTeX iteratively, using an explicit stack
 * instead of recursion so arbitrarily deep nesting cannot overflow the call
 * stack. Nodes are evaluated post-order (children before parents) and each
 * result is memoized; converters then read their children's precomputed strings
 * through {@link mathMLElementToLaTeXConverter} instead of recursing.
 *
 * Before the conversion loop, every {@link TreeNormalizer} in the pipeline runs
 * over the tree in order. New pre-passes are added to {@link makeTreeNormalizers}
 * without modifying the loop or the existing passes.
 *
 * @param root - the root element of the tree to convert.
 * @returns the LaTeX string for the whole tree.
 */
const convertTreeToLatex = (root) => {
    var _a, _b;
    for (const normalizer of makeTreeNormalizers(root))
        normalizer.normalize();
    const memo = new Map();
    const stack = [{ node: root, visited: false }];
    const previousMemo = setConversionMemo(memo);
    try {
        while (stack.length > 0) {
            const frame = stack[stack.length - 1];
            if (!frame.visited) {
                frame.visited = true;
                const children = (_a = frame.node.children) !== null && _a !== void 0 ? _a : [];
                for (let i = children.length - 1; i >= 0; i--)
                    stack.push({ node: children[i], visited: false });
                continue;
            }
            stack.pop();
            memo.set(frame.node, new MathMLElementToLatexConverterAdapter(frame.node).toLatexConverter().convert());
        }
    }
    finally {
        setConversionMemo(previousMemo);
    }
    return (_b = memo.get(root)) !== null && _b !== void 0 ? _b : '';
};
/** The normalization pipeline, in execution order. */
const makeTreeNormalizers = (root) => [
    new FenceNormalizer(root),
    new TableAnnotator(root),
];

/**
 * Parses a MathML/XML string into an array of internal {@link MathMLElement}
 * trees (one per `<math>` element).
 *
 * It cleans up MS Word artefacts (line breaks, `mml:` prefixes), tolerates
 * malformed attributes through {@link ErrorHandler}, and delegates the DOM →
 * {@link MathMLElement} mapping to {@link ElementsToMathMLAdapter}.
 */
class XmlToMathMLAdapter {
    constructor(elementsConvertor, errorHandler) {
        this._xml = '';
        this._elementsConvertor = elementsConvertor;
        this._errorHandler = errorHandler;
        // Since @xmldom/xmldom 0.9 the deprecated `errorHandler`/`locator` object
        // options were replaced by a single `onError(level, message)` callback.
        this._xmlDOM = new DOMParser({
            onError: (_level, message) => this._fixError(message),
        });
    }
    /**
     * Parses the given markup and returns its `<math>` elements as trees.
     *
     * @param xml - the MathML/XML markup to parse.
     * @returns one {@link MathMLElement} tree per `<math>` element found.
     */
    convert(xml) {
        this._xml = this._removeLineBreaks(xml);
        this._xml = this._removeMsWordPrefixes(this._xml);
        return this._elementsConvertor.convert(this._mathMLElements);
    }
    _fixError(errorMessage) {
        this._xml = this._errorHandler.fixError(this._xml, errorMessage);
    }
    _removeLineBreaks(xml) {
        const LINE_BREAK = /\n|\r\n|\r/g;
        return xml.replace(LINE_BREAK, '');
    }
    _removeMsWordPrefixes(xml) {
        const MS_WORD_PREFIX = /mml:/g;
        return xml.replace(MS_WORD_PREFIX, '');
    }
    get _mathMLElements() {
        let document = this._parse();
        if (this._errorHandler.isThereAnyErrors()) {
            this._errorHandler.cleanErrors();
            document = this._parse();
        }
        const elements = document === null || document === void 0 ? void 0 : document.getElementsByTagName('math');
        // xmldom 0.9 exposes its own Element type, which is structurally compatible
        // with the DOM Element used downstream but does not overlap for TS, so the
        // cast must go through `unknown`.
        return elements ? Array.from(elements) : [];
    }
    _parse() {
        try {
            return this._xmlDOM.parseFromString(this._xml, 'text/xml');
        }
        catch (_a) {
            // Since xmldom 0.9 a fatalError (e.g. a value-less attribute) aborts
            // parsing and is thrown as a ParseError. `onError` has already recorded
            // and fixed it on `this._xml`, so the caller re-parses the corrected XML.
            return undefined;
        }
    }
}

/**
 * Recovers from the malformed-attribute errors commonly produced by MS Word
 * exports (e.g. `<mfenced open='{' close= >`). It inspects the parser error
 * message and rewrites the offending value-less attribute out of the XML so the
 * markup can be re-parsed successfully.
 */
class ErrorHandler {
    constructor() {
        this._errors = [];
    }
    /**
     * If the parser error is a recoverable missing-attribute-value error, records
     * it and returns the XML with the offending attribute stripped; otherwise
     * returns the XML unchanged.
     *
     * @param xml - the current (possibly malformed) XML.
     * @param errorMessage - the message reported by the parser.
     * @returns the (possibly corrected) XML.
     */
    fixError(xml, errorMessage) {
        if (!this._isMissingAttributeValueError(errorMessage))
            return xml;
        this._errors.push(errorMessage);
        return this._fixMissingAttribute(errorMessage, xml);
    }
    isThereAnyErrors() {
        return this._errors.length > 0;
    }
    cleanErrors() {
        this._errors = [];
    }
    _fixMissingAttribute(errorMessage, xml) {
        const missingAttribute = this._attributeNameFromError(errorMessage);
        if (missingAttribute)
            return xml.replace(this._matchMissingValueForAttribute(missingAttribute), '');
        while (this._mathGenericMissingValue().exec(xml)) {
            xml = xml.replace(this._mathGenericMissingValue(), '$1$3');
        }
        return xml;
    }
    // Only the "missed value" warning carries the offending attribute name. The
    // "AttValue: ' or \" expected" fatalError does not, so it falls back to the
    // generic strip above.
    _attributeNameFromError(errorMessage) {
        var _a;
        return (_a = errorMessage.match(/attribute "([^"]+)" missed value/)) === null || _a === void 0 ? void 0 : _a[1];
    }
    _matchMissingValueForAttribute(attribute) {
        return new RegExp(`(${attribute}=(?!(\"|\')))|(${attribute}(?!(\"|\')))`, 'gm');
    }
    _mathGenericMissingValue() {
        return /(\<.* )(\w+=(?!\"|\'))(.*\>)/gm;
    }
    _isMissingAttributeValueError(errorMessage) {
        return ((!!errorMessage.includes('attribute') && !!errorMessage.includes('missed')) ||
            errorMessage.includes('attribute value missed') ||
            // Since xmldom 0.9 a value-less attribute written as `attr=` is reported
            // as this fatalError instead of a recoverable warning.
            errorMessage.includes('AttValue: \' or " expected'));
    }
}

/**
 * Maps parsed DOM {@link Element}s into the library's internal
 * {@link MathMLElement} tree. The mapping is iterative (explicit stack) so very
 * deeply nested markup cannot overflow the call stack.
 */
class ElementsToMathMLAdapter {
    /**
     * Builds {@link MathMLElement} trees from the given DOM elements.
     *
     * @param els - the root DOM elements to convert (e.g. the `<math>` elements).
     * @returns the corresponding internal element trees, in document order.
     */
    convert(els) {
        const result = [];
        // Iterative depth-first build with an explicit stack, so deeply nested
        // markup cannot overflow the call stack. Each node is linked into its
        // parent's `children` array by reference; children are pushed in reverse so
        // they are appended in document order.
        const stack = [];
        for (let i = els.length - 1; i >= 0; i--)
            stack.push({ el: els[i], target: result });
        while (stack.length > 0) {
            const { el, target } = stack.pop();
            if (el.tagName === undefined)
                continue;
            const node = this._convertElement(el);
            target.push(node);
            if (!this._hasElementChild(el))
                continue;
            const children = Array.from(el.childNodes);
            for (let i = children.length - 1; i >= 0; i--)
                stack.push({ el: children[i], target: node.children });
        }
        return result;
    }
    _convertElement(el) {
        return {
            name: el.tagName,
            attributes: this._convertElementAttributes(el.attributes),
            value: this._hasElementChild(el) ? '' : el.textContent || '',
            children: [],
        };
    }
    _convertElementAttributes(attributes) {
        return Array.from(attributes).reduce((acc, attr) => Object.assign({ [attr.nodeName]: attr.nodeValue === attr.nodeName ? '' : attr.nodeValue }, acc), {});
    }
    _hasElementChild(el) {
        const childNodes = el.childNodes;
        return !!childNodes && childNodes.length !== 0 && this._isThereAnyNoTextNode(childNodes);
    }
    _isThereAnyNoTextNode(children) {
        return Array.from(children).some((child) => child.nodeName !== '#text');
    }
}

/**
 * Wires up and returns the parser that turns a MathML string into element
 * trees, with its element mapper and error handler.
 *
 * @returns a ready-to-use {@link XmlToMathMLAdapter}.
 */
const makeToMathElementsConverter = () => {
    const elementsToMathMLAdapter = new ElementsToMathMLAdapter();
    const errorHandler = new ErrorHandler();
    return new XmlToMathMLAdapter(elementsToMathMLAdapter, errorHandler);
};

/**
 * Public entry point of the library: converts a MathML string into LaTeX.
 *
 * The conversion runs in two stages — the MathML is parsed into an internal
 * element tree (see {@link makeToMathElementsConverter}), then each top-level
 * element tree is converted to LaTeX iteratively (see {@link convertTreeToLatex}).
 *
 * @example
 * MathMLToLaTeX.convert('<math><mrow><mn>a</mn><mo>+</mo><mn>b</mn></mrow></math>');
 * // => 'a + b'
 */
class MathMLToLaTeX {
    /**
     * Converts a MathML string to its LaTeX representation.
     *
     * @param mathml - the MathML markup to convert.
     * @returns the equivalent LaTeX string (trimmed).
     */
    static convert(mathml) {
        const mathmlElements = makeToMathElementsConverter().convert(mathml);
        return mathmlElements
            .map((mathMLElement) => convertTreeToLatex(mathMLElement))
            .join('')
            .trim();
    }
}

export { MathMLToLaTeX };
